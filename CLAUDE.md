# EngrCAD

**Detailed design rationale lives in [design.md](design.md)** (architecture, conventions,
algorithm choices, numerical lessons, roadmap); each `src/*` project also has a
`README.md` describing its contents. **[todo.md](todo.md)** is the idea backlog
(open work only — completed items are removed as they land; largely harvested from a
survey of geometry3Sharp at `C:\Users\chris\projects\git\geometry3Sharp` — study the
referenced g3 classes before implementing an item).

**Documentation upkeep is part of every change, not an afterthought.** When a feature
lands, the SAME integration pass must update: (1) the owning project's `README.md`;
(2) this file's current-status paragraph and roadmap (including any numerical lessons
learned — they are this project's institutional memory); (3) `design.md` when a design
decision or algorithm rationale was made; (4) `todo.md` (remove the done item, file
residual follow-ups as new open items); and (5) **the docs site** (`docs/`) for
user-facing features — an example page with a ` ```csharp render:<id> ` fence per
`docs/writing-examples.md`, verified with `dotnet run --project tools/EngrCAD.DocsGen
-- docs` (snippets are compiled/executed/rendered, so stale examples fail the build).
XML doc comments on new public API feed the generated API reference. A feature
without docs is not done.

A CAD application in modern .NET built around a **hybrid geometry kernel** that natively supports three representations:

- **B-Rep** — parametric surfaces (planes, conics, NURBS) wrapped in topology, for precision modeling
- **Implicit** — signed distance fields (SDF) as an AST of primitives and operators, for lattices and organic blends
- **Mesh** — discrete half-edge triangle meshes, for rendering, FEA, and 3D printing

A distinctive design goal is **LINQ-native geometry querying**: a custom `IQueryable` provider inspects expression trees and routes spatial predicates to spatial indexes (BVH/octree) instead of linear scans.

## Current status

Phases 1–4 plus Query v1 implemented (~NNNN tests): `EngrCAD.Core` (math + spatial incl. `Bvh.Nearest`), `EngrCAD.Mesh` (half-edge engine: booleans with seam zipping, Loop subdivision, QEM decimation, `MeshIntersection` surface-crossing queries; **deformation/analysis foundation**: `LaplacianMeshSmoother` (implicit cotangent fairing, (M + λL)x′ = Mx, dimensionless scale-free TimeStep, degenerate cotangents fall back to uniform weight under the 1e-13-relative sliver guard, negative cot sums sign-clamped so L stays PSD) + `LaplacianMeshDeformer` (bi-Laplacian soft handles, rim/pins hard-substituted bit-identically so `DeformRegion` composes with `MeshRegionOperator` reinsertion), `MeshLocalParam` (Schmidt-style discrete exp map in Dijkstra settle order; trig-free minimal-rotation transport `v·c + axis×v + axis(axis·v)/(1+c)`, stable for all c > −1; exact on planes, ≤2% on a developable tube, ≤5% radial on a 35° sphere cap), `MeshIsoCurves` (per-vertex scalar contours; one crossing per undirected edge from the lower-indexed vertex — the boolean seam lesson — so endpoints are bit-identical and chains assemble combinatorially), `DijkstraGraphDistance` (edge-graph geodesics on `IndexPriorityQueue`), and `MeshIcp` (point-to-plane, 6×6 about the correspondence centroid via `SparseCholesky`; **refuses singular correspondence geometry** — all-planar leaves 3 DOF free — reporting Converged = false rather than Tikhonov-damping, the MateSolver convention; landing it made Core's `SymmetricEigen3` public with BOTH orderings and deleted Mesh's duplicated Jacobi solver). All backed by **`EngrCAD.Core.Solvers`** — dependency-free sparse mini-library (CSR `PackedSparseMatrix` with symmetric-upper storage, Jacobi-preconditioned `SparseSymmetricCG` with convergence as a return value, up-looking `SparseCholesky` with an opt-in `SparseOrdering.Amd` fill-reducing permutation (`AmdOrdering`: quotient graph, element absorption, mass elimination, hash-detected supervariables, assembly-tree postorder) — natural stays the default because reordering is not bit-identical arithmetic; also the substrate for future sketch constraints and FEA)), `EngrCAD.Implicit` (SDF AST), `EngrCAD.BRep` (curves/surfaces/topology foundations), `EngrCAD.Interop` (the full conversion triangle: implicit→mesh via manifold Surface Nets — which now visits only the blocks the surface can reach (`SurfaceCull`, bit-identical output, 1.6–3.0×) — B-Rep→mesh via tessellation — whose trimmed-face tiers now put the natural grid's **interior rows into the base triangulation** (rowed strips/periodic bands/pole fans; refinement demoted to residual duty), so tall trimmed bands converge instead of being carried by midpoint bisection —  mesh→implicit via `MeshSdf` with angle-weighted pseudonormals — plus an opt-in `MeshSignSource.WindingNumber` sign source that accepts open meshes, default unchanged), `EngrCAD.Query` (BVH-backed IQueryable), **`EngrCAD.Modeling` (the unified `Shape` API: model once with one vocabulary — primitives incl. `MakeSphere`/`MakeTorus`, extrude/revolve/sweep, booleans, smooth blends/offset/shell/lattice, transforms — then decide the representation at the end: `ToBrep()`/`ToImplicit()`/`ToMesh()`; lowering bakes accumulated transforms into construction inputs for exact B-Reps, bridges non-native nodes through tessellation + `MeshSdf` or Surface Nets, and `Explain(target)` reports each node as Native/Bridged/Impossible — see `src/EngrCAD.Modeling/README.md` for the support matrix; `Shape.From(...)` wraps raw engine geometry so designs can drop down to any engine API and come back — and `Shape.From(path)` imports .stl/.obj/.off via `MeshReader.ReadAndRepair` (out-param `MeshRepairReport`; hole filling opt-in; docs `examples/import.md`); **sketching**: `Sketch` (fluent builder — `LineTo`/`ArcTo`/`ArcThrough`/`BezierTo`/`QuadraticTo` — plus Rectangle/RoundedRectangle/Circle/Polygon/Slot primitives, `WithHole`, exact `Area()`) placed by `SketchPlane` and consumed by `Shape.Extrude/Revolve/Sweep`; **sketch constraints**: `Sketch.Constrain()`/`ConstrainedSketch` (SketchConstraints.cs/SketchConstraintSolver.cs) — the full coincident/horizontal/vertical/parallel/perpendicular/tangent/equal/concentric/fix + distance/angle/radius vocabulary solved variationally by analytic-Jacobian LM with a rank-revealing DOF report (MateSolver doctrine throughout: the drawn configuration is seed AND branch selector, under-constrained is normal and reported, contradictions and stationary configurations are named, a failed solve leaves the drawn sketch unchanged); solved output is an ordinary `Sketch`, so regions/extrudes/features downstream are untouched. Two lessons from it: **adjacent line–arc tangency must be constrained as d̂·(c − J) = 0 at the shared joint, never as center-to-carrier-distance = r** — the distance form leaves the tangency foot only second-order constrained, so a 1e-9-converged solve carries √(2r·1e-9) ≈ 1e-4 of foot slop and the near-zero singular value corrupts the rank; and **the rank floor is 1e-6 relative singular values, not the mate solver's 1e-8** — 1e-8² sits below pivoted elimination's own round-off at sketch sizes (a rank-9 Jacobian over 14 variables measurably reported rank 10, an arithmetic impossibility); **twisted/tapered extrusion** (OpenSCAD `linear_extrude(twist, scale, slices)`): `Shape.Extrude(sketch, height, twist, scale, slices)` — a pure taper is B-Rep-**Native** (a linearly scaled straight side sweeps an exact PLANE through the scaling centre, so the solid is a ruled two-section loft; holed sketches wait on loft-sections-with-holes), a twist is B-Rep-Impossible and lowers to a direct mesh section sweep (`TwistedExtrusion`) wrapped in a mesh SDF for implicit — two lessons: the PROFILE must be subdivided to match the angular step or volume converges only FIRST order in slices (a wall panel lerps a long edge's lateral rotation linearly; measured deficit 286→33→8.2 at 8/64/256 slices, twist-matched subdivision restores second order), and the subdivision points are exactly collinear so earcut filters them from the caps while walls still reference them — the caps re-expand chords over dropped runs (MeshPlaneCut's collinear-chord zip, generalized to the multi-loop layout) so the mesh closes by index; **resize/scale** (OpenSCAD `resize()`/`scale([x,y,z])`): `Shape.Scale(x,y,z)` + `Shape.Resized(newSize, auto?)` over the new measured `Shape.Bounds(quality)` (mesh-lowering bounds, eagerly at the call, scaling about the origin; support rides the existing non-uniform-transform policy — extrusion-family exact, ellipsoid-family Impossible by name, implicit bridged unless factors equal); **heightmap terrain** (OpenSCAD `surface()`): `Shape.Heightmap` over `Heightmap.Mesh/ReadDat/ReadPng` — manifold-by-construction terrain solid (top grid + perimeter walls + ONE n-gon base over the same bottom vertices, welded by index), with a dependency-free grayscale-PNG reader (BCL `ZLibStream` inflate, all five scanline filters, 8/16-bit; color/palette/interlaced rejected by name); lines/arcs→exact NURBS profiles for B-Rep, and the sketch's exact 2D signed distance (`SketchRegion`: per-segment exact distances + even–odd parity over precomputed y-monotone pieces) makes sketch extrusions and full revolutions implicit-**Native** via `Sdf.ExtrudedRegion`/`RevolvedRegion` (`IPlanarRegion` in EngrCAD.Implicit); axis-touching full-turn revolves (vases) work in **all** representations — `RevolveFullTurn` drops on-axis stretches (they revolve to nothing) and treats their endpoints as poles with no junction edge, splitting pole-to-pole generators at their midpoint so every face keeps a boundary loop (the sphere case); full-turn+holes/single-closed-curve stay B-Rep-Impossible and partial axis-touching revolves are rejected at construction; **hole feature**: `Shape.Drill(HoleSpec, points, depth, plane)` — simple/counterbore/countersink tools are axis-touching revolved sketches subtracted per point (exact everywhere; the tool overshoots the surface so booleans never see coplanar faces, and the countersink cone continues its slope so the surface diameter stays exact), with `StandardHoles` supplying metric ISO 273 clearance / DIN 974 cbore / ISO 10642 csk / coarse tap pilots / Tappex Trisert pilots (Trisert table flagged verify-against-datasheet). **Drill points**: `HoleSpec.WithTipAngle` gives a blind hole the cone a real twist drill leaves (`StandardHoles.TwistDrillPoint` 118°, `SplitDrillPoint` 135°) — exact everywhere, since the tool stays ONE axis-touching revolve with the cone as the profile run down to the apex, and the tessellated result is an n-gon prism plus an n-gon pyramid matching the discrete truth as an identity at 32/64/128 segments. **Depth is to the SHOULDER** with the tip reaching `(d/2)/tan(θ/2)` further (ASME Y14.5 / ISO 129), which makes the feature strictly additive — the same depth removes the same cylinder either way, so adding a point can never silently shorten an existing hole; `TipLength` is the number to check against the far face, and the default stays flat. **Cross-plane hole validation**: tools on DIFFERENT placement planes are now checked too (the old comment claiming they "are not compared" sat above a test that was itself building two coaxial overlapping tools). Each tool is covered by bounding cylinders about its axis and a pair is cleared by either of two sufficient conditions — axis-SEGMENT distance exceeding the summed radii, or a separating axis (a finite solid cylinder is convex; support extent `|n·d|·halfLength + r·√(1−(n·d)²)`). Neither subsumes the other: segment distance settles skew and offset-parallel layouts, but two COLLINEAR tools bored towards each other have axis segments at zero radial distance however much web is left, so only the axial projection separates them. Refinement (only when the whole-tool bound is ambiguous) is slab-by-slab over `HoleSpec.ToolSilhouette` — the same source of truth `ToolProfile` is built from — **sound in the accept direction at any subdivision** and exact wherever a slab's radius is constant, so the residual error is a conservative refusal in a near-tangent band, which is what a boolean cannot survive anyway. Kernel work that enabled it: analytic plane⊥`RevolvedSurface` intersection circles (multi-crossing generator bisection, phase-aligned to u=0), `SplitBandByWrapCurve` generalized to full-turn `RevolvedSurface` bands (generator `CurveSegment` split; single-loop pole bands allowed) with **vCut refined geometrically** (projection error ~1e-7 shifts cone rings radially past weld tolerance — solve the generator's axial coordinate against the exact cut curve instead), and `ProbePoint` probing pole-bounded single-loop faces halfway to the pole without the upward-ray parity check (which cannot see a rim below the probe). Note: CSG difference SDFs are correct-sign lower bounds near the subtracted tool's fictitious faces — don't assert exact distances there in tests. **B-Rep queries** (`BrepQueries` in BRep): `IsPlanar` (incl. extruded-line sides)/`IsCylindrical`/`IsLinear`/`IsCircular`/`Length`/`Bounds`/`IsConvex` + adjacency (`face.Edges()`, `solid.FacesOf(edge)`) + `PlanarFacesWithNormal`, plus **`BrepSelection`** — the build123d `sort_by`/`group_by`/`filter_by` capability as LINQ extension methods (string selectors deliberately not imitated): `SortAlong`/`Extreme`/`Highest`/`Lowest` (a planar face ranks by its exact plane offset ONLY when the direction is parallel to its normal — a plane's stored origin is an arbitrary in-plane point, so a box side wall grouped along Z must rank by its centre), `GroupAlong`/`GroupByCoplanar` (weld-tier; non-planar faces stay singletons — nothing silently dropped), `FilterBy(SurfaceKind)`/`Kind()` (revolved generators classified by SAMPLING five points, because the kernel spells circular generators three ways — `Circle3d`, `CurveSegment`, rational NURBS arc, the sphere/torus factories using the last — and a curve-type switch would classify identical geometry differently), `GroupByRadius`/`NthByRadius(n)` (negative = from the largest; out-of-range names the radii found), and `Area` (planar exact via the boundary integral with closed-form arc terms; curved faces midpoint-quadrature over the pulled trim loops, ~1–2% — ordering-grade for `LargestByArea`, mass-property grade stays `BrepMassProperties`' job); every query has a serializable GeometryRef spelling (`FaceSetRef.OfKind/NthByRadius/GroupAlong`, `FaceRef.LargestByArea/Largest`, `EdgeSetRef.NthByRadius`; docs page `selection.md`) — the selection vocabulary for **rim features**: `Shape.Chamfer(setback, faceSelector)`/`Fillet(radius, faceSelector)` → `Filleting.ChamferRim/FilletRim` topology surgery (chamfer: straight rims miter with planar trapezoid strips + exact cone bands on circular rims; fillet: tangent-continuous line+arc rims via quarter-cylinder/torus-segment bands sharing junction arcs; **sharp corners miter on exact bicylinder ellipses** — at a rim corner only TWO of the three edges are blended, so the two side faces keep their shared sharp edge and the two equal-radius quarter-cylinders meet in a crease; a sphere there is tangent to all three planes at single POINTS, so the cross-section would jump from round to sharp at the tangency plane and not close. The branch is read straight off the two points the surgery already computed — `Ellipse3d(centre = top − up·r, up·r, bottom − centre)` over [0, π/2], with the circular junction arc of tangent-continuous rims being literally the |bottom − centre| == r specialization (kept on its own rational-arc path so existing rims stay bit-identical). Convex AND reflex corners are exact — a reflex band reaches past its edge's end to meet the miter, an extension that is exactly zero in the convex case so the direction vector stays bit-identical. Filleted prisms are analytic with SIGNED turns: V = A₀h − P₀r²(1−π/4) + (Σ tan(θᵢ/2))r³(5/3−π/2), matched to ~1e-4 relative incl. an L-shape; sharp corners at ARC rim edges stay rejected (torus∩cylinder is not a conic), as does a radius whose mitered offsets cross — naming the edge it consumed. **Edge sets**: `Filleting.FilletEdges`/`ChamferEdges`/`RimFacesFor` + `BrepQueries.RimEdges(face)`/`ConvexEdges(solid)` + `Shape.FilletEdges`/`ChamferEdges` accept any selection covering complete planar face rims; partial runs are rejected BEFORE any surgery (rim surgery rewrites loops in place, so a late failure would leave a half-edited solid). `ChamferRimAtAngle`/`Shape.ChamferAtAngle` take setback-in-face + angle-from-face, exact to 1e-9. **Whole-solid rounding**: `Filleting.FilletAllEdges(solid, radius)` is the exact morphological **opening** (K ⊖ B_r) ⊕ B_r — boolean-free, nothing to seal: each face keeps its own plane with a shrunk boundary, each edge becomes a cylindrical band about the ERODED edge line, each vertex a spherical patch on the eroded vertex bounded by great-circle arcs (a box → 26 faces / 48 edges / 24 vertices, every face full-domain so it all tessellates on the natural grid; volume converges quadratically onto Steiner's V = V₀ + A₀r + (r²/2)Σℓθ + (4π/3)r³, deficit ratio exactly 4.0 per halving). Concave edges, valence≠3 vertices and corners with no incident face perpendicular to the other two are refused — that last restriction is what keeps the patch an exact surface of revolution (the spherical triangle becomes a lune closed by an equatorial great circle), so boxes, convex prisms and sheared boxes keep the exact lune; GENERAL trihedral corners (a tetrahedron's, a drafted block's) now build a TRIMMED spherical-triangle patch instead — one face normal becomes the pole axis, the two arcs meeting its tangency are exact MERIDIANS on the u domain boundaries, and only the diagonal arc trims the interior; a dedicated trimmed tier (TriangulatePoleGrid) meshes it as a structured column grid EXCLUDED from midpoint refinement, because the refiner's flat uv metric overstates u chords near a pole without bound (measured: apex cascades folded 52 facets at 16/8 and half-step slivers broke the 48/24 floor); Steiner on a regular tetrahedron (dimensions of the ERODED body) matches to 1e-4 with quadratic convergence. **The load-bearing detail**: every corner arc is a `CurveSegment` over `Circle3d`, NEVER a rational NURBS arc — a corner patch is a revolve sampled at even ANGLES, so an arc parameterized any other way samples to different points and the patch stops welding to its band. **Variable-RADIUS fillets landed** (`FilletRim`/`FilletEdges` law overloads + `Shape.Fillet(radiusAt, faces)` + `VariableFilletRimFeature`): along a straight run the band is the RULED skin between its two end quarter arcs, and because those are equal-weight rational conics on one frame, lerping their points equals lerping their homogeneous control points (the denominators cancel), so every intermediate section is a TRUE circle of the interpolated radius, G1 with both neighbours. The band's top and bottom boundaries must be RAILS on the band (`LoftRailCurve`) rather than free-standing lines, or the loft's v grid samples the boundary at a density the straight edge polyline does not and the face T-junctions against its neighbours. What is refused is the CORNER: two variable-radius bands are cones (the family of circles with linear centre and linear radius IS a cone, apex where the radius vanishes) that do not circumscribe a common sphere, so they meet in a quartic with no conic miter — while a CONSTANT law across a sharp corner makes them equal-radius cylinders again, which DO share an inscribed sphere, so the exact bicylinder ellipse is back and the refusal is about the law rather than about sharp corners. Variable-SETBACK chamfers landed earlier: a corner law linear along each edge — a linearly varying perpendicular inset of a straight edge is still a straight LINE and a constant top:side ratio keeps each strip's four corners coplanar (s0*d1 = d0*s1), so strips stay exact planes and miters exact intersections; arcs and full circles demand a locally constant law, since a circle offset by a varying amount is a spiral. Partial edge RUNS landed too: a contiguous selection stopping mid-rim blends with exact SETBACK terminations (the band cross-section is already planar), grouped and validated all-or-nothing before surgery; three blended edges meeting at a vertex still refuses toward FilletAllEdges). Surgery gotchas learned: derive rim circles from edge samples (`ActualCircle` — `Underlying` circles of translated extrusion tops sit at the base), build all new rim edges in the top face's traversal direction (fixes every sense), TRIM domain-driven neighbor surfaces (extruded/revolved grids ignore loops — shortened bands must get shortened surfaces), and don't double-flip arc spans already measured from traversal-ordered points. **OCCT-parity solid features in the Shape graph**: `Shape.Loft(sections, style)` (sketches placed by `SketchPlane`s or raw `Profile`s; `LoftShape` is a graph LEAF whose inputs are profiles, transform baked into the section curves — B-Rep-Native rigid + uniform scale, sheared placements Impossible because chord-length parameterization and least-twist alignment are metric; section-count mismatches and holed sections fail at the CALL), `Shape.LoftAlong(section, spine, count, scale, twist)` (the evolution-law pipe shell: stations in the SAME `SweptSurface` RMF frames `Sweep` uses, generated profiles feeding `Loft` unchanged), `Shape.Draft(angleDegrees, neutralOrigin, pull, faces?)` (per-face angles by chaining — exact and composable), `Shape.Shell(t, openings)` (exact INWARD hollow via `Shelling.Shell` — deliberately a separate overload from the SDF `Shell(t)` onion `|d| − t/2`, whose skin straddles the surface: two calls, two geometries, never one call with representation-dependent walls; the SDF shell's B-Rep-Impossible message names the exact overload), and `Shape.RoundEdges(r)` (`Filleting.FilletAllEdges`; radius scales with uniform placement). All four bridge implicit through the exact tessellation and refuse what the kernel refuses BY NAME at lowering; docs page `loft-draft-shell.md`. **Patterns**: `PatternLinear/PatternCircular` (balanced union trees) + **`LocationSet`** ("place this feature at these N poses" as ONE value — `Grid`/`Polar`/`PolarArc`/`Hex`/`Linear`/`At`, composed by `Translate`/`Rotate`/`+`, each location a point + in-plane rotation; one value feeds `Shape.Drill`, `Shape.Pattern(locations, plane?)` — which stamps an origin-modeled shape by conjugation through the plane frame, exactly `PatternCircular` of the pre-translated shape for a polar set — and `ComponentAssembly.Place`; serializable the GeometryRef way: `Descriptor` (`grid(3,2,10,8)`) parses back bit-for-bit since Parse re-runs the same deterministic constructors) + **`ExtrudeUntil`/`CutUntil`** (build123d's `until=NEXT/LAST`: probe rays from the sketch region's strict interior against the body mesh; the stop must be ONE plane ⊥ the extrusion (hits clustering within 1e-6×extent) — curved/slanted stops, overhanging profiles and tangent grazes refuse loudly naming the candidates; overshoots carry the Drill never-coplanar lessons (a Next boss reaches INTO the body, a Next cut into the void behind the first wall, a cut tool also clears the TOP when the plane sits on a face — and ray-triangle barycentric bounds are INCLUSIVE ±1e-9, because an edge crossing dropped by both triangles leaves a ray that enters and never exits); resolution eager per the `Bounds`/`Resized` policy, result = ordinary extrude+boolean nodes so Explain stays honest) + **`Packing`** (build123d's `pack`: deterministic shelf packer over `Shape.Silhouette` footprint bounds — overhangs get their room — gap honored to parts and plate edges, placements in input order, loud refusal naming the part that ran out of plate; docs `packing.md`; **builder-style authoring prototyped and DECLINED** — design.md §6b records why: C# has no ambient context managers, a mutable local with `|=`/`-=` already IS the accumulation mode, and the named `Union/Subtract/Intersect` methods already fix the `|` vs `-` precedence trap; comparison committed as `BuilderPrototypeTests`) + `BrepBoolean` disjoint fast path (no curves → whole-body classification, multi-shell unions, clone-reversed swallowed Difference tools — in-place `ReverseFace` would duplicate edge uses) + face-bounds pre-filtering and identical-carrier curve dedupe (patterned faces sharing planes produced the same split curve per pair, breaking the tracer). **STL export**: `StlWriter` (binary, merged multi-part with transforms) + `--export .stl`. **3MF/AMF/OFF export**: `ThreeMfWriter` (OPC zip package over BCL `ZipArchive` + LINQ-to-XML — content types, `.rels`, `3D/3dmodel.model`) and `AmfWriter` (ISO/ASTM 52915 XML) both take `MeshExportPart` lists (mesh + transform + name + optional RGB): one named object per part, transform BAKED into vertices (the merged-STL flattening; mirror flips winding — 3MF requires outward facets), distinct colors deduped into one material table; `OffWriter` is `OffReader`'s twin (n-gons as-is, R-format doubles, round-trips exactly — remember the reader fan-triangulates, so read-back face count is Σ(deg−2)). All three wired into `--export` and the MCP `export` tool. **Parametric features** (`Features.cs`/`FeatureHistory.cs`/`StandardFeatures.cs`): FeatureScript-in-C# — `Feature` classes with `[Param]` properties (reflection metadata cached per type; init-only setters writable via reflection for JSON overrides), `FeatureContext` (Body + lazily lowered B-Rep for selectors + `TopPlane`), `FeatureHistory.Regenerate` (prefix caching keyed by instance-identity+parameter snapshot — a fresh instance always re-runs, which safely covers non-`[Param]` inputs like sketches/hole specs/selectors; validation-first; failure keeps the last good prefix and skips the rest; suppression; `SaveParameters`/`LoadParameters` JSON). **Feature registry + whole-history persistence** (`FeatureRegistry.cs`): `FeatureRegistry` lists every feature type with instance-free `[Param]` metadata and an honest `CanCreate`/`Reason` pair (the UI-insertion catalogue), `TryCreate` builds one from data, and `FeatureHistory.SaveHistory`/`LoadHistory` round-trip the WHOLE history — type, name, suppression, `[Param]` values (through the SAME `SerializeValue`/`ApplyParameters` seams as `SaveParameters`, so the two files cannot drift) plus **constructor inputs** via the `Feature.SaveInputs` virtual: holes serialize their `HoleSpec` (kind + factory args + tip angle) and points, and sketch extrude/revolve serialize the `Sketch` EXACTLY through the public `Curve2d` vocabulary (`ToCurves`/`FromCurves` — nothing flattened), so the standard vocabulary short of code inputs round-trips with save→load→save a byte-identical fixed point and load→regenerate bit-identical volumes. What cannot round-trip is named, not hidden: `BooleanFeature` (a `Shape` graph), `VariableChamferRimFeature` (a law that is code), `ComponentFeature` (a catalogue object), `FromFunc` lambdas — still WRITTEN to the file, skipped on load with a per-record warning unless the caller's `resolveOpaque` hook supplies the instance (`HistoryLoadResult.Complete`); user types join via `Register<T>` or `Register(type, factory)`. **Whole-document persistence** (`DocumentFile.cs`): a `Document` is a `Scene` plus the `MateSet`s a scene cannot own, saved as ONE versioned JSON envelope — tabs, parts (name/colour/transform/display mode/`ClippedBySection`/debug modifiers), each part's `FeatureHistory` via `SaveHistory`, assemblies with occurrences and explode offsets, mates via `SaveMates` (parsed back in rather than restated, so the mate vocabulary stays in `MatePersistence`), annotations, results and `FieldDisplay`. **A document is its construction history, not its geometry** — a history-backed part regenerates on load and is still parametric — and the parts with no recipe (raw `HalfEdgeMesh`, imported `.stl`, `Sdf`, a `Shape` graph built in code) are handled EXPLICITLY rather than dropped: their display mesh is embedded as a binary-exact base64 **snapshot** and `DocumentLoadResult.Snapshots` names every part that came back that way, so a host can say "these are not parametric" instead of a UI discovering it when a parameter changes nothing (`EmbedGeometry = false` writes a recipe-only file where those become an explicit "no geometry" record naming the reason). Embedded rather than an external path, deliberately: a document that points at its neighbours is a *manifest*, and the reference breaks the first time the file is moved — filed rather than built for the one case it wins (a scan mesh too big to inline). Loading returns warnings and never throws (opaque features via the `ResolveOpaqueFeature` hook, selector-backed annotations — `Annotation.SaveJson` mirrors `Feature.SaveInputs`, returning null for a lambda — catalogue parts whose geometry loads but whose `HardwareComponent` cannot); only a bad envelope or an unknown version throws. **The fixed point is the test that earns its keep**: `save → load → save` being byte-identical is the ONLY check that catches a field written but never read, a default that reloads as a different default, or an ordering that is not a function of the model — all of which pass a volumes-and-poses comparison. It caught one immediately: a snapshot's `"source"` field naming the geometry type it came from is useful to a human and *cannot* round-trip, because a `BoxShape` snapshot reloads as a `HalfEdgeMesh` and the second save writes a different name — hence the rule that **an informational field either round-trips or does not belong in the file** (the actionable half is computed at load, in `Snapshots`). Scope stated honestly: a file carrying opaque records is smaller the SECOND time by exactly the records the load warned about, then a fixed point — "a record was reported and dropped" and "the file is drifting" are different things and are asserted separately. **Undo/redo** (`DocumentEdits.cs`/`UndoStack.cs`) is `MeshChange` at document granularity: a `DocumentEdit` captures whatever it is about to overwrite and restores that on `Revert`, an `UndoStack` runs them with `Group(...)` collecting several into ONE user-visible step (nesting; a group of one keeps its own label; an empty one pushes nothing), and the vocabulary is what a UI performs — `SetParameter`/`SetParameters`/`Suppress`/`AddFeature`/`RemoveFeature` (all through the SAME JSON seam as `SaveParameters` and MCP `set_param`, ending in `Part.Regenerate()`), `Rename`/`SetColor`/`SetTransform`/`SetDisplayMode`/`SetClippedBySection`, `AddOccurrence`/`RemoveOccurrence`/`Repose`/`SetExplodeOffset`, `AddMate`/`RemoveMate`, `AddAnnotation`/`RemoveAnnotation`. **The document SERIALIZER is the test oracle** — every undo asserts `Save()` is byte-identical to the pre-edit save, because a hand-written state comparison agrees with a broken revert as happily as with a correct one, and that is what forced `Assembly.Insert`/`MateSet.Insert`/`Part.InsertAnnotation` beside the `Remove`s (re-adding appends AND re-derives the occurrence name, so an undone removal would come back last and possibly renamed — which a field-by-field check passes). **A refused edit is not history**: `Part.Regenerate` keeps the previous complete body on failure but the bad parameter is still set, so a failing edit takes its own value back, rebuilds, and throws `DocumentEditException` (carrying the `RegenerationResult`) without being pushed or discarding the redo stack; a `CompoundEdit` reverts the members that already succeeded. Regeneration caching survives undo **by construction** — the cache key is the parameter snapshot, so restoring the old value restores the old key and re-runs exactly the prefix a forward edit would (asserted, not assumed). The stack is **session state, not document state**, which is also why it stores edits rather than scene snapshots: the OCAF assessment's "a document snapshot is a value" is the wrong granularity here, since a `Scene` snapshot is not cheap (parts cache meshes, B-Rep and SDF lowerings, feature edges, occlusion) and would either discard those caches or share them into two documents that diverge — hot reload keeps its whole-scene swap because it genuinely rebuilds from source; interactive editing does not. New document-model surface it needed: `Scene.Rename(part, name)` (the tab-uniqueness invariant lives on the scene, the only object that sees every tab), `Assembly.Remove`/`IndexOf`/internal `Insert`, `MateSet.Remove`/`IndexOf`/`Unground`/internal `Insert`, `Part.RemoveAnnotation`/internal `InsertAnnotation`. **Topological naming v1 — face PROVENANCE** (`BrepFace.Provenance`/`DescendsFrom`/`AddProvenance` in BRep; `Shape.Tag(name)`/`TagShape`; `FaceSetRef.Tagged(name)`/`Within(scope)`): the semantic selectors stay the default and answer "what IS this face", but they have one structural blind spot — two identical bosses are indistinguishable to every query in the vocabulary — so `Shape.Tag` says where a face CAME FROM. The B-Rep lowering stamps the name onto every face of its child's solid and faces inherit it wherever one is derived from another; `Tag` is geometrically transparent in all three reps and adds no `Explain` row. **Three decisions carry it.** **(a) Set-valued by construction** — a boolean can split one face into several, so "the face this step made" is not well-formed; narrowing to one is an explicit `FaceRef.One`/`Extreme` claim that fails its cardinality contract loudly, and a scheme promising "the" face would have to guess which fragment, which is how naming schemes come to misresolve silently. **(b) The failure direction IS the safety argument** — inheritance lives at the sites that derive a face from a parent, so a site building from scratch simply does not tag and a query returns FEWER faces, never a face from somewhere else. Landed: the whole boolean pipeline (untouched faces pass through by reference; every `FaceSplitter` fragment and both `BrepBoolean` re-wound-tool sites inherit), `BrepSolid.Clone`, hence `Drill`, patterns, transforms and `Shape.From(solid)`. NOT landed and stated rather than hidden: `Draft`/`Shelling`/`FilletAllEdges` rebuild every face, and rim surgery rewrites the blended face and its trimmed neighbours — all four have a positional parent (`Shelling` already keeps a `Dictionary<BrepFace,int>`), so threading it is mechanical rather than hard and is filed; STEP carries no provenance (a format limit). Each boundary is PINNED by a test, so the documented edge cannot rot. **(c) A tag is REFUSED, not sanitized, when the descriptor grammar cannot spell it** — the descriptor is the cache key and the serialized form and is parsed back by `RefLexer.ReadIdentifier`, so sanitizing (the rule an opaque LABEL follows, being human-read only) would turn `"boss top"` into a descriptor resolving to nothing: a silent misresolve, the one outcome a naming scheme must not have. `FaceSetRef.Within(scope)` was forced out by the first real use — a tag names a boss's cylinder AND its plane while rim surgery wants a planar face, so composing provenance with a semantic query is the normal case. Two assertion traps re-learned while measuring it: `IsPlanar`'s origin is an ARBITRARY in-plane point and a circular loop's face frame origin is its single seam VERTEX (both read the rim, not the centre) — locate a face by `Bounds().Center`. Selector-based references are the topological-naming story (semantic queries re-run per regeneration), and they now have a **typed vocabulary** (`GeometryRefs.cs`): a feature says "I need a plane" by declaring a property — `PlaneRef` → `SketchPlane`, `FaceRef` → one face, `FaceSetRef`, `EdgeSetRef`, `AxisRef` → `Ray3d` — each built from nesting named `BrepQueries` queries (`PlanarWithNormal`/`Cylindrical(r?)`/`RimOf`/`Convex`/`Circular(r?)`/`One`/`Extreme`/`On`/`RimFacesOf`/`OfCylindrical`), an explicit frame as the escape hatch, or a lambda as the last resort, and **each carrying cardinality in the TYPE** — which is what none of the five incumbent selector shapes could express. Three properties make it more than sugar. **(a) The `Descriptor` is the cache key AND the serialized form**: one canonical parseable term (`topPlane`, `extreme(planar([0,0,1]),[0,0,1])`) that `ToString` returns, so `FormatValue` picks it up for the regeneration snapshot with no special case and JSON round-tripping needs one line on each side of `Convert`/`SaveParameters` — one string, three jobs, so they cannot disagree. Lambda-backed refs print `opaque(label)`, are `IsSerializable = false` and load as a WARNING, staying sound as cache keys because the snapshot also carries instance identity and a fresh instance always re-runs. Two traps paid for here: the opaque label is sanitized to characters `System.Text.Json` will not escape (a quoted marker came back from a saved file as `'` noise), and an explicit `AxisRef` keeps an ALREADY-unit direction verbatim rather than dividing again — re-normalizing a unit vector moves it by an ulp, and the descriptor would stop being a fixed point under round-trip. **(b) Resolution is per-`Apply` and NEVER memoized on the reference** — it caches on the `FeatureContext`, which is constructed fresh per applied feature, so validation and `Apply` share one query while an edited model always re-resolves; this is the deliberate opposite of `Mates`, which pins at construction because a mate is a numerical constraint rather than a query, so eager-vs-lazy is a consumer choice, not a rule. **(c) `Feature.ValidateInputs` resolves every declared reference BEFORE `Apply`, all-or-nothing, naming the property** ("Plane: expected exactly one cylindrical face, found 0." — `Filleting.RimFacesFor`'s style, not the operation-named message the deferred rim selector gave), reported as `Failed` with the last good prefix intact. The cost note shaped the design: resolving forces `Lowered`, so a reference needing no body (an explicit plane or axis) never triggers one, a feature declaring none pays nothing, and `[DeferredInput]` opts out inputs handed to the `Shape` graph's own late-resolved selectors (the rim features' face sets) where an early resolve would buy a whole extra B-Rep lowering per regeneration and learn nothing — `AsSelector` carries the input name down so the late failure still names it. `FeatureContext.TopPlane` is now `PlaneRef.TopPlane` resolved against the context (the hard-coded special case is gone; its world-axis-aligned (0,0,z) origin, which drill coordinates depend on, is unchanged), with `PlaneRef.OnTopFace` as the face-frame variant — so todo.md's open behaviour question became a per-feature OPTION rather than a fork. `HoleFeature.Plane`/`ComponentFeature.Face` keep every incumbent spelling (implicit conversions from `SketchPlane` and from `SketchPlane?`, whose null still means the top plane); the rim features' hard-coded `Direction` became a `FaceSetRef` defaulting to the same query; `CircularPatternFeature`'s loose origin/direction pair became one `AxisRef`. Rim-feature numeric lessons from the feature work: arc corner offsets must be computed exactly from the arc's radial (finite-difference tangents carry ~1e-9 angular error that rotates band generators past weld tolerance at scale — only *some* corners crack, whichever exceed 1e-9), and rim features validate that interior loops (holes) stay clear of the shrunk boundary.). **Threads**: `StandardThreads`/`ThreadSpec` (ISO 68-1 basic profile with exact formulas — H = √3/2·P, d2 = d − 3H/4, d1 = d − 5H/4; ISO 261/262 coarse M2–M12 plus the **fine-pitch series** — `Fine(size)` for the first choice (M8×1, M10×1.25, M12×1.5), `Metric(size, pitch)` for second/third choices, `Pitches(size)` listing what a size carries, and `StandardHoles.Tapped(ThreadSpec)` cutting the pilot for any spec; the fine rows store no tap-drill column ON PURPOSE, since every fine tap drill is exactly d − P where the coarse chart rounds to a stock drill (6.8 for M8, not 6.75), and a second column there could drift with nothing to catch it; tap drills consistent with the `Tapped` chart), `Helix3d` (BRep: exact analytic derivatives, closed-form arc length, `Frame3d`-based), `Sdf.Thread` (`ThreadSdf`: solid = 2D trapezoid profile membership in (r, wrap(z − P·θ/2π)) — **sign exact everywhere**, magnitude = exact 2D profile distance scaled by cos(lead angle at root) — lower-bound-style near the surface; clearance = exact profile-normal offset; 45° start/end chamfer cones), `Shape.ExternalThread`/`ThreadedHole(spec, points, depth, plane, clearance)` (implicit-**Native**; mesh via Surface Nets for chamfered/clearance forms — the 3D-printing route with flank-normal clearance, typical FDM 0.1–0.25 mm; **B-Rep-Native for the unmodified profile** — zero clearance, no chamfers — via `HelicalSurface` (co-rotating linear-generator sweep; u NOT periodic since z advances, so the whole wrap-split machinery doesn't apply) + `SpiralArc3d` (exact cap-plane cuts) + `SolidFactory.MakeThreadedRod`: **the entire lateral boundary is ONE boolean-free helical sweep** (root flats are part of the sweep — no coaxial tangent seam ever exists); threaded holes subtract ONE clipped-profile tool (void = max(basic form, r_pilot)) so the only intersecting pairs are helical-band×plane = exact spiral arcs, chain-split via `SplitByClosedCurveChain`; any length works (phase washes out — V = L·(2π/P)·∫½R² ds, Pappus per pitch); chamfered/clearance threads stay B-Rep-Impossible with per-cause Explain messages (chamfer cones and distance-field offsets have no exact B-Rep counterpart); **left-hand threads are Native in all three representations** — `ThreadSpec.LeftHanded()` shares every diameter (handedness is not a different thread), the designation becomes `M8×1.25-LH` which `ThreadCallout` picks up for free, `ThreadSdf` spends one sign on it, and `MakeThreadedRod(leftHand:)` takes a signed axial rate: since every formula there is written in the band's own phase u with z = z_generator + rate·u, negating the rate makes u descend as the rod ascends and three things follow mechanically (a rail's helix anchors on the TOP cap — `Helix3d`'s domain always starts at its own frame's plane — the u=min/u=max band edges swap which cap they are, and both cap loops chain the other way), with counts, Euler and the Pappus volume unchanged and the result the EXACT mirror of the right-hand rod (0 residual on band surfaces, 9e-15 at vertices). **`Mirror(thread)` is therefore Native too**: a reflection across a plane containing the axis IS the handedness flip, so writing a mirrored placement as m = (m·FlipY)·FlipY leaves a proper similarity placing a rod of the opposite handedness, and Mirror(Mirror(x)) is right-handed again (FlipY, not the implicit path's FlipZ, which would reverse the rod's own axis and the profile's axial order); the refusal that remains is a genuinely different one — a sheared or non-uniformly scaled placement cannot re-place a helix at all. Verified cross-representation rather than by shape: every vertex of the mirrored B-Rep tessellation reads 2.4e-15 against the mirrored field where the unmirrored field reads up to 0.47, so the check can actually see a handedness or phase slip. Helical lessons: the sheared grid makes cap cuts the grid's first/last columns (no trimmed tessellation needed; boundary points verbatim from shared edge polylines); angular sampling must epsilon-guard Ceiling (ulp-different equal spans must not round to different segment counts); downstream booleans may cut modeled threads only with axis-perpendicular planes (helical∩cylinder/tilted-plane fall to the tracer with no trimmed-helical tessellation — fails loudly); helical surfaces not STEP-exportable (swept bucket)). **Standard components ("smart" hardware)** (`Modeling/Components/`): a catalogue item is more than geometry — placing it **modifies the host model AND assembles itself**. `HardwareComponent` carries (a) its own parametric `Body`, (b) a seating convention, (c) a **host preparation**; `ComponentAssembly.Place(component, points, face)` cuts the host and records the occurrence in one call. **One local-frame rule makes it composable**: a body is modeled +Z out of the host with its origin at the *seating datum* (the surface it bears on), `SeatDepth` = how far below the host's face that datum sits, `InsertedLength` = how far it reaches below — so a counterbored screw is the *same geometry* as a proud one, just posed deeper (one `Part`, N occurrences). **The preparation is a `Feature`**, which is the whole point: it regenerates, caches and suppresses, so *suppressing a placement removes its bore AND its occurrence*; omit the face and it seats on `FeatureContext.TopPlane` re-resolved per regeneration, so a thickness change re-seats the fasteners and re-cuts their holes (the selector story, not persisted indices). `ComponentSite` resolves depths explicit-first, with `ThroughDepth` = host extent below the face ×1.05 (so a through tool never ends coplanar with the far face, which `Drill` rejects) computed from the context's already-cached lowering. **The full fastener stack works**: `PlaceThrough` prepares BOTH bodies, engagement computed geometrically (grip = seating datum → anchor face; engagement = `InsertedLength` − grip, plus two pitches of tap runout), points *projected* onto the anchor face along the fastener axis, screw placed once. Catalogue = ISO 4762 SHCS / ISO 7380 button (exact spherical-cap dome — the profile carries the arc) / ISO 10642 csk (head diameter DERIVED from the hole table's countersink column via `StandardHoles.CountersunkHeadDiameter`, so screw and hole cannot drift; lengths overall, datum = flush head top) / ISO 4032 hex nut (exact hex extrude + nominal bore; its preparation drills the BOLT's clearance — a nutted joint taps nothing) / ISO 7089 washer (prepares NOTHING, deliberately) / 608-family deep groove bearing (two disjoint concentric rings — radial thirds; placement cuts a flush flat-bottom press-fit pocket) / Tappex Trisert / ISO 2338 m6 dowel; each body is ONE exact axis-touching revolve or extrude+drill (chosen over unions to avoid coplanar/tangent boolean input), no modeled thread, transcribed tables flagged verify-against-datasheet. **The hex socket recess is the assessed exception to the one-revolve doctrine** (`HexSocketRecess`): a hex is not a revolve, so it is a boolean, offered ONLY where exact — a pocket rimmed in a PLANAR face. Three findings scope it to `CapScrew(hexSocket: true)`: a full-turn revolve's flat cap is a `RevolvedSurface` with a POLE (the hex rim would wrap it — machinery the boolean lacks), so the socketed body is REBUILT from cylinder primitives with the shank overlapped INTO the head so every boolean is transverse; a csk head's rebuild makes cone and shank tangent along the shared rim (v1-boolean refusal, filed); a button socket would rim on the dome (traced curve). **Thread providers**: `ProvidesThread`/`CarriesThread`/`MinimumEngagement`/`MaximumEngagement` on `HardwareComponent` let `PlaceThrough(..., anchorInto: placement)` anchor a screw into a PLACED insert or nut — the far body gets no new cut; the overload checks thread match by designation, engagement against the provider's limits (a nut wants its full height, a blind insert bottoms out), and that every fastener point projects onto a provider point at the weld tier (skipped with a documented trust when the provider's face is a per-regeneration semantic reference). `ToPart()` sets `ClippedBySection = false`, so a sectioned assembly shows the housing cut open with its fasteners standing whole, as every drafting standard requires. **Remeshing through the Shape API**: `Shape.Remeshed(targetEdgeLength, iterations)` / `Remeshed(RemeshOptions)` — a **graph node, not a `Part` display option**, and the reasoning is the interesting part: a remesh looks like a rendering setting (only the discretization moves) but that is true only of the *tessellation*, so a remeshed sphere is faithful to the mesh it was projected onto rather than to the sphere. A `Part` flag would hide that behind a rendering knob; a node makes `Explain` state it — **Mesh Native, Implicit Bridged (a mesh SDF of the remeshed triangles, carrying their chord error rather than the child's), B-Rep Impossible** — and lets it compose through export, MCP and the construction tree. Projection defaults to the child's own mesh lowering (without a target, smoothing is curvature flow and the model shrinks); pass `SdfProjectionTarget` over `ToImplicit()` to project onto the exact field instead. A uniform scale above the node scales the target edge length with it (a shear uses the volume-preserving cube root of the determinant). Measured on a Ø20×20 cylinder at a 2 mm target: edges inside the `[0.66L, 1.33L]` band **42% → 95%** — and note the metric, because the **distribution converges fast while the single longest edge lags badly** (each collapse can leave a fresh edge of up to 2L for the next pass), so judge a remesh by the share in band, never by its extremes. Docs: `docs/examples/remeshing.md`. **Modeled text** (`Modeling/Text/`): a hand-rolled dependency-free TrueType reader (`head`/`maxp`/`cmap` 4+12/`loca`/`glyf` simple **and** composite/`hhea`/`hmtx` + optional `kern` 0/`name`/`OS/2`; `.ttc` and composite point-matching rejected loudly; **OpenType/CFF `.otf` fonts now load too** — `CffOutlines` parses Type 2 charstrings hand-rolled (INDEX/DICT, local+global subrs with the count-dependent bias, the whole curve family incl. flex, CID via FDArray/FDSelect), contours carry `GlyphContour.IsCubic` → `BezierTo`, and the CFF decoding trap is that hintmask/cntrmask carry one data byte per eight stems INCLUDING implicit vstems from stack arguments — pinned by exact decoded coordinates in a synthetic CFF font, seac/CFF2/arithmetic operators rejected by name; **GPOS pair kerning** — `GposKerning` reads the `kern` features' PairPos 1+2 through Extension lookups with both coverage and class-def formats, first-match-per-lookup accumulating across lookups, and per the spec a GPOS kern feature makes the legacy `kern` table invisible rather than merging) feeding `Shape.Text(text, font, size, height, plane?, style?)`. Glyph contours become `Sketch` segments **exactly** — `glyf` outlines are lines + quadratic Béziers, CFF outlines lines + cubics, and the sketch builder has `LineTo`/`QuadraticTo`/`BezierTo`, so nothing is flattened and text is Native in all three reps. Size = em size (`font.EmSizeForCapHeight(h)` converts a drawing's letter height); origin = the first line's baseline; layout does advance widths, `kern` pair kerning, tracking, multi-line and per-line alignment; a missing character throws rather than silently dropping. Types live flat in `EngrCAD.Modeling` because `Shape.Text` takes a `TrueTypeFont` — a sub-namespace would cost every call site a second `using`. Two lessons worth keeping: **the implied on-curve midpoint between consecutive off-curve points is THE TrueType subtlety** — pin it by asserting the region's signed distance at that point is exactly zero, never by eyeballing a rendered shape; and **never trust a font's contour orientation** — the spec says outer CW / counters CCW but real fonts violate it, so counters must be nested by containment (an exact point-in-region vote), which is why 'O' and '8' come out right. **Assemblies v1**: `Assembly` = named `Occurrence` list (each references a shared `Part` OR nested `Assembly` + a mutable `Frame3d` pose; DAG with cycle rejection at Add; names auto-suffix per level, `/` reserved for paths); **`PartInstance(Part, World, Path)` flattening is the seam** (`Assembly.Flatten()`/`Tab.Instances()`/`Scene.AllInstances`, World = frame chain × part transform, paths like "stack/clamp.2/bolt") — viewers/exporters only see instances; `Tab` holds assemblies beside loose parts; `Scene.AllParts` dedupes by reference so `PreMesh` meshes each distinct part ONCE (N instances share one mesh + GPU buffer set + pick BVH, drawn per-instance with composed matrices; `ViewportControl.SetInstances` is the host API, `SetParts` wraps it); viewer tree shows the hierarchy indented-flat with subtree visibility (effective = own AND ancestors), occurrence-path selection, and **assembly expand/collapse** (disclosure triangle, default expanded, state per assembly path; pure UI — collapsed rows are still built and registered so viewport visibility and instance indices never shift); **palette colors retro-assign on `Tab.Instances()`** for parts added to an assembly after `Tab.Add` (stability rule: `??=` plus a forward-only cursor — a color never changes once assigned, latecomers take the next entries in display order); OBJ/STL/`--render` flatten instances (STEP still exports distinct parts un-posed). **Assemblies v2**: **BOM** (`Bom.cs` — counts occurrences per distinct `Part` over `Flatten()`, so nested sub-assemblies roll up with no second traversal; `BomLine(Part, Quantity, Paths)` with `Item`/`Hardware` from the new **`Part.Hardware`** that `HardwareComponent.ToPart` sets, so `bom.Hardware`/`Manufactured` split bought-in from made; `Bom.Structured` is the indented per-level view whose leaf totals agree with the flat list by construction; text/CSV/`ByItem()` rollup; a viewer **BOM** button shows the table and drops a CSV. Lines group by part REFERENCE, not name — two separately-built parts sharing a designation stay two lines and `ByItem()` rolls them up, documented rather than silently merged). **Exploded views** (`Occurrence.ExplodeOffset` = the displacement at factor 1 in the PARENT's coordinates; `Flatten(factor)`/`Tab.Instances(factor)`/`Scene.Instances(factor)` compose it, so nesting works and offscreen renders get it free; factor 0 returns the frame untouched, so an un-exploded flatten is bit-for-bit unchanged. **The instance count and order are independent of the factor** — that is what lets `ViewportControl.SetInstancePoses` animate with matrices only, no GPU buffer touched, and picking follows because `HitTest` already reads the per-instance model matrix. `AutoExplode`'s heuristic carries the lesson: **the largest non-catalogue occurrence is the DATUM and does not move** — deriving the direction from the *assembly centroid* is wrong whenever parts are spread out, because the centroid sits in the empty middle and every part, base included, flies away from nothing (measured: a base plate moved 9.5 of a possible 10 units); hardware backs out along its own frame Z, which the component local-frame convention already knows; everything else is radial from the datum, scaled by how far out it sits so a stack keeps its order. Viewer toggle + slider, `--explode`, `WithExplode`, `RenderToImage(explode:)`). **Mates** (`Mates.cs`/`MateSolver.cs`: `Coincident`/`Planar(gap)`/`Concentric`/`Distance`/`Parallel`/`Perpendicular`/`Angle` + `Ground`, refs from explicit local coordinates or **semantic `BrepQueries` selectors resolved ONCE at construction**; Levenberg–Marquardt with an **analytic** Jacobian — finite differences would cap accuracy near 1e-8, an order worse than the weld tier — angular residuals scaled by the assembly's characteristic length so every residual is a length and one linear tolerance is meaningful, rotation variables divided by it so every column is O(1), DOF from a **diagonally pivoted Cholesky of JᵀJ** (rank-revealing for PSD, which is what sees through the deliberately redundant rotational encodings: 3 rows carrying 2 constraints). **It refuses loudly**: a non-converged solve writes NOTHING and names the mates carrying the residual; remaining DOF is always reported (`RequireFullyConstrained` makes it fatal). Verified at residual ≤ 1e-9: coincident, coincident-to-world, planar with/without gap, concentric, **concentric+planar (5 DOF, spin free)**, distance, parallel, perpendicular, angle, 3-occurrence chains, sub-assembly-as-rigid-body, and 0.01×/1×/1000× scale. **One stationary-configuration lesson**: an `Angle` or `Perpendicular` mate whose directions start exactly parallel has NO first-order step (d/dθ cos θ = 0 at θ = 0), so the solver detects and names it rather than nudging at random and sometimes converging. **Mates now reach ACROSS assembly levels**: every `MateGeometry` builder has an `(Assembly, path, …)` overload — an occurrence path ("carrier/bolt", `Assembly.ResolvePath`) names a unique *placement*, the deep occurrence's local frame becomes the unknown, and the chain rule through the frame chain is the one-level formulas with the rotation moment arm taken about each free link's composed WORLD origin (variables are world-space rigid perturbations, which compose as Δ_ancestor∘Δ_target∘W — so no derivative-matrix products; `Apply` pulls the moved world frame back through the PRE-step ancestor frame, snapshotted before any pose writes). Untouched sub-assemblies stay rigid for free; one-level solves stay bit-identical (dedicated arithmetic, no identity composition); a deep target whose owning sub-assembly is placed more than once is refused naming the placements (its frame is ONE shared object — flexible sub-assemblies are the follow-up); `Ground(path)` pins deep occurrences; `MateSolveResult.OccurrenceFreedoms` reports per-occurrence block rank (honestly labeled an upper bound — shared mates pin relative motion). **Typed mate references + persistence**: `MateGeometry.PlanarFace/CylindricalFace/Axis` take `FaceRef`/`AxisRef` (the GeometryRefs vocabulary, still resolved ONCE at construction — the eager choice made explicit), and `MateSet.SaveMates`/`LoadMates` follow the FeatureHistory conventions (query descriptors round-trip and re-resolve eagerly at load; failed queries fall back to pinned coordinates with a warning; missing occurrences skip the mate by name; opaque lambdas save a marker and warn; saved JSON is a fixed point under save→load→save, which is why `MateRef` keeps an already-unit direction verbatim — the AxisRef ulp rule)). **Mechanisms** (`Joints.cs`/`Mechanism.cs`/`Couplings.cs`/`HigherPairs.cs`/`MateSolverRates.cs`/`MotionInterference.cs`; docs `examples/mechanisms.md`): **a mechanism is the same mate system with DOF > 0, driven** — no second solver. Joints (`Joint.Revolute/Prismatic/Cylindrical/Spherical/Planar/Screw/Fixed`) are NAMED mate combinations from the same `MateRef`s, each nominal DOF **asserted against the solver's measured rank at Add** (a wrong definition fails by name); axis joints carry unwrapped coordinates (`Angle`/`Displacement`, zero at construction — unwrap advances only when a CONVERGED solve commits, so the residual stays continuous within one solve) and `WithLimits` stops (violation = full frame rollback naming the joint; a sweep walks onto the stop and reports it). The solver gained one internal seam: `AuxiliaryConstraint` rows (analytic derivatives over evaluated end geometry) beside the mates — screw pitch, gear/belt ratios (Δθ₂ = ∓ratio·Δθ₁ on the change since construction), cams (`CamLaw` carries lift+slope+curvature; `FromSketch` bisects the sketch's exact signed distance into a C² periodic spline), and drivers, whose angle form is the wrap-free pair [c − cos τ, s − sin τ] because a θ̂ − τ row would jump 2π mid-iteration at the seam and stall LM. `Sweep` = continuation (seed from the PREVIOUS converged pose, never the assembled one — the four-bar elbow-flip lesson; halving retries are safe because a failed solve writes nothing); a stall runs a zero-iteration rank probe at 3% threshold vs the sweep-start baseline — a dead centre names the driven joint + parameter and refuses to guess a branch, an unreachable target says so, and the SAME pose driven from another joint passes (the singularity belongs to the driven variable, not the pose; verified all three ways on one slider-crank). `RatesAt` solves J·q̇ = −∂C/∂t then J·q̈ = −r̈₀ with second-order terms ANALYTIC (composed rigid flow: centripetal per free link + 2·ω_outer×(v+ω×r) Coriolis cross terms between chain levels), matching the slider-crank closed form in velocity AND acceleration; under-constrained rates are refused (a family is not an answer). `CheckInterference` = bounds broad phase + `MeshIntersection.Crosses` narrow phase (transversal only — resting contact is not a clash) with jointed pairs skipped by default (tessellated pins interpenetrate their bores) and mesh-boolean volumes opt-in per confirmed range; `Shape.SweptOver`/`MotionStudy.SweptVolume` is a graph node, implicit-Native (child field lowered ONCE, placed per pose), B-Rep honestly Impossible. `Mobility()` reports Grübler/Kutzbach BESIDE the measured rank, disagreement informative (the planar four-bar in space predicts −2, rank measures 1 — and the four-bar doubles as the redundant-consistent stress case: 20 constraint rows, rank 17, LM converges through the redundancy for a full crank cycle within 1e-6 of the elbow-up closed form). **Animation** (`Animation.cs`/`PoseTracks.cs`/`CameraTracks.cs`/`AnimationPlayback.cs` in Viewer.Core, `ApngWriter`/`GifWriter`/`AnimationExport` in Viewer; docs `examples/animation.md` with committed APNGs): **an animation must not touch geometry** — a timeline is a duration + easing + at most ONE pose track and one camera track (two full-instance-list producers cannot compose; sequencing lives INSIDE a track), each with a clamp-semantics window, and `Animation.At(t)` is a PURE function so scrubbing, window playback, APNG/GIF/frame-sequence export and DocsGen `animate:<id>` fences evaluate one code path. Lives in **Viewer.Core by dependency direction** (pose tracks speak Scene/Mechanism, camera tracks speak CameraState; Modeling cannot reference the camera types), so a Scene cannot carry its animation — hosts pass `WithAnimation(scene => ...)`, re-invoked per live reload on a background task (track construction may mesh for bounds). `MechanismTrack` plays a swept `MotionStudy` — recorded frames VERBATIM at sample points (bit-exact, locked), chordal rigid interpolation between (delta b·a⁻¹ is rigid whatever the part transform carries; rotation slerp via new `Quaterniond.FromRotationMatrix` (Shepperd), origin on the straight chord), never a re-solve (the branch-flip trap); the (study, scene) overload grafts by occurrence path so bystanders stay put. `ExplodeTrack.Stagger` = per-occurrence timing windows over the new `Flatten(Func<Occurrence, double>)` (same walk; exact-0 leaves frames bit-identical) — the sequenced explode. Camera tracks reuse the view cube's `Ease`/`ShortestYawTarget` (turntable loops seamlessly under LINEAR easing with whole turns; fly-through along any `Curve3d`, Z-up so RMF roll is documented as dropped; `CameraMath.DefaultCamera` is now the ONE first-visit framing). Export: **APNG first** (acTL/fcTL/fdAT over the shared PngWriter internals; first frame = the PNG default image so the file ships as `.png` and degrades to a still; delay = Duration/frames; loop sampling t = i/frames so turntables don't repeat their seam frame), frame sequence always (the ffmpeg escape hatch), **GIF second and honestly banding** on shaded renders (median-cut where the PARTITION is the mapping — ≤256-colour images reproduce exactly; LZW locked by round-trip against an independently-written decoder; no dithering, wireframe/flat GIFs better — said in the docs). One camera per clip when no track drives it, framed over first∪last bounds — never per-frame framing. SceneHost transport (Play/Pause/Loop/scrub) owns only a DispatcherTimer advancing by REAL elapsed time; state is UI-free `AnimationPlayback` (loop wraps the overshoot), poses matched to the tab's instances by path. **STEP assemblies** (`StepWriter.WriteAssembly` — one PRODUCT per distinct solid, `NEXT_ASSEMBLY_USAGE_OCCURRENCE` per placement, poses as `CONTEXT_DEPENDENT_SHAPE_REPRESENTATION` over `ITEM_DEFINED_TRANSFORMATION`; non-rigid instance transforms refused by name since a placement has no room for scale, and MIRRORED (improper) ones refused too — they pass every orthonormality test but AXIS2 axes are right-handed by definition (Y = Z×X on every reader's side), so a left-handed triple would silently re-pose un-mirrored on read-back; the writer's `Placement(Frame3d)` overload mirrors `StepReader.Axis2`/`Frame3d.FromZX` (origin + Z + X stored, Y derived, frames round-trip by construction) while the matrix path keeps emitting the matrix's own columns so the written text is ULP-stable; STEP string literals now escape apostrophes — and `StepReader` **reads it back**: `StepReadResult.Instances`/`HasAssemblyStructure`, nested sub-assemblies composed, reader paths starting below the root product so our own writer round-trips exactly. `StepAssembly` in Modeling builds the list from `Part.TryGetSolid` — no second lowering — and reports what it skipped; `--export part.step` now writes ONE assembly file instead of one un-posed file per part). GL-touching test classes must join the xUnit `"offscreen-gl"` collection (parallel EGL pbuffer creation is flaky). **3D annotations (PMI) v1**: parts carry their own manufacturing information (`Annotations.cs`/`Callouts.cs`) — `Annotation` base (`Label` override, `Offset` placement, lazy-solid `Resolve`), `LinearDimension` (point-to-point + auto-measuring `BetweenFaces` via semantic `BrepQueries` face selectors — the same topological-naming story as features: selectors re-run per resolution, so dimensions re-measure through parameter edits and `FeatureHistory` regeneration), `RadialDimension.OnEdge` (reads the actual `IsCircular` radius; radius/diameter text), `LeaderNote`, `DatumLabel`; `Part.Annotate` + `ResolveAnnotations`/`TryResolveAnnotations` (geometry lowered to B-Rep once and cached; the Try path caches diagnostics so a selector broken by an edit becomes a status message, not a crash; `Scene.PreMesh` pre-resolves off the render thread) produce part-local `ResolvedAnnotation`s that viewers pose by the instance transform (assembly instances show their part's annotations in place); `HoleCallout`/`ThreadCallout` generate standard text from `HoleSpec`/`ThreadSpec` (diameter/depth/cbore/csk symbols as unicode escapes — source files stay ASCII; cbore/csk continuations on their own `'\n'` line). **PMI v2**: `AngularDimension` (three points, or two planar faces measuring the INCLUDED angle — in-plane directions perpendicular to the shared intersection line toward each face's centroid, what a drafter dimensions, not the normals' angle; vertex in `ResolvedAnnotation.AnchorC`, drawn as extension rays + 5°-chorded arc + degree text), `ToleranceSpec.Symmetric/Limits` text sugar (Label wins), `LinearDimension.Chain/Ordinate` (consecutive pairs on one line / every point from the datum with stacked offsets), **multi-line stroke-font text** ('\n' stacks lines; single-line output bit-identical — the docs PNGs hang off it), `HoleTable.For(part)` (lettered rows GENERATED from the graph's `DrillShape`/`ThreadedHoleShape` nodes — the spec now rides on `DrillShape` — with per-hole `DatumLabel` balloons + a multi-line table note) and `HoleAnnotations.AutoAttach` (one callout per call, "N×" prefix), and **pickable annotations** (`AnnotationGeometry.Pick` — ray vs the same Build segments within 8 style px, depth-blind matching the always-on-top draw; gold overdraw batch, click-through never falls to the part behind; `ViewportControl.PickAnnotation`). **Simulation results (fields) v1**: results are DATA on a mesh, carried by the document — `MeshField`/`FieldRange` (EngrCAD.Mesh, beside the writers, so a future solver produces one without referencing the modelling API; named scalar or vector values over a mesh's VERTICES with units, immutable so a field is a value, `ScalarAt` = the value for a scalar field and the MAGNITUDE for a vector one so "colour by displacement" needs no extra call, `Magnitude()`/`Component(i)`/`Sample(mesh, …)` derived, cell association a documented gap rather than a half-supported mode) attached via **`Part.Results`/`AddResult`** (same name REPLACES, so a re-solve updates rather than accumulating twins; attaching never meshes, which keeps `PreMesh` parallel) and displayed via **`Part.FieldDisplay`** (which result, which `FieldColorMap`, over what range, optionally displaced by a vector result) resolved once for every consumer by `TryResolveFieldDisplay` (deliberately does NOT mesh, so a properties panel or MCP tool can call it with no GL; a display naming a removed result becomes a status message naming what DOES exist). `FieldColorMap` follows `DisplayMode`'s precedent exactly — the enum is a document-model choice, the tables (`ColorMaps`: 17-stop viridis, monotone in lightness and asserted so by sampling the table; 9-stop Moreland diverging with a neutral grey midpoint, which only means what it looks like over `FieldRange.SymmetricAboutZero()` — deliberately asked for, never applied silently) live in Viewer.Core beside the shaders. **Colour is a vertex attribute under the baked-AO rule**: `aFieldColor` at slot 3, a context constant when no buffer is attached, and a `uFieldColor` strength of 0 making `mix(uColor, vFieldColor, 0.0)` exactly `uColor` — so a part with no results renders **byte-identically**, proved by the docs suite (all 89 rendered PNGs unchanged across the shader change; no unit test could have shown that). **A deformed shape is GEOMETRY, not a pose** — it cannot ride the matrices-only `SetInstancePoses` path the exploded view and the animation transport share, so it re-uploads deliberately and stays off the animation path; facet normals are recomputed from the displaced positions (carrying the originals over would make a deformed shape look exactly like the original, defeating the plot), the undeformed shape ghosts behind it at `FieldRendering.GhostAlpha`, a deformed part draws NO feature-edge overlay (those exact edges describe geometry that has moved) and its pick BVH is built over the displaced mesh. `FieldLegend` (Viewer.Core, pure) is the colour bar — flat-coloured bands since the line program is flat-colour, stroke-font ticks and a title stating the units and any deformation exaggeration, on the left edge (the cube owns the top-right, the meshing panel the bottom centre) — drawn by all three front ends AND in headless renders, unlike the view cube, because a legend is documentation and a colour plot without its scale is a picture of nothing in particular; ONE legend from the first visible display, since several parts on different scales under one bar would be a legend that lies (that is what an explicit `FieldDisplay.Range` is for). Two smaller rules: a **zero-span range normalizes to 0.5** (a constant field has no position to report and an extreme colour reads as a hot spot), and **NaN is skipped when ranging** so one undefined value cannot collapse a legend. Viewer toolbar **Fields** toggle + properties-panel min/max readout; `RenderToImage(fields: false)` is how a geometry figure is taken of a model that carries results. **`VtuWriter`/`VtkCellType`** (EngrCAD.Mesh) exports ParaView-readable `.vtu` dependency-free (hand-written XML, ASCII arrays) over a seam that is deliberately **(points, cells, cell types, point data)** rather than a mesh type — a surface result writes triangles/quads/polygons today and a tet mesher writes `VtkCellType.Tetra` through the same call with nothing in the writer changing; the merged multi-part overload fills a part's missing array with **NaN** (VTK's own "no value": dropping it loses the result that exists, zeros show a fake safe region). Wired into `--export .vtu` (the log states how many result arrays came out); docs page `examples/fields.md`. `RenderMesh.SourceVertices` is what places a per-source-vertex field on the flat mesh's duplicates EXACTLY — hashing positions also works (the occlusion bake does it) but is ambiguous wherever two distinct source vertices share a position. Modeling also owns the document model: `Part` (name + any-engine geometry + color + transform; `GetMesh(quality)` produces/caches the display mesh, `Scene.PreMesh()` runs it off the render thread) grouped into named `Tab`s in a `Scene` (`scene.Add(part)` shorthand targets a default "Model" tab; part names unique per tab; `Part` stays a leaf)**. **The Viewer is a library, not an app**: design code builds a `Scene` and calls `EngrCad.Show(scene)` (blocking; one call per process — Avalonia lifetime constraint; `onViewportReady` callback + thread-safe `ViewportControl.SetParts` support custom hosts; the internal `SceneHost` renders the tab strip with per-tab cameras — auto-framed on first visit, remembered after, kept in place on live reload). Rendering is an OpenGL viewport (Avalonia `OpenGlControlBase` + Silk.NET over ANGLE/GLES3) with a laptop-friendly orbit camera (drag orbit, shift+drag pan, ctrl+drag/scroll zoom, keyboard fallbacks) and click-picking that reports part *names*. **CAD chrome (dark theme)**: toolbar (Fit, Front/Top/Right/Iso, perspective/orthographic toggle — ortho frustum sized to keep the target plane's apparent size), model tree (visibility checkboxes, two-way selection sync via `ViewportControl.SelectionChanged`/`Select`/`SetVisible`/`Frame`; rows expand into a part's **construction tree** — `ConstructionTree.cs` in Modeling: a `ConstructionNode` is a label + kind + **positional path + the graph node it came from**, `FromShape` walking the operation graph with operands as children and a sketch row under sketch-driven extrude/revolve/sweep (labels straight from `Describe()`), `FromHistory` listing features with suppression and `[Param]` values each targeting `FeatureHistory.BodyAfter(i)`. Selecting a row **previews** it: a sketch's curves flattened onto its plane, or the sub-shape's feature edges — a rollback view. Previews are line geometry only, memoized by `ConstructionPreviewCache` **keyed on the shape reference** (a shared sub-shape lowers once) while expansion/selection key on the *path*, built on a background task so lowering never touches the UI or render thread, and drawn by `PreviewLayer.cs` through the line program with depth off; the preview is **restored by path** after a live reload (node references change, occurrence + construction paths do not). **Feature rows are editable**: per-row suppress toggle and a **rollback marker** (`FeatureRollback`, UI-free — suppresses every feature below the marker, records what the BAR suppressed so restoring never un-suppresses a user's own suppression), and clicking a feature row opens its `[Param]` values as editable fields in the properties panel — Enter writes through the SAME JSON seam as `SaveParameters`/`LoadParameters`/MCP `set_param` and regenerates on a background task (success republishes the tab, failure keeps the previous geometry and names the feature). **Both editing paths — the per-row suppress toggle and the properties-panel `[Param]` fields — now go through `UndoStack`/`DocumentEdits`**, so the toolbar's Undo/Redo buttons and Ctrl+Z/Ctrl+Y take them back (Ctrl+Shift+Z redoes too; `SceneHost.RunEdit` serializes every stack touch behind an in-flight flag and reports on the UI thread, which is what lets a single-threaded stack drive an off-thread regeneration). **The keyboard handler is Bubble WITHOUT handledEventsToo — deliberately the opposite of the viewport's pointer handlers**, whose trackpad lesson was that nothing upstream may starve them: a focused `TextBox` (the parameter fields) has its own undo and marks the key handled, and stealing it would make typing unrecoverable, a worse failure than a missed shortcut. The rollback bar stays outside the stack (it carries its own per-part suppression bookkeeping) and keeps the direct `RegenerateAndRefresh` path. **`Part.TryGetSolid()` lowers a part's B-Rep at most ONCE** — mesh, feature edges, selector annotations, STEP export and previews all share it (measured `Scene.PreMesh` **32.8 s → 10.1 s** on a heavy Shape scene; a five-hole drilled plate went from three ~9.4 s lowerings to one), and a lowering that fails is remembered and replayed verbatim rather than retried per consumer), properties panel (kind/faces/closed/volume/area/size), status bar, gradient background, adaptive 1-2-5 ground grid + RGB axes, and a feature-edge overlay over polygon-offset fills — **sourced from the ACTUAL B-Rep edges for B-Rep-backed parts** (`Part.GetFeatureEdges`, cached and primed by `Scene.PreMesh`; `BrepFeatureEdges` in Interop samples the exact edge curves at display resolution ≥ 96 segments/circle via the tessellator's `SampleEdge` rules, so bore rims stay smooth circles at any tessellation; sharpness decided by adjacent-face outward normals at three interior probes — periodic-face seams and equal-carrier wrap-split junctions omitted, unprobeable edges kept: draw rather than hide; a Shape part's B-Rep now lowers only once for mesh + edges + annotations + export, via `Part.TryGetSolid()`), with `MeshFeatureEdges` (EngrCAD.Mesh: boundary + sharp-dihedral) as the fallback for SDF/mesh parts. **Per-part display modes** (`Part.DisplayMode`: Shaded/Wireframe/Translucent — wireframe via `WireframeEdges` over the line program, translucent alpha-blended with per-part back-to-front ordering and opaque silhouette edges; a per-tree-row cycler drives it), a **global view style** (`ViewportControl.ViewStyle` toolbar dropdown: Points/Wireframe/Shaded/ShadedWithEdges — one precedence rule in `RenderModes.Resolve` shared by both render passes: explicitly non-default part modes override the global style, default-Shaded parts follow it; Points = vertex point sprites via a dedicated point program, desktop GL needs ProgramPointSize enabled; Shaded = fills without the feature-edge overlay), and a **`Capture` screenshot button** (`ViewportControl.SaveScreenshot` → `glReadPixels` in the render pass → dependency-free `PngWriter`, path reported in the status bar). **Section planes**: a Section toolbar toggle clips everything beyond **up to 4 section planes** (`SectionPlane(Normal, Offset)` — general normals, `On(axis, offset)` for the axis-aligned ones the toolbar/CLI expose — combined by `SectionCombine`: **Intersection** clips only where EVERY plane excludes, giving the classic **quarter cut** from two perpendicular planes and an **octant** from three; **Union** clips where ANY does, which is the old single-plane behavior generalized; with one plane the two rules coincide, so single-plane output is unchanged. Toolbar axis cycler; `[`/`]` keys nudge 2% of the scene extent along the active axis; defaults to mid-bounds, and changing the axis re-centers) — implemented as fragment-shader `discard` (`dot(world, uSectionAxis) > uSectionOffset`) in the mesh, line, and point programs (edges/wireframes clip consistently with fills; grid/axes are scene furniture and stay unclipped), with exposed interiors detected via `gl_FrontFacing` and shaded as a flat darker cut material (axis-agnostic); custom hosts drive `ViewportControl.SectionEnabled`/`SectionAxis`/`SectionOffset` (`SectionHeight` kept as a delegating legacy alias); picking and hover **honor** the section planes (`SectionClip.Hides` holds the shaders' own discard rule in one place — generalized to N planes + combine mode alongside the shader, since otherwise picking and rendering would disagree about which corner a quarter cut removes — so a click cannot select through the cut-away part), and hiding a part hides its annotations. **View cube** (top-right): stroke-font-labeled orientation widget mirroring the orbit camera (own ortho mini-projection, depth cleared, drawn last so it appears in `SaveScreenshot` captures but is excluded from offscreen renders by design); face/edge/corner clicks animate a 250 ms smoothstep shortest-yaw-path move to the orthogonal/45°/iso poses (`ViewCubeMath.PoseFor`, locked to the toolbar values; `ViewportControl.ViewCubeClick` for direct testing — synthetic mouse never reaches Avalonia); dragging on the cube orbits and then **rotate-snaps** to the nearest standard orientation on release, as commercial cubes do (`ViewCubeMath.SnapToNearest`; snapping an already-snapped pose is idempotent) — and the SceneHost toolbar's Front/Top/Right/Iso buttons route through the same `ViewCubeMath.PoseFor`, so widget and toolbar can never disagree about what "Front" means. All logic in `ViewCube.cs`. **Hover highlights**: pointer-move re-picks via the existing BVH raycast throttled by 4-DIP travel (`HoverThrottle`); one shader knob does both states — `uHighlight` 1.0 = selection gold, 0.35 = hover tint (wireframe/point parts blend line color instead); cube-region hover brightens the face set under the cursor and suppresses model hover; hover inherits picking's ignore-the-section-plane v1 behavior. **SDF isolines on the cut** (automatic when available): parts whose geometry is an `Sdf` — or a `Shape` with an implicit lowering, lowered once and cached per part — get iso-distance contours overlaid on the section plane (gold d=0 = exact surface cross-section, cool positive/warm negative families at ±k·spacing, 1-2-5 spacing from the parts' bounds — wall thickness at a glance, blend/offset debugging); extraction is `SdfContours.OnPlane` in Interop (UI-free marching squares over one batch-`Evaluate` planar grid, arbitrary plane, crossing error O(h²·curvature), shared cell-edge endpoints bit-identical so loops chain by exact equality), viewer side is `SectionContours`/`SectionContourRenderer` (plane passed as clip rule `(axis, offset)` — plane-general ahead of the section-axis generalization; recompute only on height/scene/visibility change, never per frame — and in the window on a **background task** (`SectionContourWorker`, generation-stamped like `TabMeshLoader` so a scene swap or superseding nudge never adopts a stale build; previous contours draw until the new ones land, offscreen stays inline one-shot), so first-enable no longer stalls the frame on marching squares + the first `TryGetSdf` lowering; lines lifted 1% of spacing below the plane so the shader discard can't eat them, depth-tested like feature edges); offscreen renders don't draw them yet (parity backlog item). Hard-won viewer lesson: **GLSL source strings must stay pure ASCII** — an em dash in a shader comment made ANGLE's translator reject the whole shader, the compile exception aborted `OnOpenGlInit` before the other programs were built, and the entire viewport rendered black. **Web viewer (Blazor WebAssembly, kernel in the browser)** — `src/EngrCAD.Web` (a Razor component library) + `samples/EngrCAD.WebDemo`. **The kernel compiles to WASM unmodified and produces identical geometry**: a flange with a 6-hole bolt circle and a filleted rim built in headless Edge gave 1 560 triangles, a closed mesh and volume 41 573.0 — the same as the desktop run to displayed precision, so there is no WASM-specific code path. Measured on win-arm64 from **clean publishes**, best of three headless-Edge page loads each running best-of-five builds, with the desktop control **interleaved into the same window**: **desktop 88.7 ms total (36.4 lower + 52.3 tessellate); WASM without AOT 1 677.3 ms (18.9×) at a 1.9 MB brotli payload; WASM with AOT 385.2 ms (4.3×) at 4.6 MB**. So **AOT buys 4.4× for 2.4× the download**, a per-deployment product call rather than a default — the docs deployment declines it because AOT compilation adds minutes to every docs build. **Our own code is about a fifth of the payload**: all nine EngrCAD assemblies are 1.14 MB uncompressed / 0.41 MB brotli against a 1.9 MB total, the largest single items being `System.Private.CoreLib` (1.53 MB) and `dotnet.native.wasm` (1.43 MB), so trimming our own code could win a few hundred KB at most. **Two rules the measurements cost us.** The interleaving is not fussiness: this laptop returns 88.7 ms and 185.7 ms from runs of the SAME Release binary, so a ratio across sittings is noise with units. And **only ever measure a CLEAN publish** — republishing over a previous publish without clearing `obj`/`bin`/output ships a runtime that disagrees with the assemblies, which does not fail the build: it reads 1.6× slow (2 765 ms for a row that is really 1 677 ms) and then aborts with `MONO interpreter: NIY encountered in method EngrCAD.Core.Vector2d:.cctor ()` (a red herring — that cctor is four `static readonly` struct fields). That corrupted artifact almost got the whole table "corrected" downward in the public docs; the tells were that only ONE row moved while the others reproduced, and that the demo's beacon had quietly stopped firing. **Re-verify the artifact before believing a number that moved when the source did not.** CI is immune (fresh checkout), so this is a local-iteration hazard, which is worse because local iteration is where numbers come from. **The demo is live on the docs site** (`docs/examples/web.md` embeds it; `.github/workflows/docs.yml` publishes it into `_site/live/`), which works because the published app is **path-portable**: `<base href="./" />` plus the already-relative asset references the build emits mean no `StaticWebAssetBasePath`, no post-publish rewrite and no repository name baked into the artifact. `?embed` strips the page furniture for the iframe. **The viewport now DRAWS** (`EngrCadViewport.razor` + `ViewportFrame.cs`): shaded geometry with per-part colours, ground grid and axes, an orbit camera, **feature edges, per-part display modes (Shaded/Wireframe/Translucent) and the global view style (Points/Wireframe/Shaded/ShadedWithEdges)** — plus, as of the parity-ladder pass: **section planes with picking parity and SDF isolines on the cut, the view cube, 3D annotations, a toolbar (Fit/style/Section/Annot/Props/BOM), the properties panel (`PartFacts`, gated on `Part.HasMesh` so it can never mesh a part the loader is still on) and the BOM overlay** — every one riding shared code rather than restating it (`SectionClip.Hides` through `ScenePick` for pick-render agreement; `SectionContours`, `ViewCubeMath`/`ViewCubeGeometry`/`ViewCubeAnimation` (the 250 ms constant taken, not re-typed), `StrokeFont` and `AnnotationGeometry`, all moved public into `EngrCAD.Viewer.Core` as **shared-render-model step 2** with the desktop delegating to them, verified by all 53 docs PNGs byte-identical; `TabMeshLoader`/`MeshFlavor` moved too, though the browser deliberately keeps its own single-threaded loader — Avalonia-free but thread-model-bound). The `?report` self-check covers the new rungs as pixel relationships (sectioning 32 374 → 20 593 body px + 676 gold isoline px; annotations toggle 786 px; cube 4 770 px in-region, claims region clicks, `cubeSnapped=1` — the camera lands EXACTLY on the shared pose table because the animation's final step returns the target) — all routed through the SHARED `RenderModes.Resolve` precedence rule and `RenderModes.SortBackToFront`, never restated (the tests read their expectation *from* `Resolve`, since a copied precedence rule agrees with a broken implementation as happily as a correct one). `WireframeEdges` moved to `EngrCAD.Viewer.Core` for the same reason the camera did: it has no GL in it, and **its walk order decides the vertex order in the uploaded buffer**, so two copies would not even upload the same bytes. Measured against `OffscreenRenderer` as the reference at matched size/camera/style, fills, points and translucency agree within 2–10%; **wireframe deliberately does not** (26 228 lit vs 19 980) because the offscreen pass supersamples 2× and box-downsamples, dropping a 1-px line below an absolute brightness threshold — same primitives, different reconstruction filter, reported rather than tuned away. Points needed no `GL_PROGRAM_POINT_SIZE` (WebGL2 is GLES 3.0, where the cap does not exist and `ViewportControl` already skips it), but point size IS in framebuffer pixels, so `viewportSize` now also returns the device pixel ratio — the same correction the window makes for DPI and the offscreen pass for its supersample. Earlier rungs verified headlessly by reading the canvas back (33 912 lit pixels in a 693×420 buffer, and 111 481 pixels changed after an orbit, so the camera provably drives the render; WebGL2 IS available under headless `--disable-gpu`). Architecturally the load-bearing rule is that **no GLSL lives in JavaScript**: `wwwroot/engrcad-gl.js` owns the context, uploads buffers and issues draws, while shader source comes from `EngrCAD.Viewer.Core`'s `ViewerShaders` — the same strings the window and the offscreen pass compile — and every decision about what a frame looks like reaches JS as a plain `FrameDescription` (the background gradient's fullscreen triangle arrives as `Geometry = null, Count = 3` rather than an `if (isBackground)` in JS). **The camera is not forked either**: `CameraState` and the input bindings (`DragOrbit`/`DragPan`/`DragZoom`/`WheelZoom`, plus `PitchLimit`/`KeyStep`) moved into `CameraMath`, and `ViewportControl` now delegates to them with every constant preserved verbatim (−0.01 rad/px yaw, 1.006 and 0.88 zoom bases, 0.0018 pan scale) — so there is one answer to what dragging 100 pixels does and it is tested once; the only front-end difference is that a DOM wheel event reports ~100 px per notch counting DOWN as positive, normalized at the edge. **`ViewportFrame.Build` is a PURE FUNCTION**, which is the point: the window and offscreen passes drifted because comparing them meant looking at pixels, whereas draw order, clear colour, furniture ranges, per-instance matrices and neutral shader state are now asserted as values. Two of those assertions encode traps — **fills deliberately do NOT cull** (a section plane exposes interiors as backfaces, which the shared shader shades as cut material via `gl_FrontFacing`, so culling would look fine today and break sectioning silently), and **`uSectionCount` travels as a typed marker, never a plain number** (an `int` uniform, and the interop marshals every JSON number through `uniform1f`, which GL rejects with no visible error — `IntUniform` serializes as `{"int": n}` → `uniform1i`, and `Vec4ArrayUniform` as `{"vec4": [...]}` → `uniform4fv`, needed because four packed section planes are exactly 16 floats, indistinguishable from a mat4 by shape; WHICH uniforms carry which type stays a C# decision). Geometry crosses as `byte[]` (Blazor marshals that as a binary array; `float[]` would go through JSON) and that packing step is also the single double→float32 narrowing point. Two traps found the hard way: **`<script type="importmap"></script>` is REQUIRED in `index.html`** — the build fingerprints every framework asset and rewrites that map, and without it `dotnet.js` resolves its siblings by unhashed names and the boot 404s with no visible error; and **in-page timings cannot be read from a `--dump-dom` run**, because `--dump-dom` fires at load so the browser needs `--virtual-time-budget` to reach the end of the work, and under virtual time the clock does not advance during synchronous computation — every measurement reads 0 ms. The demo's `?report` beacon exists for exactly that reason and is measured under a real clock — and it is a 1×1 `<img>`, NOT `IJSRuntime.InvokeVoidAsync("fetch", url)`, because that marshals the JS `Response` back across the interop boundary and throws when it cannot, losing the measurement AND tripping Blazor's error UI: **a beacon must not be able to fail.** The former hardcoded showcase now lives in `samples/EngrCAD.Demo` (console app calling the Scene API — the consumer experience). **The live-modeling loop is `dotnet watch` + hot reload, not a custom CLI**: `EngrCad.ShowLive(Func<Scene>)` registers a `MetadataUpdateHandler` (`HotReload.cs`) that re-invokes the factory after each patch and calls `SetScene` (camera preserved; factory exceptions keep the last good scene and surface in the overlay; camera pose persists to a temp file across rude-edit restarts, 30-min freshness). **`.csx` scripting rides the SAME loop** (`tools/EngrCAD.Script` + `docs/examples/scripting.md`): a script defines `Scene scene` — the DocsGen snippet contract, same Roslyn seam, same references/imports, so fences and scripts are interchangeable verbatim — and the runner passes a recompile-the-file factory to `EngrCad.Run` (all switches free) while a `FileSystemWatcher` triggers the new public `EngrCad.NotifySourceChanged()` (= the hot-reload path — debounce, keep-last-good-scene, camera — for hosts whose model source is DATA; no-op unless ShowLive is active). The documented reusable-component pattern is deliberately NOT a module system: a parametric component is a plain C# method returning `Shape`/`Part`, identical in scripts (`#load`-shareable, see `samples/scripts/bracket.csx`), class libraries, and `Feature`/`HardwareComponent` wrappers. `EngrCad.Run(args, factory)` wraps model programs: no args → ShowLive, `--view` → static Show, `--export part.step|part.obj|part.vtu` → headless export (STEP per B-Rep-representable part via `Part.Source`, OBJ merged with transforms, VTU carrying the parts' simulation results as point data), `--render out.png` → **headless offscreen render** (no window; `--render-style points|wireframe|shaded|shaded-edges` and `--section x|y|z <offset>` select view style and section plane — CLI wins over the builder's `WithViewStyle`/`WithSection`; DocsGen `render:` fences take matching `section:<x|y|z>,<offset>` and `style:<name>` options). **Host configuration**: `EngrCad.Configure()` fluent builder (`WithTitle/WithQuality/WithRenderSize/WithLogger/WithLoggerFactory/WithViewportReady` → terminal `Run/Show/ShowLive/RenderToImage`) over a plain `EngrCadOptions` POCO — bindable as `IOptions<EngrCadOptions>` and seeded via `Configure(options)`. **Logging is `Microsoft.Extensions.Logging.Abstractions`** — abstractions only, no provider; Viewer/Mcp host the message vocabulary, and it now **extends inward on a weighed per-project basis**: `EngrCAD.Interop` and `EngrCAD.BRep` take the abstractions reference (still no UI/provider — the kernel rule is about UI/rendering deps) so the long operations accept an **optional trailing `ILogger`** — `BrepBoolean.Union/Intersection/Difference` (event **80**, Information, passing the logger to its own sub-steps), `BRepTessellator.Tessellate` (**81**, Debug), the `MeshSdf` ctors incl. the winding build (**82**, Debug), `StepReader.Read/ReadFile` (**90**, Information) — null default, one branch, results stay return values (`StepReadResult.Diagnostics`, `Explain` reports are data the caller acts on; logging complements them). Mesh/Core/Implicit deliberately take NO reference: everything the todo named is reachable at the Interop/BRep seams (MeshSdf wraps the winding build), so the leaf kernels stay dependency-free. This REVERSED the earlier `IEngrCadLog` no-dependency decision, because the shim made every host write an adapter to save a reference most of them already had transitively. `Logging.cs` holds the host message vocabulary as source-generated `[LoggerMessage]` templates with **stable event IDs** (10s usage, 20s export/render, 40s live reload, 50s display, 60s MCP, 70s RPC, 80s Interop kernel, 90s BRep kernel), so sinks get fields not strings and levels can finally distinguish a skipped part (Warning) from a failed export (Error). **The default is `EngrCadLoggers.Console` (Information → stdout, Warning+ → stderr), NOT `NullLogger`** — a library defaults to silence but a program's front door does not, and `EngrCad.Run` is a model program's front door; pass `NullLogger.Instance` to silence it deliberately. `EngrCadLoggers.StandardError` is the MCP sink, and the console logger resolves `Console.Out` per call so it follows `StdoutGuard`'s redirection instead of writing protocol-corrupting bytes. Mesh-quality precedence is explicit `Scene` options > `EngrCadOptions.Quality` > `MeshQuality` defaults, implemented by `Scene.HasExplicitOptions`/`ResolveQuality(fallback)`/`PreMesh(fallback)` and honored by display, hot reload, `--export .stl/.obj`, and `--render` (which also uses the configured image size); all Run/export/render reporting and the live-reload overlay messages route through the log seam. **Adaptive tessellation** (`TessellationQuality`, opt-in via `MeshQuality.Tessellation`, docs `examples/quality.md`): a criterion — max angle/segment ($fa) and/or max chord deviation (OCCT deflection), clamped to [Min, Max]Segments — replaces the fixed counts for B-Rep tessellation; `ResolveFor(solid)` scans curvature radii and sizes ONE count pair at the largest radius (chord binds there, n ≈ π·√(r/2d)), and `Part.GetMesh` + `Part.GetFeatureEdges` resolve through the SAME criterion so the exact edge overlay can no longer detach from the faceted fill on large rims (the fix todo's chord-deviation investigation called for; with fixed counts the overlay stays deliberately finer at ≥ 96 seg/circle). Null keeps fixed counts bit-for-bit (locked by a bit-identity test); `SdfResolution` deliberately untouched. Offscreen rendering (`EngrCad.RenderToImage(scene, path, w, h, camera?, style?, sectionAxis?, sectionOffset?)`/`CanRenderToImage`, backed by `OffscreenRenderer` + a direct EGL-pbuffer `EglContext` over Avalonia's ANGLE natives + `PngWriter`) is the viewer self-verification loop — tests and agents render a scene and inspect pixels instead of screenshotting the demo app; it now has **full parity with the window** (display modes, global view style, axis sections, translucent ordering — only the selection highlight stays neutral; SDF isoline overlays included). **3D annotation rendering + measure tool**: `StrokeFont` (one polyline glyph table — digits, A–Z, dimension symbols — shared by the view cube's labels and annotation text) + `AnnotationLayer.cs` (self-contained like the isoline/cube precedents: `AnnotationGeometry` pure math builds the classic dimension anatomy — extension lines with gap/overshoot, dimension line, V arrowheads, radial/note leaders, datum boxes, billboarded screen-constant text; CPU-side billboarding rebuilt ONLY when the camera pose/viewport/annotation set changes, value-equality on `AnnotationCamera` is the cache key) drawn through the shared line program with the depth test off (always-on-top v1) and never section-clipped; per-part resolution failures surface in the status bar; SceneHost gets an Annot toggle (`ShowAnnotations`, default on) and a Measure toggle (`MeasureMode`: two picks via the pick raycast — `HitTest` gained an out world-point — create a transient point-to-point dimension, Escape clears, `MeasurePick` public for direct-call testing). Annotations DO render offscreen (unlike the view cube) — they are documentation, so docs renders carry dimensions — via one additive `AnnotationLayer.DrawOffscreen` call (supersample factor doubles as the text pixel scale). Infra lesson: Avalonia's ANGLE `av_libglesv2.dll` exports EGL entry points under an `EGL_` prefix (not the standard `egl*`), so a hand-rolled EGL binding must try both spellings. **Ambient occlusion streams** rather than blocking the window: nothing waits for the bake (measured at 12.3 s on the demo scene, and it already saturates every core, so more parallelism had nothing to give). The scene appears **flat-lit immediately** and each part's occlusion arrives as its own background bake finishes (`AmbientOcclusion.BakeInBackground`, cheapest part first, per-part progress as a transient "ao" badge on the tree rows via `ViewportControl.OcclusionBaked`, queued per *visible tab* so a tab you never open is never baked; `TryGet` is a pure cache read, the only lookup on the render thread). This is honest rather than a placeholder: **a mesh VAO with no occlusion buffer reads the context constant 1.0, which is exactly the AO-off shading**, so an unbaked part is the correct flat-lit render and the bake only darkens crevices. Measured time-to-window 27.1 s → 14.2 s in eager mode. `RenderToImage` still bakes inline (one-shot, must be deterministic) and shares the same cache and `Bake`, so window/offscreen parity holds. Also on `Part`: **`TryGetSdf`** joins `TryGetSolid` as the cached implicit lowering (toggling a section no longer re-lowers; deliberately NOT primed by `PreMesh`, since only the section overlay needs it), **`ClippedBySection`** (default true — false renders *and picks* the part whole inside a cutaway, the drafting convention that shafts, bolts, nuts, keys and pins are drawn unsectioned; the model tree carries a per-row cut/whole toggle via `ViewportControl.SetClippedBySection`, and the isoline overlay detects the changed flag itself like a visibility change), and `SectionPlane.On(Frame3d)`/`Through(point, normal)` for oblique cuts (the toolbar's **Cut@View** button places one from the current camera — through the orbit target, normal = eye direction). `SectionClip.Siblings` sits beside `Hides` as the cut-face overlay rule, derived from one sentence — a point on a plane is on the *visible* cut face iff `!Hides(p) && Hides(p + eps·n)` — from which both combine modes fall out with no special case. Underneath, occlusion is baked per-vertex on the CPU (hemisphere rays against the part's existing pick BVH, parallelized) rather than screen-space: no FBO or render-pass restructuring, and it is **identical in the window and offscreen by construction** — the parity rule `RenderCore` exists to protect. It rides as vertex attribute 2 (`aOcclusion`), so a mesh uploaded without an occlusion buffer just reads a constant 1.0; on by default (`EngrCadOptions.AmbientOcclusion`, `WithAmbientOcclusion`, `RenderToImage(ambientOcclusion:)`). Offscreen renders use 24-bit depth (16-bit + `PolygonOffset(1,1)` bit sawtooth notches out of silhouettes) and 2× supersample + box-downsample for AA (MSAA pbuffers are unreliable under ANGLE/WARP). **The render core now lives in its own assembly, `src/EngrCAD.Viewer.Core`** — no Avalonia, no Silk.NET, so it loads in a browser. It holds `ViewerShaders` (ONE shader set; `Header(es)` already emitted both an ES3 and a desktop 3.3 version header, which is exactly what WebGL2 needs), `CameraMath` (scene-scaled `FrustumPlanes` replaced the fixed 0.1/200 planes and 110/120-unit distance clamps in BOTH paths, so large scenes frame fully), `RenderModes`/`EffectiveMode`, `SectionPlane`/`SectionAxis`/`SectionCombine`/`SectionClip`, and `RenderGeometry`'s pure half — **never fork shader or camera code between front ends again**. Everything taking a `GL` stayed in `EngrCAD.Viewer`: `ViewerPrograms` (LinkProgram/CompileShader), `SectionUniforms`, `RenderUploads` (UploadMesh/UploadLines/UploadOcclusion) — a class cannot span assemblies, so those two renames were forced. **The namespace stayed `EngrCAD.Viewer` even though the assembly is `EngrCAD.Viewer.Core`**: `SectionPlane`/`ViewStyle` are public API used by `EngrCadOptions`, the MCP server, docs snippets and tests, and an assembly boundary is a packaging decision while a namespace is API. The refactor's real oracle was not the 1966 unit tests but the **50 rendered docs PNGs compared byte-for-byte** — a shader or camera change survives every unit test and still changes what users see. **Historically `RenderCore.cs` was the single source of truth for both render paths** (`ViewerShaders` — one shader set, offscreen sets neutral uniforms; `CameraMath` — scene-scaled `FrustumPlanes` replaced the fixed 0.1/200 planes and 110/120-unit distance clamps in BOTH paths, so large scenes frame fully; `RenderGeometry`) — never fork shader or camera code between window and headless again. `samples/EngrCAD.LiveDemo` is the parametric-bracket demo (`dotnet watch --project samples/EngrCAD.LiveDemo`); verified end-to-end: body edits hot-apply in ~0.1–0.7 s without restart. **On-demand tab meshing** (`EngrCadOptions.LazyTabMeshing`, default ON; `WithLazyTabMeshing(false)` or `--mesh all` opts out): `EngrCad.Show` no longer `PreMesh`es the whole document before the window — each tab meshes when first VIEWED on a background task (measured on `samples/EngrCAD.Demo`: **53.6 s → 2.3 s** to a visible window). `TabMeshLoader.cs` (Avalonia-free, unit-tested headlessly) publishes a **growing prefix** of the tab's instances as parts land, so the viewport is orbitable while the rest computes, and an already-meshed tab publishes synchronously with no background work — which makes eager mode the same code path rather than a second implementation. Three rules it enforces: a **generation token** checked in the worker between parts *and again after every posted callback*, so a tab switch cannot let a stale result land in the wrong tab; **cancellation stops at the LOWERING, not at part boundaries** — `Part.GetMesh(quality, progress)` passes its `ProgressCancel` to Surface Nets *and* to `BRepTessellator.Tessellate`, but deliberately NOT to B-Rep lowering, whose result is cached in `TryGetSolid` and would be poisoned by abandoning it. So a cancelled part keeps the expensive half and a revisit re-tessellates from the cache instead of starting over; the earlier rule was "the part in flight finishes", which made a tab switch wait out a whole tessellation for a result nobody wanted; and a **part that throws is named, not swallowed** (dropped from the published instances, red tree row with the reason, status bar + log) while the rest of the tab still loads. Progress UI is a bottom-centre panel (hit-test-invisible so it never steals an orbit drag): honest count plus a secondary line naming the kernel route (`MeshFlavor`: `Lowering to B-Rep...`/`Polygonizing the field...`/`Tessellating surfaces...`/`Reticulating splines...` — the last shown only for geometry that genuinely carries NURBS, which sketch profiles and glyph outlines do). Document-model side is additive: `Part.HasMesh` (lock-free probe so the UI thread never queues behind a mesh in flight), `Part.Prepare`, `Tab.PreMesh`, and `ConstructionTree()` moved to its own lock for the same reason. Headless paths (`--export`/`--render`/`RenderToImage`/DocsGen) unchanged. Testing lesson: `SendInput`'s `INPUT` struct must be **40 bytes** on x64 or every call silently returns `ERROR_INVALID_PARAMETER`; and `EngrCAD.Demo` is `WinExe` (so `MainWindowHandle` is the real window) while `EngrCAD.LiveDemo` is `Exe` (its `MainWindowHandle` is the *console* — enumerate non-console top-level windows or startup timings are meaningless). **MCP server** (`src/EngrCAD.Mcp` — its own package, so viewer/kernel consumers never inherit a protocol stack): `EngrCadMcp.Run(args, factory, title)` wraps `EngrCad.Run` and adds `--mcp`, serving *that program's* scene over the Model Context Protocol on stdio — tools `list_tabs`/`list_parts`/`describe_part` (properties-panel facts + resolved annotations + `Part.ConstructionTree()`)/`screenshot` (a real PNG as an MCP **image block**: standard views, view styles, section plane, tab/part scoping, via `OffscreenRenderer`)/`export`/`reload` (hot-reload semantics: a throwing factory keeps the last good scene), plus an `engrcad://scene` resource; failures are `isError` results naming what *does* exist, never protocol errors. **The server can now DRIVE a parametric model, not just inspect it**: `set_param`/`suppress_feature`/`unsuppress_feature` edit `[Param]` values through the history a part already carries (`Part.History` IS the seam — no registration surface) and regenerate via the new `Part.Regenerate()` (Modeling: a fully successful regeneration swaps the fresh body in and clears every derived cache — mesh, cached B-Rep/SDF lowerings, feature edges, resolved annotations, construction tree — while a failure keeps the previous complete geometry; `Feature.Suppressed` became settable), with values routed through the SAME JSON seam as `SaveParameters`/`LoadParameters` so spellings cannot drift, the `RegenerationResult` surfaced verbatim as the tool result, and a successful edit bumping the session generation; edits die with the session (reload re-runs source, the truth). `screenshot` gained the viewer's full option surface (up to 4 `SectionPlane`s + combine — one `[0,0,1,offset]` plane is byte-identical to the axis spelling, locked by test; explicit camera as yaw/pitch degrees + distance/target or `cameraEye`, mutually exclusive with named views) and `export`'s `.png` takes width/height. **Structured content**: every JSON tool declares an output schema (`ToolSchemas.cs` — the SDK's explicit-`OutputSchema` + `UseStructuredContent` form, needed because the methods return `CallToolResult` directly; pinned 1.4.1 already has it, no bump) and one `JsonObject` feeds both `structuredContent` and the text block so they cannot disagree; `screenshot` deliberately declares none (an image block is not structured content). `StandardViews.cs` is **deleted** — poses route through the now-public `ViewCubeMath.PoseFor`/`CameraMath.FrameDistance` (bit-exact equivalence asserted against both implementations before the deletion; `NamedViews.cs` keeps only the name table, whose vectors now live in `ViewCubeMath.DirectionFor` beside the pose math). Two session-test lessons: **the server dispatches requests concurrently** (as MCP allows), so a client that edits then reads must await the edit's response — the batch-write test driver read the pre-edit model; and the no-GL path is forcible via **`ENGRCAD_NO_GL=1`** (a seam in `OffscreenRenderer`'s availability probe, doubling as an operational kill switch — the probe is a process-wide `Lazy`, so only a child process can simulate a GPU-less machine), locked by a stdio test asserting `screenshot` returns `isError` naming the constraint while every other tool works. **Live-viewer RPC (option (b))**: `RemoteControl.cs` in EngrCAD.Viewer exposes a **loopback-only** newline-JSON-RPC 2.0 TCP endpoint behind `WithRemoteControl(port, token)`/`--rpc [port] [--rpc-token t]` (OFF by default; port 0 = ephemeral, reported via log event 70 + status bar; token checked per request before dispatch), in three separable layers — `RemoteControlServer` (transport/framing/token/error envelopes), `RemoteViewerDispatcher` (vocabulary: ping/list_parts/set_view/fit/set_section/set_display_mode/set_view_style/select_part/get_selection/measure/screenshot over `IRemoteViewer`), `ViewportRemoteViewer` (the only Avalonia-aware layer: **every call marshals through `Dispatcher.UIThread`**, GL never touched — screenshot rides `SaveScreenshot`'s capture-on-next-frame) — which is exactly what makes transport+vocabulary+bridge testable headlessly over real sockets with a stub viewer (only the thin window wiring needs a manual pass, filed in todo). The MCP process bridges with `--mcp --viewer <port>`: bridge tools set_view/fit/set_section/set_display_mode/set_view_style/select_part/get_selection/measure/`viewer_screenshot` (per-request connections, so a watch-restarted viewer just reconnects; a dead endpoint is an `isError` naming `--rpc`; without `--viewer` the tools are never advertised). `ViewportControl` grew `InstancePaths`/`SelectedPath` (remote hosts speak occurrence paths, not indices) and one-shot `Measure(p1, p2)`; the JsonNode trap worth keeping: **an in-memory `JsonValue<int>` refuses a plain `double` cast**, so numeric RPC params go through a TryGetValue double/int/long ladder. Built on the official C# SDK's `ModelContextProtocol.Core` (not the metapackage — that would drag in `Microsoft.Extensions.Hosting` for no benefit). **The stdio lesson: stdout IS the protocol channel** — `StdoutGuard` takes the raw stdout handle for frames and repoints `Console.Out` at stderr *before the scene factory runs*, so a design program that prints while it builds cannot corrupt the stream (a real child process whose model deliberately prints is driven through a full JSON-RPC session in tests, asserting every stdout line parses as a frame). Laziness is the other rule: the listing tools evaluate **zero** geometry (locked by an evaluation-counting `Sdf`), `describe_part` meshes only the named part, and render/export scope-mesh — whole-scene scope goes through `Scene.PreMesh` rather than forking it. **2D views** (`Region2dOffset` in Core, `PlanarSection` in Interop, `Sketch.Offset`/`Shape.Section`/`Shape.Silhouette`/`Shape.Wedge` in Modeling): **offset is ONE algorithm, not two** — outward = the region unioned with a slab per edge and a join per corner (`OffsetJoin.Round`/`Miter`/`Chamfer`, Clipper's miter limit 2 degrading to chamfer), and inward = that same dilation applied to the COMPLEMENT (`B \ dilate(B \ R, d)` with B the bounds grown by 3d), so self-intersection falls out instead of needing a cleanup pass and a shrunk plate returns two regions or none. Round joins are **inscribed** arcs, matching `Sketch.ToRegions`' one-sided contract so error never accumulates outward. **Open-path stroking** (`Region2dOffset.Stroke(path, width, cap, join)` — toolpath footprints, slots, SVG strokes) rides the same union: full-width slabs, corner joins offered on BOTH sides (the inner wedge is already inside its slabs, and a 180° reversal legitimately fills both — the round nose on a doubled-back path), butt/round/square caps; self-crossings just union and a closed circuit encloses its hole. `Shape.Section(plane)` = OpenSCAD `projection(cut = true)`, exact from the B-Rep surfaces when the shape lowers (fidelity set by chord tolerance alone) and mesh-derived otherwise — sealed internal cavities come back as holes; a plane flush with a face or containing an edge is refused (that section is an area, not a curve). `Shape.Silhouette(plane)` = `projection(cut = false)`, the union of front-facing projected triangles folded through a **balanced tree over Morton-sorted faces** — measured on a 64-segment torus: **67 ms sorted+tree vs 2.4 s tree-unsorted vs 259 s linear**, because merging face 1 with face 900 gives two disjoint regions and cancels nothing. `Shape.Wedge` (OCCT `BRepPrimAPI_MakeWedge`) **is an extrusion** — a trapezoidal XZ section swept along y — so it carries one internally and every lowering delegates: no fourth code path, native in all three reps, exact under any affine. Four numerical lessons, each of which cost real geometry: **(a)** a miter apex must divide by `sum.LengthSquared`, never `sum.Length` squared — at a right angle those are exactly 2 and 2.0000000000000004, which tilts the apex a few ULPs off both offset lines so the collinear T-junctions stop collapsing and a mitered square returns with 8 corners; **(b)** `Arrangement2d` hole assignment must be by **connected component** (union-find over edges), not by area+containment — a lone convex cell adopted its OWN reversed perimeter as a hole (the two shoelace sums differ by an ULP and every vertex is shared, so the containment probe sat exactly ON the boundary and decided by luck), cancelling the cell to ~1e-16 and silently vanishing a whole operand from a union; loops of one component can never nest, which is the structural fix; **(c)** `FaceGeometry.Contains`' one-sided upward-v ray **cannot see a rim below the probe**, so it calls every point on a pole-bounded face outside (a sphere's cross-section came back empty) — run the parity in BOTH v directions: they agree on a properly closed trim and disagree exactly when one side is a pole; **(d)** independently-evaluated mesh vertices projected to 2D are only ULP-equal, so two edges that should be collinear sit ~2e-16 apart — too small for the arrangement to see as a T-junction, too large to ignore — and the sliver's interior sample rounds onto its own boundary, making bulk unions **order-dependent** (60.42 vs 59.33 on a 16-segment torus; a 64-segment one threw outright). `PlanarSection.SilhouetteGrid` quantizes projected coordinates at 1e-12 of the outline extent (scale-free tier) and every merge order then agrees. **Exact curved 2D booleans** (`CurvedEdge2d`/`CurvedRegion2d`/`CurveIntersection2d`/`CurvedArrangement2d`/`CurvedRegion2dBoolean`/`CurvedRegion2dOffset` in Core.Geometry2; `Curve2d.TryToCurvedEdge`/`FromCurvedEdge` and `Profile.FromCurvedRegion` in BRep; `Sketch.ToCurvedRegions`/`FromCurvedRegion`/`UnionExact`/`IntersectExact`/`SubtractExact`/`OffsetExact` in Modeling): everything built from a `Region2d` used to inherit chord flattening — offsets, sketch booleans, sections, silhouettes, and the `Profile`s an extrusion is built from — because arcs were polygonized before the arrangement saw them. The curved tier carries **lines and circular arcs through the arrangement unflattened**: a disc measures πr², two overlapping discs' union/intersection/difference match the analytic lens formulas to 1e-10, round offset joins are **exact sectors** (which RETIRES the inscribed-arc contract rather than honouring it — an exactly-offset arc IS the true offset), and a 40×20 plate with a Ø12 bore extruded 5 mm comes out within 1e-6 relative of (800 − 36π)·5 where the default-flattened route is 3.6e-5 off — an error that is a FLOOR no tessellation density can lower, since it is baked into the profile before any solid exists. **A PARALLEL type, not an extension of `Arrangement2d`**: the straight arrangement is boolean-critical and pinned bit for bit (now by `Region2dGoldenTests`' committed fingerprints as well as by the rendered PNGs), and teaching it curves would change its fan comparator (positions → tangents), its edge identity (a vertex pair → a vertex pair PLUS a carrier, since two points on one circle are joined by two arcs and by a chord) and its area rule at once, inside the code with the widest regression surface — the same call design.md §5 makes for `FaceSplitter`. **Lines and arcs is a COMPLETE tier, not a budget**: the cell walk orders edges at a node by departure TANGENT with departure CURVATURE as the tie-break (from p(s) = v + s·d + ½s²κ·n̂), and for lines and circles agreeing in both means sharing a carrier — a line and a circle never osculate, and two circles that osculate are one circle — so the tie-break is complete and the walk never guesses, whereas two Béziers can agree to second order and separate only in the third derivative. Béziers therefore stay flattened at the entry points (stated in the API contract, not hidden) and the comparator refuses by name if ever handed a second-order tie between different carriers. **ONE tolerance, and it is a LENGTH** — the arrangement's own vertex snap distance, with no second epsilon in the tier: a line is tangent to a circle when the centre's distance from it differs from the radius by less than it, two circles when their centre distance differs from r₀+r₁ or |r₀−r₁| by less than it, a point is on an edge when its distance is under it. **Near-tangency SNAPS to one touch point rather than refusing or dropping the contact**: both alternatives are unstable (two crossings a nanometre apart is a sliver cell classified by rounding), snapping is area-neutral to O(τ^1.5), and a tangential contact is *representable* here precisely because the two edges leave the node with equal tangents and different curvature. The interior sample gains one thing the straight proof did not need — the push is capped by the edge's own **curvature radius**, or a small circular hole inside a large cell sends the sample past the circle's centre and out the far side — and classification uses the epsilon-free `ParityInside` rather than the closed-set `Contains`, whose on-boundary band would answer "inside both" for a cell thinner than the weld tolerance. Section loop assembly **never welds**: crossings are solved once per EDGE on that edge's exact curve so both adjacent faces get the same point, and endpoints are node positions rather than the curve re-evaluated at a searched parameter (a ternary search left ~5e-11, enough to put a box corner at x = −4.999999999963106). **Debug modifiers + validation report**: `Part.Ghost/Hidden/Isolated` (OpenSCAD %/*/! analog; `#` highlight stays the selection mechanism) with ALL rules in `DebugFilter` (Modeling) — Ghost renders translucent via `Part.EffectiveDisplayMode` (the property every render path reads: window `EffectiveModeOf`, offscreen, web frame build) but never exports; Hidden neither renders nor exports (byte-identical offscreen render to a scene never containing it — it must not even influence framing); Isolated shows/exports only isolated parts — shared by SceneHost visibility, `RenderToImage`, `--export` (all formats incl. STEP via the filtered instance list) and MCP screenshot/export; no flags = identity, locked by test. `SceneReport.Create(scene)` (assert/echo analog): per-part kind/faces/watertight/volume/area/bounds + named notes (open mesh with boundary-loop count, meshing failure captured not thrown, non-positive volume, active debug flags), `ToText()` aligned table behind the viewer's **Check** toolbar button, `AllClean` for scripts; docs in `examples/viewer.md`. **2D interchange (DXF & SVG)** (`DxfDocument`/`SvgDrawing` in Modeling, docs `examples/dxf-svg.md`): DXF both ways — sketches out as exact LWPOLYLINE bulge arcs (bulge = tan(sweep/4)) / CIRCLE per full-circle loop / cubics flattened at a stated chord tolerance, entities (LINE/ARC/CIRCLE/LWPOLYLINE, layers) back in as `Sketch`es (closed polylines directly, loose entities endpoint-chained at the weld tier, unclosable chains reported not invented; nesting left to the caller); SVG out with line-CLASS-driven styling (Visible/Hidden/Section → solid/dashed/dash-dot groups per layer — the build123d edge-classification lesson) consuming Section/Silhouette regions and exact sketches (`A`/`C` commands), y-up mm flipped once at the root. **Engineering drawings** (`HiddenLine.cs`/`Drawings.cs`/`SheetAnnotations.cs`/`SheetExport.cs` in Modeling; docs `examples/drawings.md`): `HiddenLineRemoval.Project` turns a scene into classified 2D line work — `HiddenLineRun(Points, Visibility, Source)` — and the fidelity split is stated in the output rather than hidden: what is DRAWN is exact wherever the kernel has it (a B-Rep part's `GetFeatureEdges`, so a bore rim is a smooth circle at any mesh quality) while what is ANSWERED ("is there material in front of this point") comes from the display mesh, and the one gap — a smooth surface has no modelled edge along its outline — is filled from the mesh's view-dependent silhouette and labelled `EdgeSource.Silhouette`. **Visibility is two stages and the first is exact**: the surface immediately around a sample point is read from its own instance's mesh, and if every face there points away from the viewer the point is buried in its own material — hidden, no ray, no non-local query — which settles the majority of a solid's edges for free; only then does a ray go out, started at the point stepped off along the **most eye-facing local normal**, which is what makes the tangencies work (on a bore's bottom rim that normal points into the void, so the ray runs up the empty hole instead of scraping the wall it is parallel to) and simultaneously fixes the exact-edge-inside-the-inscribed-mesh problem (an exact point on a concave surface sits inside the mesh by up to the chord sagitta, which is why the bias is a FRACTION of the model and not a weld-tier constant). Three measured rules: **feature-edge segments must be CHAINED into polylines before sampling** — a segment is the unit a run can split at, so a rim delivered as 96 chords put the boundary on 4.870, exactly the 52.5° sample, where the chained form bisects to 5.000 (the general lesson: check that what a refinement refines is not already quantized by its input); **a run shorter than a pen stroke is absorbed into its neighbour**, because within one bias step of a model VERTEX "the surface here" legitimately includes the far side's faces and no epsilon removes a geometric ambiguity (measured cost: three corner junctions on a box lose 0.037 of an analytic 57.155, one bias each); and **at a tangency the coincident pair is drawn once, solid** — reached structurally, since a cap normal exactly perpendicular to the view is not rejected by the back-face stage. `DrawingSheet` is paper + border + title block + placed `DrawingView`s in sheet millimetres (origin bottom-left, y up — `SvgDrawing`'s convention); `StandardLayout` builds front/top/right + iso at one shared scale, the largest ISO 5455 ratio that fits (`DrawingScales`), third or first angle, with the three directions from the new **`StandardViews`** — the direction table MOVED down from `ViewCubeMath` into Modeling and delegated back, because a sheet needs it and is built with no viewer in the room, so widget and sheet read ONE table. A projection is cached and PLACEMENT applied after, so layout is cheap. **A section view takes a POINT, not a plane** (`SectionThrough`): a section view's cutting plane is perpendicular to its own view direction by definition, which is what makes the exposed faces project in true shape and so worth hatching and dimensioning — an oblique cut is offered as a view along the oblique normal rather than as a foreshortened lie; `ClippedBySection = false` parts pass through whole; cut faces come from `PlanarSection.OfSolid` where the part lowers and the mesh cut's loops where it refuses (a flush plane). `SheetHatch.Fill` clips 45° lines by an **exact even-odd scan** whose one careful decision is a crossing at a vertex — a HALF-OPEN span test, so a vertex is counted by exactly one of its two edges — anchored to the ORIGIN so neighbouring cut faces share one pattern. The drafting layer (`SheetAnnotation`: linear/aligned/horizontal/vertical, radial, diameter, angular, notes, BOM balloons) **splits two unit systems, which is the point**: anchors and measured values in projected MODEL coordinates so a dimension reads the part, anatomy in PAPER millimetres so it stays printable at any scale — and `SheetStyle` states each length as a RATIO to its text height, those ratios being `AnnotationGeometry`'s pixel constants over its own 12-px text height, asserted by a test that reads both sides. `SheetWriter.ToSvg/ToDxf` consume one `Compute()` so the writers cannot disagree; SVG gained fixed sheet sizing, open polylines and text (written OUTSIDE the single y-flip with y negated, since inside it the flip mirrors every glyph), DXF gained a TEXT entity both ways and an **LTYPE table** — a file that NAMES a line type its layers use must DEFINE it or every reader shows solid lines and the classification is lost in transit. Two layout bugs the rendered sheet caught that no unit test could: sizing the view grid from the three orthographic views alone lets the isometric (always widest, being a diagonal of the whole box) spill over its neighbours, and a label that clears the line work still lands on a dimension VALUE. Core gained **`Intersect3d.RayTriangle`** beside `Distance3d` for the same reason `ClosestPointOnTriangle` moved there — it had been written twice (`ScenePick`, `AmbientOcclusion`) and the copies differed in exactly the place the split now makes explicit: the predicate is shared, the acceptance range is the caller's. **Docs site**: DocFX under `docs/` (getting started + 22 example pages + API reference from XML docs) with **executable examples** — `tools/EngrCAD.DocsGen` extracts ` ```csharp render:<id> ` fences from the markdown, compiles/runs each via Roslyn scripting (snippet must define `Scene scene`), renders through `EngrCad.RenderToImage`, and writes `docs/examples/images/<id>.png`; nonzero exit on any snippet failure, so examples can't rot (NN snippets, MM rendered; PNGs committed so the site builds without a GPU; `render:` fences may declare `sectionPlanes`/`sectionCombine`/`camera`/`explode`/`preview` as snippet variables — `preview` is a `ConstructionPreviewRequest`, so a docs page can render a construction-tree rollback view headlessly). The site also hosts the **live WASM demo** at `/live/` — the workflow publishes `samples/EngrCAD.WebDemo` into `_site/live/` after docfx runs and `docs/examples/web.md` embeds it in an iframe, so one page on the site is the real kernel rather than a screenshot. `.github/workflows/docs.yml` builds + deploys via actions/deploy-pages (windows runner for WARP rendering; Chris must set repo Settings → Pages → Source: GitHub Actions before first deploy). Remaining big rocks: SIMD passes, nuget.org publish (`Directory.Build.props` URLs still placeholders — a real remote exists at github.com/veggielane/EngrCAD).

- .NET SDK 10.0.302 installed **user-local** at `%USERPROFILE%\.dotnet` (win-x64 — the dev box is an i9-9900K; historical arm64 benchmark labels are from the previous laptop, and NEW measurements must be labeled win-x64), on the user PATH with `DOTNET_ROOT` set. Build with `dotnet build EngrCAD.slnx`, test with `dotnet test EngrCAD.slnx`.
- Git repository initialized; commit only when Chris asks.
- Target framework: **.NET 10 (LTS)** via `Directory.Build.props`.
- **NuGet**: all `src/*` projects pack (shared metadata — `Version 0.1.0`, MIT, package READMEs — lives in `Directory.Build.props`; license/URLs are placeholders Chris must confirm before any nuget.org push). `src/EngrCAD` is a meta-package whose ProjectReferences become package dependencies (viewer kept separate so headless consumers don't pull Avalonia). Local feed workflow: bump `<Version>`, `dotnet pack EngrCAD.slnx -c Release -o C:\Users\chris\nuget-local`; the folder is registered as source `engrcad-local`, so consumers just `dotnet add package EngrCAD` (+ `EngrCAD.Viewer`). Note: NuGet caches by version — after repacking the *same* version, delete the cached copies under `%USERPROFILE%\.nuget\packages\engrcad*\<version>` (or just bump the version).

## Architecture

Three engines with different mathematics and data structures, plus interop and query layers on top of a shared core.

### Core (foundation for everything)
- Zero-allocation math: `readonly struct` `Vector3d`, `Matrix4x4d`, quaternions, `AABB`
- Central tolerance/epsilon policy for robust floating-point comparison
- Spatial acceleration: BVH and octree (used by all engines and by the query layer)

### Mesh engine (discrete)
- Half-edge data structure for O(1) topology traversal
- Algorithms: booleans, decimation, subdivision
- Bulk data stored data-oriented (SoA) for cache locality

### Implicit engine (volumetric)
- SDF evaluator: `(x, y, z) → distance`
- Primitives (sphere, box, cylinder, …) and operators (union, intersect, smooth blend) composed as an AST
- SIMD-batched evaluation; later, compilation of C# expression trees down to SDF graphs / IL / compute shaders

### B-Rep engine (parametric) — hardest, built last
- Geometry: planes, cylinders, cones, NURBS surfaces/curves
- Topology wrapper referencing geometry: Solid → Shell → Face → Loop → Edge → Vertex
- Surface–surface intersection engine; booleans and filleting on top

### Interop layer — conversion triangle complete
- Implicit → Mesh: `SurfaceNets.Polygonize` (manifold dual contouring)
- B-Rep → Mesh: `BRepTessellator.Tessellate` (shared edge polylines + ear clipping + welding)
- Mesh → Implicit: `MeshSdf` (BVH nearest-triangle + angle-weighted pseudonormal sign; requires closed mesh; composes with all `Sdf` operators)
- Field → Mesh quality: `SdfProjectionTarget` implements EngrCAD.Mesh's `IProjectionTarget` over any `Sdf` (`p − d·∇d/|∇d|`, iterated; difference step relative to the field's bounds). **The guarantee is one-sidedness, not convergence**: a 1-Lipschitz lower bound puts the surface at least `|d|` away in every direction, so a step of that length can never cross it — but `|d|` need not decrease, and near a CSG difference's **fictitious faces** (the subtracted tool's own surface, inside removed material) two branches trade the point back and forth forever (measured on `Box(2,2,2) − Sphere(1.2)`: six steps leave a probe exactly where it started, true distance 0.45 vs a field reading 0.14). Harmless for the job it exists for — a remeshed vertex is always within a fraction of an edge length of the surface, where every field here is locally exact — so project near-surface points only, never as a general closest-point query

### Simulation layer (`EngrCAD.Fea`) — tet meshing done
- Closed surface mesh → `TetMesh`: exact-predicate Delaunay, winding-number classification, verified boundary recovery, optional quality/sizing refinement
- `QuadraticTetMesh` (10-node), `TetQuality` (dihedral + radius-edge + slivers), per-element region ids, per-facet source-triangle tags
- Depends on Core + Mesh only; nothing depends on it (a clean leaf). Next: element stiffness and assembly onto `EngrCAD.Core.Solvers`

### Query layer (LINQ) — v1 implemented
- `SpatialCollection<T>` (items + bounds expression + BVH) with `AsQueryable()`: a custom `IQueryProvider` rewrites expression trees, recognizes `SpatialPredicates` clauses (`.Within(box)`, `.WithinDistance(p, d)`, `.HitBy(ray)`) applied to the registered bounds accessor, answers them from the BVH, and re-applies the full predicate over candidates (interception is a pure optimization). Residual/non-spatial queries fall back to LINQ-to-Objects. `LastQueryUsedIndex` is the diagnostic.
- IMPORTANT: query predicates use the by-value `SpatialPredicates` extension methods, NOT `Aabb.Intersects` — expression trees cannot contain calls with `in` parameters, which the kernel API uses.
- Topology-traversal LINQ exists on mesh handles (`vertex.OutgoingHalfEdges().Where(...)`, `face.AdjacentFaces()`, `Face.Bounds`).
- Future: metadata indexes for B-Rep feature queries (cylindrical faces by radius), expression-tree→SDF compilation.

## Planned solution layout

```
EngrCAD.sln
src/
  EngrCAD.Core/       math structs, tolerances, AABB, BVH, octree
  EngrCAD.Mesh/       half-edge mesh engine
  EngrCAD.Implicit/   SDF primitives, operators, evaluator
  EngrCAD.BRep/       parametric geometry + topology
  EngrCAD.Interop/    conversions between representations
  EngrCAD.Query/      IQueryable provider, spatial/topology LINQ
  EngrCAD.Fea/        tetrahedral meshing for simulation (Core + Mesh only)
  EngrCAD.Modeling/   unified Shape API (rep chosen at the end via To*)
  EngrCAD.Viewer.Core/ UI-free render model (shaders, camera, sections) - no Avalonia/Silk.NET
  EngrCAD.Viewer/     Avalonia + Silk.NET (OpenGL) viewer *library* (EngrCad.Show)
  EngrCAD.Web/        Blazor WebAssembly viewer component (WebGL2, kernel in the browser)
  EngrCAD.Mcp/        MCP server: a model program serves itself to AI assistants (--mcp)
samples/
  EngrCAD.Demo/       console showcase: builds a Scene, calls EngrCad.Show
  EngrCAD.WebDemo/    Blazor WASM host for EngrCAD.Web
  EngrCAD.LiveDemo/   live-modeling loop: EngrCad.Run + dotnet watch hot reload
tests/
  EngrCAD.Core.Tests/ (one xUnit test project per src project)
  ...
```

Kernel projects (`Core`, `Mesh`, `Implicit`, `BRep`, `Interop`, `Query`, `Fea`) must stay free of UI/rendering dependencies; only `EngrCAD.Viewer` references Avalonia/Silk.NET.

## Performance mandates (non-negotiable in kernel code)

- Math types are `readonly struct`; hot paths allocate nothing on the heap
- Use `Span<T>`/`Memory<T>`; temporaries come from `ArrayPool<T>` or `stackalloc`, never `new` per call
- Use `System.Runtime.Intrinsics` (SIMD) for SDF evaluation, ray/primitive intersection, and other batch kernels
- Bulk mesh data uses structs-of-arrays, not arrays-of-objects
- Never compare floats with `==`; all comparisons go through the central tolerance policy in `EngrCAD.Core`
- **A nearest-first branch and bound is already its own seed.** Seeding `Bvh.Nearest` with the previous coherent sample's answer (distance is 1-Lipschitz) is provably result-identical given strict `>` pruning and a strictly-greater seed — and buys only **1.12–1.20×** on the most coherent run available, plus a small net *loss* on scattered probes. Descending the nearer child first already reaches a tight bound in O(log n) node tests, so a seed can only save part of the first descent. `MeshSdf` therefore keeps the base class's scalar batch loop, and `MeshSdfBatchTests` pins batch == scalar bit for bit so the next attempt is held to the same bar.
- **Never benchmark an optimization against a baseline you wrote differently.** A prototype of that same seeding measured 1.88×; its baseline went through a `Func` delegate per triangle while the seeded path called the kernel directly. The entire gap was the delegate.
- **A packet query's shared bound is governed by its group's DIAMETER — and the batch seam delivers collinear ROWS, not compact blocks.** The other lever on `MeshSdf`'s 74–85% `Bvh.Nearest` cost was a packet: one traversal per coherent group, per-point pruning at the leaves. Built and measured (`MeshSdfPacketBenchmark`) against the production `Bvh.Nearest` with the same struct metric: a compact 2³ block of grid points wins **1.45×**, the same 8 points laid out as a z-consecutive row **0.86×**, and 64 of them — spanning 1.9 units on a 3-unit model, so the shared bound becomes the whole model and the packet degenerates into a brute-force scan — **0.30×**. A node is visited whenever it could beat the *worst* member's best, so spread kills it. `Sdf.Evaluate(points, distances)` is a flat span every bulk consumer generates z-fastest, and `MeshSdf` cannot regroup a collinear run into blocks; teaching the batch contract to carry "these points form a compact block" is a large API change to buy a win in a shape no caller produces. Tie-breaking never became the question. Two negatives ride along: seeding the packet from one exact query at the group's centre changes nothing and makes the best case *worse* (the nearest-first lesson again), and pruning on **squared** distances throughout — removing a `Math.Sqrt` from every box and triangle test — is 0.94–0.99×.
- **A continuation needs a complete seed set; the only sound way to prove one complete is a cull; and the cull has already produced the visit set, so the continuation adds nothing.** That is why `SurfaceNets` visits a shell around the surface by *culling* rather than by seeding and flooding (`SurfaceCull`). The argument needs one property and no epsilon: an `Sdf` here is 1-Lipschitz, so every point p of a block with centre c and half-diagonal R satisfies |d(p)| ≥ |d(c)| − |c − p| ≥ |d(c)| − R, and |d(c)| > R therefore rules out a sign change anywhere in it. Cell and quad loops keep their exact (j, k) order and only skip runs of tiles, so output is **bit-identical** including face ordering. Two implementation notes: the x/y dilation must be computed per row rather than baked into a stored mask (dilating a block per axis costs a block of extra samples on three sides of the surface — most of what the cull just saved), and the one-cell slack on the radius is cushion for blends, which are only approximately Lipschitz, not part of the argument.
- **An 8× saving in the dominant cost bought 2.5×, because the dominant cost stopped being dominant.** The cull evaluates 28.4% / 15.6% / 12.0% of the grid at resolution 96 / 192 / 256 and buys 1.60× / 2.18× / 2.46× (a `MeshSdf` field, 2.99× at res 96). Measured at res 256 with a field costing one square root per sample, the whole call still takes 132.8 ms against 129.3 ms for the real CSG field: evaluation is now free and the cost is assembly — `HalfEdgeMesh.Build` alone is 39–48%. **Re-measure where the time goes after every win**, or the next optimization goes into the part that no longer matters.
- **The half-edge builder's twin resolution is a COUNTING SORT, and the grid's contribution was the flat buffer, not the adjacency.** Following the finding above into assembly: the cost was a `Dictionary<(int, int), int>` probed twice per directed edge (over a million tuple hashes at resolution 256) plus one `int[4]` heap allocation per quad. Both are gone — every directed edge is filed under `min(from, to)`, so an edge and its reverse share a bucket that holds only one vertex fan (three to six entries), and ONE scan of it finds the non-manifold duplicate and the twin at once; buckets fill in ascending half-edge order, so "already seen" is a `break` and the scan observes exactly what the dictionary probe observed, error-detection order included, which is what keeps every exception message identical. Measured interleaved in one sitting: assembly **3.3–3.7×** (47.5 → 13.4 ms at 129 268 vertices), whole polygonization 1.2–1.5×, allocation at res 256 **145 → 103 MB**, output bit-identical with the golden fingerprints untouched. **Two things worth keeping.** The plan was "build the half-edge structure from the known grid adjacency, keeping the generic `Build` as a debug cross-check" — but the grid does NOT hand over twins for free (a dual edge is a grid FACE, and matching its up-to-four claimants needs an O(n³) face table the streaming window cannot hold), whereas the counting sort is O(n) with no hashing and serves every caller in the repo. Making the generic path fast beat making a second special one, and it left ONE implementation instead of two-plus-a-cross-check. And the honest closing number is the SHARE, not the speedup: assembly went 38–51% → 15–18% of the call, so the builder has stopped being the place to look and the remaining ~175 ms of a 213 ms res-384 polygonize is the grid walk itself.
- **A direct-vs-iterative crossover measured on ONE operator is a measurement of that operator's conditioning, not of the two algorithms.** `SparseCholesky` vs `SparseSymmetricCG` was benchmarked on grid Laplacians **plus an identity shift**, which is strongly diagonally dominant — so CG converged in an *n-independent* ~35 iterations at every size, and the recorded verdict ("past ~14k unknowns one-shot CG wins") was really "CG wins on a well-conditioned system", which is not news. Drop the shift for a pure Dirichlet Laplacian, the regime an FEA stiffness matrix lives in, and at 62 500 unknowns CG needs **858 iterations / 750 ms** against AMD-ordered Cholesky's **221 ms factor + 5.8 ms per solve** — the direct solve wins on the FIRST right-hand side, by 3.3×, the exact opposite conclusion from the same code at the same size. Vary the CONDITIONING as well as the size, or the table describes a fixture. (AMD itself: 4.6–13.4× on factor time, 3.5–8.3× on fill, never a loss — but opt-in, because a fill-reducing permutation changes the summation order and every committed upstream number was measured natural.)
- **An ambient-occlusion bake is a NEAREST-hit query, and a ray that ESCAPES cannot be pruned.** Two negatives worth keeping about the AO bake's 12.3 s. First, the obvious lever is not there: occlusion accumulates as `1 − t`, so a hit darkens in proportion to how close it is, and the any-hit early-out that "is this ray blocked" invites would be a different renderer (measured 0.055 darker over the vertices that see an occluder at all; a boolean bake is literally the search-radius-to-infinity limit of this one). Second, the exact lever that IS available — descending the nearer child box first — pays only in proportion to how OCCLUDED the geometry is, because an escaping ray never sets the bound below 1 and therefore prunes nothing: **1.19× on a gyroid lattice (mean occlusion 0.70), 1.04× i.e. nothing on a smooth CSG blob (mean occlusion 0.95)**. And the ray count is not a free knob either — halving 16 → 8 is 1.6–1.9× but changes **59 of the 87 rendered docs PNGs**, so it is a decision about appearance, not about performance. The no-less-honest-bake rule survives all three.
- **A block-pointer table costs 8 bytes per block whether or not the block is ever touched.** That is 0.2% of a block's samples and irrelevant up to a 1024³ grid — and 134 MB, allocated before a single sample is evaluated, at 4096³. Hence flat below a threshold, two-level above it, rather than one shape everywhere.
- **Interleaving a grid algorithm's phases is what drops the memory bound from O(n³) to O(n²)** — but the face ordering must then be reconstructed deliberately (bucket by whichever loop variable was outermost in the un-streamed version, then concatenate), or a "bit-for-bit identical" claim quietly holds for vertices and fails for faces.
- ****A fold COUNT is not a tessellation quality metric — measure the worst normal dot.** The cross-drilled bore wall that rendered as a visibly crumpled fan had **zero** strictly-inverted triangles before the fix and zero after; what was wrong was a worst facet-vs-surface dot of **0.0198**, an 88.9° sliver, which a fold count reports as a clean mesh. And the count is not the convergence test either: volume excess over the analytic value ran 61.19/18.60/13.40/11.25 at 32/64/128/256 segments-per-circle, i.e. ratios 3.29/**1.39/1.19** — stalling near 11 and never converging — against 76.20/21.49/5.97/1.82 (ratios 3.55/3.60/3.27, quadratic) after. **Ear clipping is STRUCTURALLY forced into a fan wherever a boundary run is exactly uv-collinear**, which is the normal case, not a pathology: both ring loops of an `ExtrudedSurface` band pull back to a bit-identical v (measured: distinct v bits = 1, 32/32 exactly-collinear uv triples), so `IsEar`'s `<= 0` rejects every corner along both chains and only the unrolled rectangle's four corners are clippable — so no change to the shortest-diagonal metric could have helped, and `Refine` then bisected the fan chords into slivers. Nor would the existing merge walk (`ZipBand`) have worked: it pairs chains by u, so a dense breakout curve against a coarse ring gets fanned from one far vertex and inverts where the curve turns back (measured uv cross −5.9e-4). A monotone sweep is the fix. A fold test must also never take the surface normal at a triangle's CENTROID.** A centroid sits a sagitta (~1e-3 at display quality) inside a curved surface, far past the 1e-6 inverse-evaluation tolerance, so `TryProjectPoint` fails and the assertion silently checks nothing — the first run of the mitered-fillet fold test reported zero folds when there were 808. Average the normals at the triangle's three **vertices**, which are exact surface points by construction.
- **A grid quad's diagonal is now chosen by GEOMETRY — and the tie guard was the hard half.** `PolygonFan` is the one rule (`Triangulated`, `SignedVolume`/`FaceFanStart`, `MeshMassProperties`, `MeshConnectedComponents`, `RenderMesh`, STL/3MF/AMF): a quad splits along its **shorter 3D diagonal**, n-gons keep the corner-0 fan. That resolves the recorded left-hand-thread finding exactly — the mirrored rod's deficit was 3× its twin's purely because mirroring swapped which diagonal a corner-0 fan picked, and the two now measure IDENTICALLY (1.5963 / 0.3997 / 0.1001 at 32/64/128 segments for both, against a mirrored 4.7643 / 1.1974 / 0.3002 before). **Three things the work taught.** (a) **An exact comparison would have reintroduced the same defect**: a great many grid cells have *mathematically equal* diagonals — every UV-sphere quad is mirror-symmetric about its own meridian — so their computed squares differ only in the last ulps, and **408 of the 960 quads of `UvSphere(40, 26)`** flip on round-off at a ratio of 1.000000000000, with the two triangulations equal to twelve digits of the volume deficit. Letting an ulp decide measurably perturbed decimation and remeshing; the fix is a *relative* tie guard (1e-12 on squared lengths, scale-free tier), and the collateral test failures vanished with it — which is what "a test needing a looser band is a bug" looks like when you follow it. (b) **The win is CONSISTENCY, not universally less error**: on a saddle cell the two triangulations bracket the surface (a twisted lofted column reads 244.37/255.27 at 16/12 and 249.66/250.34 at 256/192 — equal in magnitude, opposite in sign, both converging on 250), and one docs assertion was quietly reading the SIGN of the corner-0 fan rather than a property of the solid. (c) **`MeshMassProperties` was 1.5 out of 69.5 adrift** because it walked the half-edge arrays with its own corner-0 fan — a shared rule only holds if every consumer asks it, and the test that asserts volume/render/triangulate/mass-properties agree is what found it. Cost: **18 of 87 rendered docs PNGs move**, all in the non-planar-quad families (SDF/Surface Nets, threads, lofts).
- **A polygon's winding may be reversed freely; its FAN DIAGONAL may not.** Re-winding a quad `[a, b, c, d]` as `[d, c, b, a]` is the same cyclic polygon the other way round, so it is correct for orientation — and wrong for geometry, because everything downstream triangulates by fanning from vertex 0, which moves the split from a–c to b–d. On a skewed non-planar grid cell those are not equally good triangulations. `BRepTessellator`'s reversed-face flip did exactly this, so every *subtracted* tool's face got the worse diagonal: measured on an M8 B-Rep threaded hole (sheared helical grid, cell diagonal ratios up to 40:1) **5 544 of 30 912 facets faced INWARD at a worst facet-vs-surface agreement of −0.163**, while the identical geometry unsubtracted — a threaded rod — was clean at 0.99976. The fix is to rotate the reversal so vertex 0 stays put (`[a, d, c, b]`). The lesson generalizes past this bug: **a quality audit must fan polygons exactly as the consumer does**, because auditing the quad as a unit would have reported this mesh perfect.
- **The general algorithm beat two special cases, again.** The strip path's two documented gaps — a band end sampled at more than two points (a curved cross edge) and a band whose chains meet at a point — were both closed by replacing the rung-counting split with the **monotone stack sweep** already used for slabs, which handles a tied run of end vertices and a single apex as its ordinary start and finish. The load-bearing detail is the tie-break: take the LAST of the tied minimum run and the FIRST of the tied maximum run, so a whole tied run lands on ONE chain — split across both, the merge interleaves the sides at equal keys and the sweep is handed collinear points to triangulate. Every band in the docs tessellates bit-identically either way (all 52 rendered PNGs unchanged), which is what a generalization done right looks like. **Neither shape is reachable from the `Shape` API yet** — `Sphere − Box` corner cuts and cone-apex cuts are refused earlier by the exact boolean, and eighteen further candidates reached neither — so they are covered by hand-built faces in `TrimmedBandGapTests`.
- **Both loud trimmed-face refusals are UNREACHABLE from the `Shape` API, and that was worth finding out rather than guessing.** Pole-bounded single-chain bands with holes and |winding| > 1 loops are blocked one layer up, by the exact B-Rep boolean. The near miss is instructive: a latitude cut DOES give a sphere a pole-bounded cap with a single winding loop, and drilling that cap off-axis does NOT put a hole in it — the boolean re-splits so the bore's rim lands on the two-ring band below, which the slab sweep already handles. Cutting lower, or the same on a cone or a torus, fails earlier with "produced an unclosed solid"; |winding| > 1 needs a curve wrapping a periodic surface twice, which only a helical intersection gives and those are refused before tessellation. `TrimmedFaceRefusalTests` locks the verdict AND drives both refusals on hand-built faces, so the backstop's messages cannot rot while nothing reaches them.
- **A non-converging error whose sign FLIPS is a fixed floor, not a tolerance.** A bore drilled into an extruded SIDE face misses the inscribed-ngon volume by −7.4e-4 / −5.3e-5 / +4.7e-5 / +6.5e-5 at 32/64/128/256 segments, while the identical hole in the top cap lands within 1e-13. The cause is not the trimmed triangulation, which was the standing assumption: the rim on the side face is a **57-sample `PolylineCurve3d` baked in by the marching tracer**, because plane-as-a-bounded-extrusion ∩ cylinder is not one of `SurfaceIntersection`'s analytic pairs, and a 57-gon is a floor no `segmentsPerCircle` can lower — the sign flips when the analytic reference's own n-gon crosses it. Same shape of finding as the cross-drilled housing's breakout curves. The fix is a promotion in `SurfaceIntersection`, not a sampling rule.
- **Refinement can make a mesh WORSE — resolved by interior rows in the base triangulation, and the numbers prove the diagnosis.** The base has to carry the surface's curvature itself: trimmed bands now take the natural grid's own sample rows into their BASE (`RowedStrip`/`RowedPeriodicBand`/`RowedPoleFan` — level paths threading between scallops, anchored on existing boundary vertices, never inventing a boundary point; full-period rows with closure duplicates for winding bands; seam chords pre-split with bit-identical twins since a seam is an unrolling artifact internal to the face; a uv-area identity as the closing guard with the rowless sweep as fallback, so the rowed path can never be worse). The drilled sphere `Sphere(10) − Cylinder(3, 40)` went **43 948 facets / 12 folds / worst −0.2022 → 3 244 / 0 folds / 0.9994** (32/24), stopped refusing at 128/96, and converges at ratios 4.35 / 5.08 — it is now a corpus member with an analytic napkin-ring volume row. Two rules that made it work: **`Refine`'s step metric is per-axis (max-norm), never the 2-norm** — the natural grid's own cell diagonal spans one step in EACH axis and measures √2 under a 2-norm, so refinement would bisect the very grid that defines the quality bar; and **pole-fan edges are refinement-exempt** — the pole's u is arbitrary, so a fan edge's uv u-span is an artifact, not curvature (refining a *flat* vase disk's fan bent it into 467 folds at −1.0). With rows in place Refine measures IDLE on 16 of 19 corpus members' trimmed faces and keeps only its honest duty: residual coarse columns between honest samples.
- **The corpus quality gate is `TessellationCorpusQualityTests` + `TessellationQuality`** — 30 constructions (drilled/cross-drilled plates, spherical cavities, a drilled sphere, threaded rods and holes, lofts, shells, drafts, mitered and whole-solid fillets, chamfers, vases, partial revolves, sweeps, tori, cones, curved-face shells and drafts, sketch pockets, Bézier engraving, wedges) audited facet-by-facet against their exact surfaces at 16/8, 48/24 and 96/48, on **unwelded per-face polygons** (`BRepTessellator.TessellateByFace`, an internal seam — welding is what destroys the facet→face attribution the measurement needs). One floor for every surface family, `cos(3·2π/n)`: three natural grid steps, the allowance being for facets where two INDEPENDENTLY sampled boundaries meet. A per-family floor tuned to what each happened to measure would pass everything and protect nothing. It also separates **degenerate slivers** (facet height below `1e-13 × the face's extent` — relative, since an absolute epsilon on a cross product is an area threshold) from **folds**: "the normal is noise" and "the normal points the wrong way" are different defects with different fixes, and both get asserted. First run caught three defects; all three are fixed (the third by the interior-rows upgrade above). Now **30 constructions** — the drilled sphere joined once rows took it past the floor at every density, and the curved-corner work added a shelled cup, a shelled cone, a shelled elbow, a drafted cylinder and a variable-radius fillet, all clean at all three densities with `KnownOffSurface` still empty. The one construction still below the floor, `Box(20,20,20) − Sphere(12)` (the cavity breaks out of all six faces), went **101 246 facets / 266 folds / worst −0.2426 → 4 608 / 0 folds / 0.7024** at 48/24 and tessellates at 96/48 where it used to refuse; what remains is a narrow column at each hole rim's u-extreme where the rim tangent goes vertical and no level path can anchor — locked with committed baselines in `SpherePiercingEverySide_HasNoFoldsAndABoundedResidual`, residual filed in todo.md.
- **Zero-area triangles are not merely useless — near a tangency they are actively WRONG.** A sliver clipped along a boundary curve takes that curve's binormal as its normal, so wherever a trim curve is tangent to a neighbouring face (every fillet's tangency line) its orientation is decided by rounding, and no epsilon fixes it. See the trimmed-face tier order above.
- **Refinement is not a convergence mechanism.** The midpoint-split pass terminates on a monotone-decrease rule, so wherever that rule cuts a cascade the mesh keeps a coarse patch forever. A tessellation whose accuracy depends on refinement has no convergence guarantee — measured, the ear-clipped fillet's volume error stopped improving past curveSamples 96 and then wandered. Get the base triangulation right; use refinement only for genuine curvature between honest samples. **This rule is now enforced structurally in the trimmed tiers**: interior rows put the natural grid into the base, and the corpus measures refinement idle wherever the rows engage — where refinement still visibly inflates a mesh, the finding is a missing row path, not a refinement bug.
- **A recursion whose depth is a property of the INPUT has no "big enough" stack.** Learned from the (now deleted) BSP boolean, and the rule outlives the code: `CsgNode`'s splitting plane was the first polygon's plane, so a convex body — a sphere above all — built an essentially degenerate chain of depth O(polygons). Two 32k-triangle spheres overflowed the CLR stack **inside `Build`** (not `Invert`, as this file long assumed) and killed the process; measured threshold, a 3000-node chain built in 419 ms and a 4000-node chain crashed. Rewriting all five walks with explicit stacks fixed the crash and bought honesty rather than speed — the completed run returned an *open* shell in 74.9 s — which is what settled the decision to retire it.
- **A batch of refinement points must be INDEPENDENT, or the bound that makes refinement finite silently evaporates.** Delaunay refinement terminates because a tetrahedron's circumcentre is, by the empty-circumsphere property, at least its circumradius from every existing vertex — so points pack no denser than the local radius and a bounded domain holds boundedly many. That guarantee is against the triangulation *as it was when the circumcentre was computed*. Inserting a batch straight off the priority queue voids it: the second point has no relationship to the first, so two candidates land arbitrarily close, make a sliver, and feed the next round. Measured on a 4-unit box asked for 1.5-unit elements: 200-, 800- and 3000-point budgets all exhausted without converging; requiring each accepted point to be half a circumradius from the others accepted in the same pass converges to 836 elements identically at every budget. The same shape of error as seeding a priority queue off incomplete quadrics — a quantity used after the thing it depends on has moved.
- **A refinement rule that responds to a defect must be LOCAL, or the response is the next defect.** The boundary-recovery answer to a face that failed to lie on the surface was to split every sub-triangle of the PATCH it touched; on a box that quadruples a whole face per round, so one interior insertion near a wall cascaded the entire surface and exhausted a 500 000-point budget. Ruppert's encroachment ball — split exactly the sub-triangles whose diametral ball contains the offending vertex — is the smallest region that provably has to change. It also needs a size floor: without one, a circumcentre sitting essentially ON the surface encroaches forever, each split halving the balls while the point stays inside.
- **Seed a priority queue only after the quantity it is keyed on is COMPLETE.** Folding QEM seeding into the quadric-accumulation loop keys the whole initial queue off *partially accumulated* quadrics. Every mesh still came out closed, manifold and at exactly the requested face count — the sole symptom was a **2.4× worse approximation error at light decimation**, where the cheapest (earliest) choices dominate. A committed per-case error baseline is what caught it; without one it would have shipped invisibly.
- **Feature detection reads the mesh you give it, not the surface you meant.** A coarse tessellation of a smooth surface has large dihedrals — `UvSphere(12, 8)` facets meet at ~30°, so the remesher's default 30° feature angle pins much of it and refinement cannot help (splitting an edge leaves the halves coplanar with their parents, so the dihedral is unchanged). Pass 0, or a larger angle, when remeshing tessellated curvature; the default protects CAD creases, which is the more dangerous failure to get wrong.
- **Where the standard algorithm exists, take it over the reference library's heuristic.** g3's `MinimalHoleFill` is four iterative edge-flip passes whose own comments describe strong ordering effects, non-convergence, hard pass caps to stop oscillation, and a forced interior-vertex-removal stage with a debugger break in it. The Barequet–Sharir/Liepa dynamic program solves the same problem deterministically and globally optimally in O(n³)/O(n²) — trivial at realistic rim lengths. Its dihedral measure is `atan2(|a×b|, a·b)` on **raw** normals: exact at any magnitude, no normalization and no epsilon anywhere.
- **Degeneracy guards on areas and determinants must be RELATIVE, never absolute** (the BSP lesson, occurrences two through five — `SketchRegion`'s bounding-box reject compares SQUARED distances, so its conservativeness slack is relative too: `1e-15`, about 9 ulps against the bound's own ≤4 ulps of arithmetic error, the scale-free tier and never the weld tier; `MeshDecimator` was the third and fourth — `MeshDecimator` was the third and fourth: it normalized face normals with `TryNormalize(Tolerance.Default)`, an absolute 1e-9 applied to a *cross product* i.e. an area, so below ~1e-4 scale every face read as degenerate, contributed no quadric, and decimation became arbitrary — measured **91% volume error** on a 1e-5-scale sphere, now 3.1%; its normal-flip guard likewise compared a squared area against an absolute 1e-24. Both are now `1e-13 × extent`, the exact boolean's scale-free tier, and `Remesher` was written to that rule from the start — every threshold relative to target edge length, every degeneracy guard to its square, and guards reading the **sign** of the dot of two unnormalized cross products): `Sketch`'s enclosed-area, arc-chord and `ArcThrough` circumcenter guards were absolute 1e-12 and therefore wrong in *both* directions — a legitimate 1 µm × 0.5 µm pocket was rejected as enclosing no area, while a 1000 × 1e-10 sliver passed; three metre-scale points 1e-10 off a line built a radius-1.25e15 circle. They now scale by extent² / magnitude² (`Sketch.RelativeDegeneracy`), because an area is quadratic in scale. Deliberately still absolute, with reasons in code: sketch closure (the 1e-9 weld tier) and sweep angular guards (radians are dimensionless).
- **An exact predicate applied to a quantity that is round-off answers CONFIDENTLY and WRONG — and a tangent evaluated AT a tangency is exactly such a quantity.** The curved 2D arrangement orders the edges at a node by the exact `Orient2dSign` of their departure directions, which is right everywhere except where it matters most: at a tangency the two directions differ only by arithmetic noise. Measured, a disc tangent to a plate's straight edge from outside gave the arc a departure of **(−1.22e-16, −1)** — the x sign is nothing but the error in `sin(π)` — which put it on the wrong side of the plate's exactly vertical edge, so the tightest-turn walk closed **no face at all** and the union came back **empty**, not merely inaccurate. Only the CURVATURE carries information there, so a second pass re-orders each cyclic run of tangentially tied departures by it (cyclic because a tangency along +x puts one departure at the very start of the fan and its partner at the very end). **The tie band is DERIVED, not chosen**: a vertex may sit up to the snap tolerance from the true tangency point, and displacing a point by δ along a circle of radius r rotates its radial — hence its tangent — by δ/r = δ·|κ|, so the band is `snap·max(|κ₁|, |κ₂|)` plus a few-ulp arithmetic floor. That floor is all that remains for two straight edges, so genuinely distinct line directions stay decided exactly. Same shape as design.md §5's `DepartureAngle` note: **Shewchuk exactness is exactness about the coordinates you hand it.**
- **A closed curve's END must be its START, exactly — a parameterization that merely evaluates to the same place is not closed.** `sin(2π)` is not 0 in doubles, so a full-turn arc built as `centre + r·(cos θ, sin θ)` at θ₀ and θ₀ + 2π lands ~2e-16·r apart. That gap is not cosmetic: a +x parity ray whose ordinate falls INSIDE it counts the seam piece's two endpoints on opposite sides, and a point measurably inside a disc reads as outside — found by a disc ∩ rectangle that returned empty where the analytic answer was a 61.4-unit circular segment. Two rules fix it and both are load-bearing: a full turn's end point IS its start point (an exact substitution, not a snap), and an arc's first and last y-monotone piece take their ordinate from the **stored endpoints**, never re-derived from the angle — which is also what makes a chain's parity consistent across every joint, since the next edge starts at exactly this edge's stored end. The same shape of bug as the boolean's canonical-crossing rule: recomputation must be bit-reproducible, not merely close.
- **When a "boolean instability" and a "tessellation artifact" are both plausible, PROVE which by asking the mesh, not by tightening an epsilon.** The 2D boolean carried a filed defect for a long time — "leaves ~1e-7-area pinholes at near-tangency; a cell-classification fix, not an epsilon one" — repro'd by a 64×48 torus silhouette seen side-on. It is the opposite: 780 of 780 probe points inside that 1.45e-5 hole are covered by **zero** facets, tested with the exact `Orient2d` over every triangle, so the union returned is the correct union of what the boolean was given, and the *tessellated solid's own shadow* has the hole. In the band |z| ∈ [r·cos(π/n_minor), r] — the minor polygon's scallop, 4.28e-3 deep at n = 48 — the discrete tube only reaches that height near its minor-polygon VERTICES, and the major discretization breaks that thin band into lenses that need not overlap (the hole measured 1.16e-3 deep, a quarter of the scallop). It is not systematic either: holes at 64×48 and none at 32×24, 96×72, 128×96, 64×96 or 128×48, because whether two neighbouring lenses overlap is an *alignment* question, not a tolerance one. **A silhouette is the shadow of the mesh you give it, not of the surface you meant** — the remesher's feature-detection rule in new clothes. The test now asserts the strong form (every hole is uncovered by every facet), which is what separates "the boolean lost material" from "the tessellation has no material there".
- **The epsilon ladder** (audited repo-wide; every site is documented in place): new epsilons must pick a tier — **1e-9 absolute weld** (= `Tolerance.Default.Linear`: exactly-constructed shared geometry — tessellation welds, seam breaks, clone dedupe) / **1e-7 seam** (geometry built independently on two sides of one curve — `SealSeams`, boundary-vs-grid matching; `MeshWelder`'s T-junction zip expresses this tier **relatively**, as 1e-7 × the soup's extent, since every source of independent-construction error — a chord's sagitta, a float32-quantized STL's cracks — is proportional to the model, and an absolute form was wrong in both directions away from unit scale) / **1e-6 inverse-evaluation** (named: `FaceGeometry.InverseEvaluationTolerance` — Gauss–Newton pullback; three decades looser than weld, do not tighten; the 1e-7 seam tier is likewise named `FaceGeometry.SeamTolerance`, and `SurfaceIntersection`'s marching constants live together in `TracerSettings` because they must be tuned as a set) / **scale-free algorithmic guards** (relative machine-epsilon degeneracy tests, exact-zero division guards, exact-zero *semantic* tests — these deliberately bypass `Tolerance` and say so in a comment). Never invent a fresh literal. Shewchuk `Predicates2d` error-bound constants are the algorithm — off limits.

## Roadmap (bottom-up — do not skip ahead)

1. **Core math & spatial acceleration** ✅ done — `Tolerance`, `Vector2d`/`Vector3d` (implicit conversion from tuples), `Matrix4d` (column-vector convention), `Quaterniond`, `Aabb`, `Ray3d`, `Bvh` (static, median-split; the build sorts each node's item range through a **contiguous key array** rather than an `IComparer<int>` chasing scattered centroids, and **forks sibling subtrees onto the thread pool** above 4096 items, then replays the canonical sequential node numbering so scheduling is unobservable — the tree stays **bit-identical** to the original builder's, locked by `BvhBuildOrderTests`' fingerprints of the item permutation and every node's range/leaf-flag/bounds BITS. Measured 32 400 boxes 22.6 → 4.6 ms, 130 000 boxes 142.5 → 33.6 ms, and the exact mesh boolean's broad phase 46.8 → 10.0 ms. Quickselect instead of a full sort is 3.9× on its own but *repermutes items*, so it was rejected: `Query` appends in leaf-visit order, `Nearest` breaks ties by traversal order, and the imprint boolean interns seam points in `QueryOverlap` order, so "an equally good tree" silently moves downstream geometry. Serves box/ray/nearest plus t-ordered `QueryAll`, tree–tree `QueryOverlap` (the imprint-boolean broad phase), allocation-free struct-metric `Nearest<TMetric>` — `MeshSdf.Evaluate` measured 104 B/call → 0 — and exposes per-node contiguous ranges via `NodeView`/`ItemsInTreeOrder`, so future hierarchies need not fork the way `MeshWindingNumber` did), `Octree` (dynamic), **`Predicates2d`** (adaptive-exact Shewchuk `Orient2d`/`InCircle` — sign exact for ALL finite doubles, stackalloc expansions, locked against BigInteger ground truth incl. the Kettner ulp-grid where naive determinants provably lie. **Lesson on WHICH inputs are actually hostile**: a translated cross product is Sterbenz-exact when points cluster, so the Kettner grid does NOT break a monotone-chain hull — the family that does is near-collinear points spread over a wide *exponent* range, where the differences themselves round. Measured: the naive chain returns a non-convex hull on ~7% of such clouds, the predicate version on none. `ConvexHull2` routes its turn test through `Orient2dSign` for exactly this reason; its internal error-bound constants are the algorithm — never "clean them up"), **`Geometry2.Arrangement2d`/`ArrangementCell2d`** (2D segment arrangement: exact-incidence decisions via the predicates, rounded coordinates under a snap tolerance, tightest-turn cell extraction with CCW outers + CW holes — the sketch-engine robustness foundation; FaceSplitter's planar tracing is the same dance and could one day sit on it, deliberately not unified — boolean-critical), **`IndexPriorityQueue`** (O(1) id→slot, decrease-key; upgraded `MeshDecimator`'s lazy PQ — equal-or-better quality; lesson: never key dictionaries by packed `(min<<32)|max` longs — `long.GetHashCode` is `lo ^ hi`, collapsing structured edge keys into a handful of buckets, measured 4× whole-algorithm slowdown; tuple keys hash properly), **`ProgressCancel`** (optional-trailing-param cooperative cancellation, wired into Polygonize/Decimate/`BRepTessellator` and the whole exact mesh boolean — imprint, broad phase, and the per-patch winding probes, with **polling following cost rather than code structure**: linear passes get one checkpoint at their boundary, superlinear ones poll per unit of work), **`ParallelFor.Blocks`** (deterministic block-parallel-for — each index writes only its own slot; SurfaceNets sampling ~1.6× and dense GridSdf bake ~2.5–5× faster, output bit-for-bit identical), **`ConvexHull2`** (monotone chain) + **`Fitting2d`/`Fitting3d`** (`MinAreaBox`/`MinCircle` (Welzl)/`FitBox3` (PCA heuristic)/`FitPlane` → `Frame3d`; **`MinVolumeBox(hullVertices, hullTriangles)`** is the exact minimum-volume OBB, and the place this project learned that **the 2D rotating-calipers theorem does NOT lift to 3D**: the minimum-volume box need not have a face flush with a hull face — Freeman–Shapira, a great many implementations since, and until now this repo's own `FitBox` doc comment all assumed it does. The regular tetrahedron on alternate corners of [−1,1]³ fits its cube at volume **8** while EVERY face-flush candidate measures **16** (equilateral section's min rectangle a²√3/2 = 6.93 × height 4/√3 = 2.31). What does hold is **O'Rourke's** characterization — at least two ADJACENT box faces each contain a hull EDGE — so the search runs a one-parameter family per unordered hull-edge pair (48-step sweep + golden section on the best three brackets), with exact face-flush candidates and PCA/AABB seeds on top so it can never lose to `FitBox`. **The hull is the caller's to supply**: Core owns no polyhedron type and the 3D quickhull lives in EngrCAD.Mesh because it speaks `HalfEdgeMesh`, so passing the hull as plain data keeps the layering intact rather than dragging half-edge topology into the dependency-free foundation. O(E²·h): 3.6/22/122 ms for 18/42/78-vertex hulls), **`Distance3d`** (`ClosestPointOnTriangle` — Ericson's Voronoi-region form, six barycentric sign tests, no tolerance anywhere bar exact-zero division guards, plus an `out TriangleRegion` overload the plain one delegates to; it had been written TWICE, privately in Mesh's `MeshProjectionTarget` and again in Interop's `MeshSdf`, and the copies had already drifted — only one carried the degeneracy guards, so the other returned NaN on a collapsed triangle. The region matters to exactly one caller and cannot be reconstructed from the point: `MeshSdf`'s sign is the angle-weighted pseudonormal of whichever *feature* won), integer grid types (`Vector2i`/`Vector3i`/`Interval1i`/`AxisAlignedBox3i`; `DVector`/`MemoryPool` deliberately not adopted — List/ArrayPool cover the call sites), **`Frame3d`** (rigid pose: origin + orthonormal basis; `FromXY`/`FromNormal`/`FromZX`(STEP AXIS2)/`FromOrthonormal`, `ToWorld`/`ToLocal` for points/vectors/rays, `Then`/`Inverse` (transpose-exact), `ToMatrix`, `Renormalized`. **`FromNormal` delegates to `Vector3d.ArbitraryPerpendicular`** — the codebase's single arbitrary-perpendicular convention, locked by tests; the phase-alignment lesson is orthogonal: when a *natural* frame exists (a cylinder's own), use it, never an arbitrary one. Adopted by `SweptSurface.Frame`, `StepReader.Axis2`, sweep caps; `SketchPlane` wraps it; frame ctors on `PlaneSurface` + all conics; **`BrepQueries.Frame(face)` + `SketchPlane.On(face)` enable sketch-on-face** end-to-end through the Shape API)
2. **Mesh engine** ✅ done — half-edge structure (`HalfEdgeMesh` + `Vertex`/`HalfEdge`/`Face` handles for LINQ traversal), manifold-validating `Build`, boundary loops, metrics (area/volume/Euler), primitives (box, uv-sphere, n-gon-capped cylinder), triangulation, Loop subdivision, booleans — **the imprint boolean is now the ONLY one** (`MeshMeshCut` imprint + per-patch winding-number classification + exact-equality weld; scale-free relative guards, survives near-tangency). The BSP/csg.js clipper behind `BooleanMethod.Bsp` is **deleted** (`Csg.cs` and the enum both gone), retired once coincident surface was classified — the last thing it did that the imprint path could not: the comparison was one-sided in every dimension, so maintaining two algorithms had become a liability rather than a hedge. **Coincident (coplanar-overlapping) surface is classified by normal agreement**: agree → keep one copy for union/intersection and drop both for difference; oppose → drop both for union/intersection and keep A for difference — the surviving copy is always the *first* mesh's, and that asymmetry is deliberate since both meshes cover the region. What makes per-face centroid membership legal there: **the coincident region's rim is already imprinted by the ordinary transversal path** (a coplanar patch ends where the other solid's surface leaves the shared plane, which is a transverse pair and does produce a segment), and coincidence is also a patch-flood boundary, so a ½ can never leak into an ordinary patch's probe. The imprint path is **1.6–2.2× faster than before and beat BSP on every case measured** (spheres 7.4 ms vs 262 ms; a 32k+32k sphere union 0.71 s closed against BSP's 74.9 s for an *open* 347k-face shell). Two BSP failures it exposed remain locked by tests as hostile inputs worth pinning, now asserting only the imprint path's own correctness: a bore stopping 1e-9 from a wall (BSP returned an **open shell** — absolute plane epsilon), and 1e-5 scale (BSP returned **zero faces** — `Tolerance.Default`'s absolute 1e-9 applied to a *cross product*, an area threshold that fails quadratically with scale). Cross-checks that used to compare against BSP now assert **inclusion–exclusion identities** instead (|A∪B| = |A| + |B| − |A∩B|), which hold exactly for the polyhedra the boolean produces and need no second algorithm, QEM decimation (`MeshDecimator`: quadric edge collapse with link-condition and normal-flip guards, boundary preserved exactly; the topology layer is now **`EditableMesh.CollapseEdge`** — the port replaced a private indexed-face-set scratch state after a measured comparison committed as `MeshDecimatorQualityTests`' baseline: **bit-identical output** on twelve fixture/budget pairs, **0.84×** the time, and correct at 1e-5 scale where the old path lost **91%** of the volume), `RenderMesh` extraction, OBJ export, plane cutting (`MeshPlaneCut.Cut`: keeps the side the normal points away from, exact canonical-direction crossing points, Sutherland–Hodgman face clipping — non-convex crossing faces are Newell-plane triangulated before clipping, not fanned from vertex 0 — returns ordered boundary loops, optional earcut caps with collinear-chord zip), winding-number classification (`MeshWindingNumber`: exact Van Oosterom–Strackee solid-angle sum + Barill/Jacobson order-2 multipole `FastWindingNumber` over its own contiguous-range hierarchy — built before `Bvh` exposed per-node ranges; could migrate now, stays put with locked tests — robust inside/outside for non-watertight meshes); **EditableMesh** (mutable companion over free-list SoA — `HalfEdgeMesh` stays immutable-after-build STRUCTURALLY, not by discipline; guarded Euler operators SplitEdge/FlipEdge/CollapseEdge/PokeFace/MergeEdges where guards never partially mutate, refusal = untouched mesh + enum reason; `MeshChange`/`MeshChangeSet` = journaled slot-write undo records — the COMPLETE journal incl. free-list links and counters, so do→revert is bit-identical by construction and replay verifies each slot before writing; fuzz-tested with Validate after every random op; g3's per-element records rejected because they don't restore IDs); **readers + repair** (`StlReader`/`ObjReader`/`OffReader` + `MeshReader` facade → `MeshReadResult` carrying mesh-or-soup + diagnostics, never throwing on dirty geometry; `MeshRepair.Clean`: weld → degenerate → duplicate → per-component winding flood with outward vote → zip → Build; **`MergeCoincidentEdges`** (crack closing on an `EditableMesh` — spatial hash over boundary-edge midpoints, opposite-direction pairs only so a merge can never fold the surface, positions never move, and `MergeEdges`' guards refuse anything unsafe so the tolerance may exceed a safe vertex weld) and **`AutoRepair`** (Clean → crack weld if still open → `HoleFiller.FillAll`, reporting cracks merged / holes filled / holes skipped; `MeshReader.ReadAndRepair` exposes it opt-in since closing holes invents geometry). Note the ordering constraint: crack merging cannot sit among Clean's soup passes — no half-edge mesh exists yet — so it runs after Clean's `Build`, where a valid-but-open mesh first exists. Lessons: STL binary detection is the exact 84+50n size test BEFORE any "solid" sniffing — binary exporters write "solid" into headers; binary STL cracks below one float32 ulp cannot exist, so STL welding needs tolerances at/above float quantization; open-component orientation votes must probe BOTH sides of faces via solid angles — a single behind-the-normal probe is blind to inward sheets). **Exact-boolean lessons** (from the imprint rewrite, all institutional memory): a seam welds by *equality* only if every shared point is computed from a **canonical (edge, plane) pair** — derive the crossing from the edge's lower-indexed endpoint so both meshes and both adjacent faces produce bit-identical coordinates; recomputation must be bit-reproducible, not merely close (the boolean then welds via a `Dictionary<Vector3d,int>` on exact bits and the 1e-9 weld tier never enters). A crossing that coincides with an **existing vertex** must be reconciled to one coordinate **across both meshes** before either imprint runs, honoring any mapping the crossing already carries, or the seam silently stops welding. Inside/outside must be decided **per patch at the largest triangle's centroid**, never per face — seam slivers sit exactly where the winding number approaches ½ (the same lesson `BrepBoolean` learned about sagitta-safe probes). And degeneracy guards here are `1e-13 × operand extent` — the scale-free tier, never the absolute weld tier — which is what makes small models and near-tangent pairs work. **Two more traps, both found by the coincident-surface work, both the same defect in new clothes**: never call `Vector3d.TryNormalize(Tolerance.Default)` inside a scale-free algorithm — it applies the absolute 1e-9 to a **cross product**, i.e. an *area*, so mating faces stopped being recognized below ~1e-4 scale (measured: a non-manifold union at 1e-5 — the BSP defect reintroduced inside the very algorithm that exists to avoid it; read only the *sign* of the area-weighted normal instead). And **`Orient2d` returns twice an AREA**, so an epsilon compared against it must be scaled by the edge length — unscaled, coplanar triangles that merely *touch along a shared edge* (the commonest relation between neighbouring solids) report as overlapping. **Performance lesson, the same shape as the B-Rep boolean's**: the two biggest costs were an O(k²) scan of every fragment ever cut from a host face when locating interior seam points (96% of the imprint for a 64-sided bore), and **building an O(n log n) winding hierarchy to answer eight questions** — below ~32 queries the exact O(n) sum is the faster algorithm (`MeshWindingNumber.Direct`; two 32k-triangle spheres went 65 ms → 6 ms). Also: `HalfEdgeMesh.Triangulated()` was rebuilding meshes that were already triangles, revalidating everything per boolean stage in a cascade. **Region seams may now be REFINED** (`MeshRegionOperator`): a replacement may subdivide a seam edge, and reinsertion carries the split into the neighbours (base triangles re-fan from the corner between their two unrefined edges — exactly what an edge split leaves — and the split point is created ONCE and shared, since two vertices at one position *is* the crack), so `LoopSubdivision` now round-trips through a region operator as `MeshDecimator` already did. **The order of the two seam checks is the load-bearing part**: every original seam vertex must be shown present BEFORE any chain is walked, because otherwise a replacement that MOVED a rim vertex is indistinguishable from one that removed it and inserted a new one nearby — and would be accepted as a "refinement", welding a crack silently, which is precisely the failure the seam contract exists to prevent. `LoopSubdivision.Subdivide(mesh, n, preserveBoundary: true)` is the new pinned-rim mode that makes it usable (the default Warren boundary rule smooths the outline, i.e. moves it, and stays refused). **Isotropic remeshing** (`Remesher.Remesh` + `IProjectionTarget`/`MeshProjectionTarget`): g3's `Remesher`/`RemesherPro` rebuilt on `EditableMesh`'s guarded Euler operators — one pass is a per-edge collapse→flip→split sweep on a **modulo-prime stride** (index order makes a tessellated cylinder erode away edge by edge), then a double-buffered uniform/cotangent smoothing pass, then a projection pass; fully deterministic (fixed stride constant, no RNG, bit-identical positions across runs). **Constraints are expressed on VERTICES, not edges** — a deliberate departure from g3 forced by our topology: an undirected edge is named by the smaller of a twin pair and a collapse *merges* edge pairs, so the survivor gets a different canonical index and freed indices are recycled; an edge-keyed constraint table goes stale — worse, silently aliases a different edge — after the first collapse, while vertex indices never do because a collapse always removes the *unpinned* end. Everything g3 spells with edge flags then follows: two pinned ends means neither collapse nor flip (a flip destroys the edge), splitting stays legal and the midpoint inherits the pin (so a constrained chain keeps its geometry while gaining resolution), and boundary/crease pins are re-derived from geometry every pass so they need no bookkeeping at all. Split/collapse thresholds are **1.33 L / 0.66 L**, NOT Botsch's 4/3 and 4/5, which thrash — a fresh split lands below the collapse threshold. Measured: 90% of a coarse sphere's edges outside the length band → **0%** after 40 passes, 73 ms. **RemesherPro scheduling** (`RemeshOptions.Scheduling = Queue`, `FastSplitPasses`, both opt-in): the modified-edge/vertex queues and the split-only prepass, deterministic (FIFO, fixed stride, no RNG; queued ids are recycled so every pop re-validates AND re-canonicalizes, since a collapse merges edge pairs and the survivor is usually named by the other half). Measured on 12 096 faces → target 0.05 at 40 passes: sweep **777 ms**, queue **532** (1.46×), queue + 4 fast-split passes **331** (2.35×) — but **neither feature pays alone under sweep scheduling** (240 vs 243 ms at 12 passes for the queue; 239 vs 243 for fast split), because a queue with nothing quiet to skip is pure bookkeeping and a sweep visits everything however the density got there: they are complementary, since reaching the target density sooner is what makes regions go quiet sooner. Cost is a slightly less relaxed mesh (0.6% of edges out of band vs 0.2%) — a different answer, hence opt-in. **The wake-up threshold is 1e-2 × the target edge and the obvious 1e-3 makes the whole feature pointless**: damped smoothing still moves a *converged* vertex ~5e-3 L per pass, so every vertex re-woke its own ring, the active set stayed the entire mesh, and queue scheduling measured **slower** than the sweep it exists to beat (783 vs 747 ms) — a scheduler whose wake-up rule never fires is pure overhead. **Face-aligned (RZN-flow) reprojection** (`RemeshProjection.FaceAligned`): each TRIANGLE moves rigidly onto the target (centroid to closest point, plane rotated to the target normal there) and each vertex takes the `area × (n·n')³`-weighted average of where its incident triangles put it — **the cube of the normal agreement is the crease-preserving mechanism**, since a triangle straddling a crease agrees with neither side and contributes almost nothing, so a vertex ON a crease is pulled by the two flat groups either side onto their intersection, where plain closest-point projection would drag it onto whichever face is nearer. Measured on a 2×2×2 box, target 0.35, 12 passes, feature pinning OFF: volume **7.2244 → 7.6964** (corner erosion 9.7% → 3.8%). `IProjectionTarget` grew an oriented `Project(point, out normal)` whose **default reports a ZERO normal** — the exact-zero spelling for "unoriented", since no unit normal is ever zero — so old targets keep working and face-aligned degrades to vertex projection rather than failing; both targets here override it from work they already do. **`RegionRemesher`** remeshes ONE face selection in place (extract → remesh with the seam pinned → reinsert), so a bore wall can get 0.2 mm triangles while the plate is untouched; two settings are decided for the caller and both follow from the seam contract — `PreserveBoundary` is forced on, and a null `ProjectionTarget` defaults to one over the region *as extracted*, because "no projection" is the wrong default rather than the neutral one (smoothing is curvature flow, so the patch would sink and leave a dent inside an unmoved rim). Rim refinement rides on the region-operator work above; `FixedVertices` are base-mesh indices, mapped for the caller. `HoleFiller.FillSmoothed` stays a *different* operation — it builds a patch that does not exist yet, so its rim must be barred from splitting. **Hole-filling upper tiers**: `HoleFiller.FillMinimal` (minimum-weight triangulation of the rim's own vertices — the Barequet–Sharir/Liepa **dynamic program**, weight = (largest dihedral, area) lexicographic; refilling two removed adjacent box faces restores the box's *exact* volume, i.e. it puts the crease back rather than chording across it) and `FillSmoothed` (a relaxed membrane: coarse fan → remesh at the hole's own rim-edge length with the rim pinned → Laplacian relaxation, built and stitched as a standalone patch so the surrounding surface is untouched and the stitch welds by index, not by tolerance); `HoleFillOptions.Fallback` picks the tier `FillAll` uses for loops the planar and simple fills decline, and defaults to **`Minimal`**. It shipped as `None` on the reasoning that reporting a hole honestly beats inventing questionable geometry — which is right, and is exactly why `Minimal` is the better default: **it invents nothing**, since every vertex of a minimum-weight patch is already a vertex of the hole. The report-what-you-cannot-fill contract survives wherever the tier genuinely cannot decide (no admissible triangulation, or a rim past `MaxMinimalFillVertices`); what changed is that `MeshRepair.AutoRepair` now *closes* a long non-planar rim instead of naming it. `Fallback = None` restores the old behaviour exactly. **HoleFiller** (simple fan + planar nested-hole earcut on `FitPlane3` frames — orientation intrinsic from boundary walk order, ring-aware chord zip repairs earcut's collinear filtering; fills the annular caps `MeshPlaneCut` refuses); **MeshExtrude** (patch extrude + open-surface thicken — wall quads are exactly the directed edges freed by moving the patch, winding correct by construction); selections (immutable Grow/Contract) + connected components (per-component area/volume/closed + extraction); viewer renders meshes
3. **Implicit engine** ✅ done — `Sdf` AST (`Evaluate`/batch/`Normal`/conservative `Bounds`), primitives (sphere, box, cylinder, torus, capsule, half-space, gyroid lattice), operators (union/intersect/subtract with `|`/`&`/`-` overloads, smooth blends, offset, shell, translate/rotate/scale; **N-ary** `Sdf.Union`/`Intersection`/`SmoothUnion` as flat single-node loops — children evaluated once per query; the N-ary smooth union folds the pairwise polynomial smooth min, so it matches chained binary exactly and reduces to hard min outside the blend band; `Sdf.Blend(a, b, d, Falloff.Wyvill|Exponential)` falloff-kernel blend whose bump is bounded by the blend distance — Wyvill's compact support makes it *exactly* union outside the band; smooth-op bounds clamp their expansion at 0 for blend ≤ 0; **sampled grids**: `Sdf.Sampled(cellSize[, region][, lazy])` bakes any `Sdf` to a dense or lazy 16³-block trilinear grid through the batch `Evaluate` seam — exact at nodes, O(h²) between, outside the box = boundary value + distance-to-region — the standard acceleration for expensive ASTs like `MeshSdf`); `SurfaceNets.Polygonize` in Interop converts implicit→mesh (manifold variant: one vertex per inside-corner component per cell). **SIMD batch evaluation** (`BatchEvaluation.cs`): the batch entry deinterleaves AoS→SoA **once at the AST root** (pooled 1024-point chunks) and drives an internal `EvaluateBatch(x, y, z, distances)` seam that operators forward straight to their children, so the transpose is paid once per batch rather than per node; kernels use width-agnostic `Vector<double>` (one kernel serves NEON/AVX2/AVX-512). **Bit-for-bit identical to the scalar path** — each kernel mirrors its scalar form term-for-term in the same association order, the ragged tail falls back to scalar, and tests assert `DoubleToInt64Bits` equality over a 41-node catalogue at randomized *and* structured (on-surface/edge/corner) points and every batch length around the register and chunk boundaries; the one documented deviation is that `Vector.Min/Max` break a ±0 tie by operand position rather than by sign. The suite also passes under `DOTNET_EnableHWIntrinsic=0`, so the no-SIMD fallback is exercised, not assumed. Gyroid, exponential falloff and `ThreadSdf` stay scalar on purpose (no vector transcendental reproduces `Math.Sin`/`Math.Exp` bit-for-bit, and a silently divergent fast path is worse than none). Measured ~3.5× on CSG trees and ~3× on `Sampled` bakes — but honestly, a bare **sphere is 0.81×** (a five-flop kernel cannot pay for the transpose at 2 lanes; this inverts on 4/8-lane x64). **`Sdf.NarrowBand(cellSize[, region][, bandWidth])`**: evaluate the source only near its surface (octree culling, sound because distance is 1-Lipschitz and the field's magnitude is a lower bound on it) and fill the rest with a **two-scan chamfer transform** — one causal plus one anti-causal raster pass is the complete distance, no iteration to convergence, and no ray-parity signing is needed because an `Sdf` is sign-exact. 8–11× faster than a dense bake for `MeshSdf`, and honestly documented as **3–10× *slower* for cheap analytic fields** (the outward fill costs ~60 ns/sample and raster scans don't parallelize). Outside the band the sign stays exact but the magnitude over-estimates by up to ~13% (Borgefors' anisotropy bound; measured 1.112× worst case). Two perf lessons worth keeping: **the AoS→SoA transpose must be written against raw references** — four span bounds checks per point cost more than a cheap kernel saves, and going through indexers halved `Sdf.Box` throughput; and **JIT tiering makes single-warm-up benchmarks meaningless** (the same code measured 147, 314 and 548 Mpts/s across runs — use a wall-clock warm-up *budget*, not a warm-up count; confirmed a second time by the decimator port, which one warm-up call measured at 1.4× *slower* and a 1.5 s warm-up budget plus best-of-9 measured at 0.84× — same code, same machine, opposite conclusion). **Sparse lazy grids**: `Sampled(region, cell, lazy: true)`'s block table is **flat while that is free and grouped once it is not** — up to 64³ blocks (a 1024³ grid, 2 MB of pointers) it stays flat so existing models keep exactly the lookup they had (6.2–6.9 Mpts/s before and after); above it, blocks group into 16³-block super-blocks whose slot tables allocate on first touch, turning a 4096³ grid's 134 MB of *never-touched* pointers into 32 KB plus 32 KB per super-block visited. The `int`-addressing cap moved out of `GridFrame` into `RequireDenseAddressable` (dense bakers only) and its message names the lazy overload as the way out, so a domain the dense path cannot allocate at all now simply works: at a 0.0012 cell (11.7 G samples, 87 GB of doubles) the dense bake throws and the sparse grid answers in 80.6 MB; at 0.01 (20.3 M samples) dense is 18 816 ms/156.5 MB against 8 976 ms/28.9 MB sparse. Both levels publish lock-free by `CompareExchange` (block values are deterministic, so a racing fill just loses), and the two table shapes are held bit-for-bit equal against the dense bake via a `flatBlockLimit` test seam. g3's `DSparseGrid3`/`BiGrid3`/`HBitArray` were read and deliberately NOT ported — `BiGrid3` is an unfinished stub with no value API, `DSparseGrid3` hashes `Vector3i` into a plain `Dictionary` with no thread safety and bounds that never shrink, and `HBitArray` carries a bug its own author flagged (clearing a bit clears its parent summary bits unconditionally). **`Sdf` gains a deinterleaved batch entry** `Evaluate(x, y, z, distances)` for callers that generate sample positions procedurally; it drives the same `EvaluateBatch` seam with the same chunking, so it agrees with the interleaved overload bit for bit. **`SurfaceNets.Polygonize` no longer materializes the grid**: coordinates are generated from the indices into pooled x/y/z scratch and the grid is walked in a **sliding window of x-slabs** (64 MB budget) with cell-vertex construction and the three quad passes interleaved into the same walk, so peak sample memory is O(cross-section) rather than O(volume) — a 1024³ grid needs 16 MB where the dense array needed 8.6 GB, and below ~resolution 200 the window IS the grid so small models take the old path. Output is bit-for-bit unchanged **including face ordering**, because quads go into per-axis buckets keyed by whichever loop variable was outermost in the dense emission pass and are concatenated at the end (measured: res 96 **39.9 → 15.6 ms**, 40.9 → 19.9 MB; res 256 **735.5 → 258.8 ms**, 562 → 145 MB; res 384 **1922.7 → 747.5 ms**, 1842 → 289 MB — and all 47 rendered docs PNGs are byte-identical across the change). **Sketch fields are batched**: `IPlanarRegion` carries a defaulted batch overload `SignedDistance(x, y, distances)`; `ExtrudedRegionSdf` asks the region **once per distinct (x, y)** (a prism's field does not vary along z and bulk consumers sample z fastest — an exact memoization, the run test being an *identity* comparison, not a tolerance), and `SketchRegion` flattens its segments into structure-of-arrays form with lane-wise line/full-circle kernels transcribed term for term from `LineSeg.Distance`/`ArcSeg.Distance`, a conservative bounding-box reject in front of the bézier kernel (a cubic costs 17 curve evaluations plus 8 Newton steps), and a y-bucket index over the ray-parity pieces. Every one is a pure restructuring, locked by golden bit-hashes of the plain segment loop plus batch-vs-scalar bit equality (measured: a 14-segment plate **3.78 → 10.92 Mpts/s** scalar and **15.8 → 7.9 ms** to polygonize; a 108-segment engraving-shaped profile **0.18 → 0.81 Mpts/s** and **107.2 → 10.2 ms**). **Partial arcs and cubic béziers now have lane-wise kernels too, and neither could be a transcription** — which is the institutional-memory part. An arc's in-sweep test is `Math.Atan2`, with no bit-exact vector form; the replacement reads the signs of the cross products against the sweep's two boundary rays (`c₀ ≥ 0 && c₁ ≤ 0` up to a half turn, the same pair OR'd beyond it, because past π the *complement* is the narrow wedge; at exactly π the forms coincide, so the branch has no boundary of its own). Deciding the same predicate by different arithmetic means the two agree only away from the flip, so the kernel **refuses near it**: `c₀ = |o|·sin(δ)` and `c₁ = |o|·sin(δ − span)`, so requiring both to exceed `1e-9·|o|` puts the point a nanoradian off either boundary ray — five orders outside everything the scalar path can contribute (`Atan2`'s few ulps of a result bounded by π, plus the subtraction and the reduction by the *double* `2*PI`, both bounded because an arc is only classified vectorizable when |from| ≤ 64; the `%` itself is exact) — and any lane inside that **certainty band** sends its whole block back to `Atan2`. So the contract stays the strong one: **bit-identical for every input, not a bounded deviation**. A bounded deviation was available and simpler (the two branches of an arc's distance are continuous across the sweep boundary, so disagreeing there costs only O(r·ε)) and was rejected because this field's *sign* drives boolean classification kernel-wide. Note which inputs land in the band: a segment endpoint, shared bit-for-bit with its neighbour, sits exactly ON a boundary ray, so the cases that most want exactness take the exact path by construction. The bézier kernel's lesson is smaller and sharper: its Newton stage's `break` on a vanishing derivative is reproduced by **masking the WRITE** to the refined parameter with a sticky per-lane flag, NOT by letting stopped lanes iterate on — Newton from a stopped point is not a fixed point here, since a vanishing `g′` makes the step infinite and the clamp would walk the lane to 0 or 1. Its other half is structural: the 17 scan points are the same for every query, so they are baked at construction instead of re-evaluated per call. Third rule, from the same work: **a lane-wise kernel behind a conservative skip should substitute +∞ for the skipped lanes, not reuse the skip's proof** — blending +∞ before the min-fold is what "skip" means to a running minimum and costs one select, whereas "the reject proves they cannot lower the minimum anyway" makes the two paths' agreement rest on a second argument about the computed value's error. Measured on the batch entry (win-x64, `Vector<double>.Count` = 4, builds alternated within one sitting): a stadium **14.31 → 31.90 Mpts/s** (2.23×), an all-cubic 12-petal outline **0.41 → 0.56** (1.37×) and **10.6 → 9.3 ms** to polygonize, the plate **12.64 → 15.35**; nothing measured loses. Future: expression-tree→SDF compilation for the Query layer
4. **B-Rep engine** 🔶 modeling operations done — curves (`Line3d`, `Circle3d`, `Ellipse3d`, **`Parabola3d`/`Hyperbola3d`** (focal / cosh-sinh parameterizations completing the analytic conic family; parabola has closed-form arc length) and **`OffsetCurve3d`** (planar offset as first-class geometry, exact O′ = (1 − dκ)·C′ — |d| vs curvature radius deliberately unvalidated, as OCCT), with **`Curve3d` exposing virtual `DerivativeAt`/`SecondDerivativeAt`** overridden exactly by every analytic curve and wrapper; `NurbsCurve` with exact rational conics, **exact analytic derivatives** (`DerivativeAt`/`SecondDerivativeAt` via The NURBS Book A2.3 + the rational quotient rule; `TangentAt` overridden — the finite-difference note below now applies only to curves without an exact override) and **`NurbsCurve.InterpolatePoints`** (chord-length parameterization, natural-end open cubic via tridiagonal Thomas solve, or C2-seam closed periodic; exact pass-through), `ReversedCurve`/`TransformedCurve` wrappers with `Underlying` for sampling rules), surfaces (`PlaneSurface`, `CylinderSurface`, `SphereSurface`, `NurbsSurface`, `ExtrudedSurface`, `RevolvedSurface` with angle, `SweptSurface` with rotation-minimizing frames), topology (`BrepSolid`→…→`BrepVertex` with `Validate` + Euler–Poincaré incl. genus), `SolidFactory`: `MakeBox`/`MakeCylinder`/**`MakeCone`** (frustum or apex — the side is an exact `RevolvedSurface` of a slanted line generator, deliberately NOT a new surface type: the revolved-band machinery already provides projection/tessellation/intersection/wrap-split/STEP; rim circles built on the surface's own radial frame stay phase-aligned with u=0; a zero radius makes that end a pole like sphere poles) + **`Extrude` (incl. shear + hole profiles), `Revolve` (full or partial turn, holes on partial, pipe elbows from closed profiles), `Sweep` (RMF along open paths, holes)** over `Profile` (planar closed chain; winding auto-corrected). **Mesh engine additions**: `ConvexHull` (quickhull: extent-scaled epsilon for all visibility tests, coplanar points absorbed by consistent triangulation, degenerate inputs throw, output passes manifold-validating Build) and `HalfEdgeMesh.Transformed(Matrix4d)` (negative-determinant transforms flip winding so meshes stay outward-oriented). **`Shape.Cone`** (Native all three reps), **`Shape.Hull`** (mesh-construction: B-Rep-Impossible, implicit/mesh Bridged via quickhull over tessellated vertices — exact for polyhedral operands), **`Shape.Mirror(point, normal)`** (Householder reflection through the transform node; implicit-**exact** — the compiler decomposes improper similarities as m = (m·FlipZ)·FlipZ, lowers at identity, wraps `Sdf.Mirror`, places the proper remainder; mesh exact with winding flip; B-Rep Native for box/cylinder/extrude/sphere/torus/cone — mirrored cylinders keep exact `Circle3d` rims because FlipZ fixes the rim plane — and **mirrored revolve/sweep/rim/drill are Native too**: a reflection conjugates a rotation — F∘Rot(d,φ)∘F = Rot(−F·d,φ), the LH-thread identity — so a mirrored revolve is the same angular sweep about the NEGATED transformed axis; sweeps need no fix at all because rotation-minimizing transport is intrinsic and commutes with any isometry; rim surgery and drill tools commute with isometries, with selectors keeping their run-on-the-LOWERED-solid contract. Draft/Shell/RoundEdges/Loft stay rigid-proper-only, filed in todo). `BRepTessellator` handles planar (multi-loop, earcut) /cylinder/extruded/revolved/swept faces with edge-consistent grid sampling and seam zipping. Notes: default `Curve3d.TangentAt` uses 2nd-order one-sided differences at domain ends (sweep frames are sensitive to start-tangent error); `PolygonTriangulator` is a faithful mapbox-earcut port — its collinear filtering can merge exactly-collinear boundary runs, which the tessellator's zip pass repairs — with **one deliberate departure: a sliver ear is DEFERRED, not clipped, while any other ear remains**. Earcut filters *exactly* collinear vertices, but boundary samples of a straight edge that came through a boolean are collinear only to a few ulps, so a run of them survives and one is eventually clipped as a facet whose normal direction is decided by round-off. The fix is neither clipping nor removing (the middle sample belongs to no other triangle, so dropping the facet opens a T-junction — a guarded drop in `BRepTessellator` was built and measured to do nothing): **that vertex is a corner of a FAT triangle once its neighbours are consumed, so preferring any other ear reroutes the diagonal instead of eating the run.** Sliver-ness is a HEIGHT (twice the area over the longest edge) at 1e-13 × the polygon's extent — the scale-free tier, and the same measure the tessellation audit counts degenerate facets with, so the two cannot disagree about what a sliver is. The preference cannot change a sliver-free polygon's output and cannot fail (a sweep finding nothing but slivers takes them); `TessellationCorpusQualityTests.Corpus_EmitsNoDegenerateFacets` now pins **zero** across the corpus at every density, where it used to pin "at most one, and only from a planar face". **Surface–surface intersection done** (`SurfaceIntersection.Intersect(a, b, region)`): analytic curves for plane/plane (line), plane/cylinder (circle, ellipse, or parallel lines), plane/sphere, sphere/sphere, parallel cylinders; general marching tracer (grid seeding + BVH pairing + damped Gauss–Newton refinement + tangent-predictor/Newton-corrector stepping, periodic-aware, multi-branch, closed-loop detection) for all other pairs, returning `PolylineCurve3d` (exact at vertices, chordal between). New curves: `Ellipse3d`, `PolylineCurve3d`. **Trimming groundwork done**: `Surface.TryProjectPoint` (inverse evaluation — exact for plane/cylinder/sphere, Gauss–Newton otherwise), `FaceGeometry` (curve pullback into parameter space with periodic-u unwrapping, `LoopSignedArea`, point-in-face by upward-v ray parity with periodic segment compaction), `FaceSplitter.SplitByClosedCurve` (closed interior curves → face-with-hole + disk sharing one manifold edge; `createDisk:false` hands the edge's second use to another face). End-to-end drill rehearsal in `DrillTests`: box + intersection circles + cap splitting + bore-wall assembly → valid genus-1 solid with exact volume. **Open-curve face splitting done**: `TopologyEditor.SplitEdge` (splits an edge at a parameter, patching every using loop — neighbor faces stay consistent), `FaceSplitter.SplitByCurve` (full parameter-space arrangement: polyline crossing detection + 2×2 Newton refinement, boundary-edge splitting with endpoint-vertex reuse, interior curve segments as shared two-use edges via `CurveSegment`, sub-face tracing by smallest-clockwise-turn walking, CW loops assigned as holes to their smallest containing CCW loop). Verified through whole solids: single cut (exact volume), cross cut through a split-created vertex (4 quadrants), annular cap cut beside its hole (hole follows the correct side). **Trimmed-face tessellation + cut-through-hole booleans done**: `TrimmedFaceTessellator` (Interop) renders faces whose loops don't cover the surface's grid domain, routing in **four tiers**: a single-loop region that is a **band** — two boundary chains monotone in one parameter joined by a single rung at each end, the shape of every mitered rim-fillet band and every partial cylinder fragment — is **zipped** chain-to-chain (the chains are already paired, so the merge walk the periodic-band path uses IS the correct triangulation, minus the period closure); then band-with-holes seam unrolling — whose unrolled region is **slab-swept** (`ZipSlabs`/`SweepMonotone`): each hole is split at its extreme-u vertices into a lower and an upper u-monotone chain, cutting the band into u-monotone slabs triangulated by the textbook stack sweep, with cut halves shared verbatim between adjacent slabs so it is watertight by index with no invented vertices, and a global uv-area identity as the closing guard (any failure returns null and defers, so the new path can never be worse than the old); then periodic bands; and **only then** the exact-coordinate ear clipper (shortest-diagonal ears, ~1e-9 jitter blocking band, hole bridging) — plus surface-exact midpoint refinement (Steiner-by-refinement, no upfront classification). Routing is a two-sided 3D match of loop samples against the natural grid boundary (the welding invariant, so existing faces can't misroute). **Why the tier order is a numerical rule and not an optimization**: ear-clipping a band lets the shortest-diagonal rule eat the dense boundary chains, and three consecutive samples of a smooth boundary curve span a sliver whose normal is `T × K` — the curve's **binormal**, not the surface's. Since `T × K = k_g·N + k_n·(T × N)`, the sliver agrees with the surface only where the *geodesic* curvature dominates — and a miter ellipse meets the top of a fillet **tangent to the flat face**, where `k_g` passes through zero, so the sliver normal turns perpendicular to the surface's and **its sign is rounding noise**. Half faced inward: `Shape.Box(30, 20, 6).FilletEdges(2, topRim)` rendered a dark folded lens at every mitered corner (13 088 triangles, **808 inverted**, worst facet-vs-surface normal dot −0.22; now **280 triangles, none inverted**, worst 0.99994). Worse than the count, the ear-clipped path **did not converge at all** — refinement subdivided the clipper's long diagonals, the monotone-decrease rule cut those cascades unpredictably, and volumes wandered (3516.70 / 3517.03 / 3516.82 / 3516.84 / 3517.04 at curveSamples 24–192) around an analytic 3517.2274 they never reached, before refusing outright at 256. The strip path is **linear** in the sample count and converges **quadratically from inside** (−4.8e-5 at default quality). Its rung epsilon is the **1e-6 inverse-evaluation tier expressed RELATIVELY** (1e-6 × the loop's extent in that parameter) — u and v carry no model units, so an absolute epsilon there is meaningless; and note that anisotropic uv is a trap for any Euclidean heuristic (a mitered band is ~1.57×1.0 in uv while the surface is 3.14×30 in model units, so "shortest diagonal" in raw uv is not shortest on the surface — precisely why the clipper chose to eat the chains). **The silent grid fallback is gone**: a trimmed face the trimmed path cannot handle now THROWS, naming the surface type, where it sits, its loop shapes, both sample counts and the reason. A fallback is only legitimate when it computes the same thing more coarsely — the natural grid covers the whole parameter rectangle, so for a trimmed face it computed something else, welding into an open mesh with no complaint (the tessellation-side twin of `BrepBoolean.Verified`). **Never earcut pulled-back curved-face loops** — `PolygonTriangulator` filters exactly-collinear vertices and iso-parameter runs ARE exactly uv-collinear but not 3D-collinear: a dropped sample is an unzippable crack, and jittered earcut breeds zero-area folds that refine into non-manifold welds. `FaceSplitter` hardened for the boolean path: tolerant partial-curve pullback runs (extrapolated run-end seeds), crossing refinement by 3D curve–curve Gauss–Newton (projected-uv Newton fails near bounded domain edges), inclusive crossing seeds (cuts through split-created vertices), and **reversed-face splitting** (CW-aware tightest-turn tracing, `IsReversed` preserved — cascaded booleans on boolean output need this). `BrepBoolean`: both sides of a shared closed intersection curve take a mandatory seam break at `Domain.Start` (the wrap-split side anchors its seam vertex there), and probe points prefer the largest fragment triangle's centroid (sliver centroids sit within the SDF's sagitta of the other solid's curved surface — misclassification). End-to-end: drilled box minus a slot narrower than the bore → Validate-clean genus-3 solid, closed manifold tessellation, volume within 7e-4 of analytic. Two-ring band faces with hole loops tessellate properly (seam in the largest u-gap, unrolled + ear-clipped with hole bridging; tracer `PolylineCurve3d` edges are sampled at exact vertex parameters — uniform sampling lands on chords ~1e-4 off-surface); `Refine` terminates via a monotone-decrease enqueue rule with fail-safe fallback. Remaining trimmed-face gaps: pole-bands-with-holes and |winding|>1 fall back to grid, no Delaunay quality flips, and **tier 4 `TriangulateRegion` still ear-clips**, so it would hit the same forced fan on a non-wrapping region with an exactly uv-collinear boundary run — nothing exercises it today, so it was deliberately left alone. **Sphere-piercing booleans fixed** (was: silent wrong geometry when a sphere pierces box faces so every intersection curve is a closed circle interior to a face) — lessons: `BrepQueries.Bounds` must sample surface domains, not just edges (a hemisphere's equator-only edges hid the dome from the face-pair prefilter); tracer-clipped arcs can NEVER be refined against a boundary they stop short of — promote full-turn revolved spheres to exact analytic plane∩sphere circles (`TrySphereCarrier`, generator sampled, never `Underlying`); closed interior curves need mandatory seam breaks honored on BOTH sides (`SplitByInteriorClosedCurve` builds matching arc pairs); period-wrapping traced loops are band boundaries paired bottom-to-top by v, not area-classified holes. `Shape.Drill` validates up front (overlap/tangency per point pair; far-face coplanarity at B-Rep lowering via the `DrillShape` node) instead of failing deep in tessellation. **Cross-drill-through-bore booleans fixed** — the root cause was NOT the wrap-split but silent no-op pullback: tracer polylines sampled at uniform parameters sit step²/8r (~1e-4) off the carrier surface, past the 1e-6 inverse-evaluation tolerance, so `PullCurveRuns` returned zero runs and unsplit bands were whole-classified away. Lessons now baked in: **tracer polylines are on-surface only at their vertices — ALL pullback/parity/crossing sampling goes through one shared rule** (`FaceGeometry.ExactSampleParameters`: polylines → vertex parameters, `CurveSegment`-over-polyline → mapped base vertices, else uniform), never per-call-site patches — **and "one shared rule" only holds if every caller ASKS it**: two later sites restated the test instead and both got it wrong the same way, matching only a RAW `PolylineCurve3d`, so a `CurveSegment` wrapping one (exactly what the face splitter hands back after a cut) fell through to uniform sampling. `BRepTessellator.SampleEdge` put every interior sample a sagitta off the surface, and `TraceFaces`' tightest-turn probes read their departure direction 2% along an edge — mid-chord for a polyline — so `ProjectNear` returned a uv for a point the surface does not contain and the angle the walk sorts by was noise. Both now call the rule, which is public alongside an `IsPolylineBacked` predicate; that predicate deliberately does NOT test `Underlying`, since a segment's underlying curve is its base's and says nothing about the parameter mapping the rule turns on. Probes fall back to the FAR endpoint on a single-chord edge (an on-surface vertex, and a chord's exact direction); a **non-planar wrap cut cannot become a domain boundary** — `SplitBandByNonPlanarWrapCurve` sub-bands keep the original surface and rely on trimmed-face tessellation (the trim-the-surface rule applies only to constant-parameter cuts); multiple wrapping cuts on one band decide containment **per fragment by loop parity** (every fragment shares the full carrier, so every cut pulls back onto all of them). Equal-radius perpendicular cylinders (tangent bicylinder) are rejected loudly (overlapping v-ranges). **Automatic B-Rep booleans done** (`BrepBoolean.Union/Intersection/Difference` in Interop): per-face-pair `SurfaceIntersection`, seam-aligned splitting (each side takes the other's crossing params as mandatory breaks so tessellation welds), fragment classification by probing the other solid's `MeshSdf` (hybrid trick), reversed faces for subtracted tools (`BrepFace.IsReversed`, tessellator flips), wrap-splitting of periodic bands (`SplitBandByWrapCurve`: bore wall → two exactly-reconstructed sub-bands), and circle-extrusion→cylinder promotion for exact bore circles. Boolean output is **topologically sealed** (`TopologyEditor.SealSeams`: stale-use pruning, vertex unification, seam-edge merging; Difference re-winds reversed faces' loops) — results pass `Validate()` and Euler–Poincaré with correct genus. **Loft / draft / shelling** (wired into the `Shape` API — `Shape.Loft`/`LoftAlong`/`Draft`/`Shell(t, openings)`/`RoundEdges`, see the Modeling status — with `docs/examples/loft-draft-shell.md`; `Draft.Apply` also takes per-face angles in one call — corners solve once from the two FINAL planes, bit-equal to the chained spelling — and `Shelling.Shell` a per-face wall thickness, both exact because every inner corner was already a three-plane solve): `SolidFactory.Loft(sections, Smooth|Ruled)` skins a solid through profiles via `LoftedSurface`, P(u,v) = Σ αₖ(v)·Cₖ(uₖ) where α is the **cardinal basis of B-spline interpolation, inverted ONCE at construction** — that is the load-bearing decision, because a chord-length reparameterization recomputed per u gives every strip its own v mapping and cracks every shared rail; αₖ(v_j) = δⱼₖ holds by exact equality, so the v=0/v=1 grid rows reproduce the end sections bit-for-bit. Mismatched segment counts are rejected (no degree elevation yet); alignment minimizes **centroid-relative** squared corner travel — *the lesson*: leaving the sections' separation in that objective makes it a large constant plus a tiny quadratic well, which cost eight digits and left 3e-8 of residual twist, past the weld tolerance. `Draft.Apply` rotates each selected face's plane about its **neutral line** by exactly the angle (n′ = n·cosθ + p̂⊥·sinθ, corners by three-plane Cramer solves), so it is closed-form, composable (twice by θ/2 == once by θ to 1e-12) and still emits selectable `PlaneSurface` faces; curved faces, holed caps and profile-folding tapers are rejected loudly. `Shelling.Offset/Shell` is the B-Rep counterpart of the SDF-only `Shape.Shell`, exact for polyhedra (an offset plane is a plane; offset vertices are three-plane intersections), carrying topology verbatim so a plate's outline shrinks while its bore *grows*; no opening ⇒ sealed void as a second shell, two openings ⇒ genus-1 tube. **Curved faces work too, and are equally exact** — the corner machinery that blocked them is `SurfaceOffset` + `SurfaceCorner` + `ImplicitSurface` + `CarrierBody`, built ONCE for three consumers (curved shelling, curved draft, variable-radius fillet bands). `SurfaceOffset` keeps every carrier in its own family (plane→plane, cylinder→cylinder, cone→cone, torus→torus, since a revolve's offset is the revolve of its OFFSET GENERATOR) with frames, spans and sweep angles carried over verbatim so u = 0 does not move; `SurfaceCorner` solves the corner as the root of a small square system — three planes by Cramer, anything with a closed-form implicit distance by Newton, machine precision either way, residual always reported. **The step is MINIMUM-NORM, and that one choice deletes every special case**: the carriers routinely do not pin a point (a seam vertex has two incident faces because a closed rim starts and ends there; a tangent junction has three faces of which two are the SAME carrier — a pipe elbow's profile circle split into two arcs offsets to two halves of ONE circle, a sphere's hemispheres to one sphere), and confining the step to the span of the carriers' normals reproduces every rule that would otherwise be hand-written (a cylinder's seam stays in its seam half-plane, an elbow's junction keeps its angle on the offset profile circle, a sphere's equator moves radially — each of which was written down as a synthetic constraint plane first, before the general rule replaced all of them). A corner CURVE is where the exactness brand bites: analytic pairs give a conic trimmed to its corners, everything else is the chordal tracer, so `CornerPolicy.ExactOnly` is the default and refuses by name while `AllowTraced` is opt-in and LABELS the result with its measured deviation — and even then the curve's ENDS are replaced by the solved corner points, so the chord error stays strictly interior and never reaches a vertex. Two construction rules the consumers paid for: **a curved rim is CONSTRUCTED and then VERIFIED, never intersected and trusted** (a concentric circle about the original's own axis and phase lands on the offset carriers' grids by construction, while the same circle recovered from an intersection is silently chordal wherever the analytic tier does not reach — every sample is measured against BOTH carriers, which is exactly what refuses a SEALED pipe elbow, whose moved cap planes cut the offset torus in a quartic rather than a circle); and **a domain-driven carrier must be re-trimmed to its new loops** (an inward offset moves a cone's rims axially as well as radially, so the inner band kept sampling its old extent and the trimmed tessellator refused the face outright — the generator parameter for that trim is solved against the loop points' own axial coordinate on the exact carrier, never by projection, the vCut lesson). **Two fits broke on CLOSED generators, the commonest input there is**: a circle extruded into a cylinder returns to its start, so a first-to-last chord is zero and a (first, middle, last) circumcircle is degenerate — both now take well-separated samples, and `CircularArc` fits over the EDGE's domain rather than the curve's, because a partial revolve's rail is a whole `Circle3d` used over a quarter turn. Still refused: carriers with no same-family offset (swept, NURBS), non-circular curved edges, and >3-valent vertices (over-determined — the offset surfaces do not meet in a point and it needs corner PATCHES, not a better solve). **`Draft` rides the same rebuild**: a face of revolution about the pull direction tapers by rotating its GENERATOR in its own axial half-plane about the point where that generator crosses the neutral plane — the same "rotate about the neutral line" rule one dimension down — so a drafted cylinder is EXACTLY a cone and a drafted torus band another torus band; the rotation SENSE is chosen by measurement (build both candidates, keep the one leaning further toward the pull direction) rather than derived, because deriving it means re-proving three sign conventions that have each cost this kernel real geometry for an answer one dot product settles. **Filleting**: `Filleting.FilletEdge` for closed circular rims (planar cap ↔ coaxial cylinder band) via exact quarter-torus `RevolvedSurface`; general fillet chains with corner patches are future work. **Biarc fitting + a 2D curve family** (`BiArcFit`/`BiArc2d`/`BiArcChain2d/3d`, `Curve2d` with `Line2d`/`Arc2d`/`BezierCurve2d`/`NurbsCurve2d`, public `BSplineBasis`, `ArcLengthTable2d`): two tangent-continuous arcs through a point+tangent pair, tolerance-driven chains through a polyline, and a 3D wrapper turning a **planar** traced polyline into exact `Line3d` + rational-arc `NurbsCurve` pieces. **Adoption is opt-in and the deviation is always reported** — nothing in the kernel fits biarcs implicitly, `MaxDeviation` is the max distance from an *input sample* to the fit (3D combines in-plane error and out-of-plane flattening in quadrature), and non-planar input is refused rather than silently flattened. Two lessons: the free parameter must be computed as **`d = |v|²/(√disc + v·t)`**, algebraically identical to `(√disc − v·t)/denom` but accurate as the tangents become parallel *and reducing exactly to the equal-tangent case at denom = 0* — which deletes the reference implementation's epsilon branch on the SQUARED quantity `|t1+t2|² ≈ 4` and the broken semicircle case behind it (g3's assigns its first arc twice and never sets the second); and the second arc is built **backwards from the end point and reversed**, so both data points and both end tangents are exact and all round-off lands on the interior joint, where nothing has to weld. The 2D family diverges from `Curve3d` deliberately: derivatives are **abstract** (every 2D curve here is analytic — no finite-difference fallback for a new type to inherit by accident) and `Arc2d`'s sweep is **signed**, so orientation is intrinsic instead of a flag plus a reverse-and-hope repair. `BSplineBasis` (A2.1/A2.2/A2.3) is now one public copy shared by `NurbsCurve`, `NurbsCurve2d`, `NurbsSurface` and `LoftedSurface`. **`SweptSurface.TryProjectPoint` completes the 1D-projection family — by a different structural fact.** The RMF frame varies along the path so no parameter has a closed form, but every surface point at path parameter v lies in the frame's own plane there, so **`f(v) = (p − Path(v))·Tangent(v) = 0` is a scalar equation in v alone** (the foot-of-perpendicular condition), and u then follows from a 2D offset match: two decoupled 1D solves. f is not monotone on a curving path, so roots are **bracketed** by a 16-sample scan and refined by safeguarded bisection — the bracket guarantees convergence, not the seed. Measured **3.9×** (curved profile) and **5.2×** (full-circle tube) per projection. **The correctness win was larger than the speed win, and is the lesson worth keeping**: a sweep's *effective* profile is the generator **projected into the start-frame plane**, which can be a sliver whose two branches sit closer together than one seed interval; the sampled distance then shows ONE broad minimum straddling both, single-seed 1D Newton descends to whichever branch is nearer, and the projection silently returns the **mirrored** parameter — a point tens of millimetres away that still passes every structural check. Refining from every local minimum **and its two neighbours** (purely combinatorial, no epsilon) took round-trip acceptance 266/400 → 400/400 on a swept tube. `ExtrudedSurface`/`RevolvedSurface` since gained the same multi-seed rule on their 1D scans, and now also DEFER to the (multi-seeded) base grid on failure, so the overrides are never worse than the base by construction. **B-Rep booleans consume their inputs, so `Shape.From(brepSolid)` clones at the source boundary** (`BrepSolid.Clone()`: topology deep-copied, geometry shared — curves and surfaces are immutable, only topology mutates). **The failure is conditional, which is why it hid**: a tool whose curves stay interior to the body's faces (a plain through bore) only adds hole loops to freshly-built cap faces and survives two lowerings even unfixed, while a tool that crosses the body's edges (a notch, a corner bite) makes the second lowering throw `Hole loop is not contained in any sub-face` or produce an unclosed solid — and counts survive either way, so the damaged solid still *looks* intact. A `DrillShape` also lowers its body **once** now (the expansion's difference chain is peeled so the coplanarity validation and the subtraction share one lowering, with a fallback if the chain shape ever changes): measured **1.77×** on a 6+2-hole plate, the saving equal to exactly one body lowering. Holes are cross-validated against earlier `Drill` calls **on the same placement plane**; different planes are deliberately not compared, since opposing bores need a tool-vs-tool solid intersection rather than a 2D centre-distance test. **Mass properties** (OCCT `BRepGProp`): `MeshMassProperties.Compute(mesh, density)` in EngrCAD.Mesh is **exact for any polyhedron** (closed-form tetrahedral moments per facet, Newell area so concave n-gons are right where a fan-area sum is not), returning a `MassProperties` readonly struct that carries the *volume-weighted* second moment about the centroid S = ∫(r−c)(r−c)ᵀ dV and derives everything else (`Inertia` = ρ(tr S·Id − S), `InertiaAbout` by parallel axis, `Principal()` = ascending moments + a right-handed `Frame3d` at the centre of mass, `WithDensity`, `Transformed` — similarity only, since shear and non-uniform scale change the surface area in a way the properties alone cannot express, so it refuses rather than returning a wrong area — and `Combine`, which handles mixed densities and reports the bulk density). Storing S rather than I is the load-bearing choice: S transforms as a clean congruence and adds under the parallel-axis theorem, so every derived operation is two lines instead of four special cases. `BrepMassProperties.Compute` in Interop takes the B-Rep route by **tessellate-then-sum, deliberately**: exact-as-an-identity for planar-faced solids and O(h²) inscribed convergence for curved ones, with **Richardson extrapolation on by default** — measured relative volume error at the default 64 segments/circle drops 1.6e-3 → **1.9e-7** (cylinder), 2.2e-3 → 4.8e-7 (sphere), 2.0e-3 → 3.7e-7 (torus) and 1.1e-4 → **1.4e-8** for a drilled plate, i.e. through a boolean whose bore wall is a trimmed face. Exact-surface quadrature was rejected on purpose: it needs the trimmed parameter domain scanned against trimming curves that are themselves only approximate for tracer edges, so it would hide the same error behind a bigger integral. `Part.MassProperties(density)` / `PartInstance.MassProperties` / `instances.MassProperties(densityPerPart)` ride the cached `Part.TryGetSolid()` lowering rather than lowering again. **Shape healing** (OCCT `ShapeFix`): `ShapeHealing.Heal(solid, options)` repairs imported topology — the case being third-party STEP, since `StepReader` shares topology by entity identity (right for files this kernel wrote) while foreign writers emit each face's wire separately, so a solid arrives as a face soup with duplicated edges and merely-coincident vertices. Six switchable passes: vertex merging, small-edge collapse, degenerate-face removal, edge sewing, opt-in straight-edge refitting, wire re-ordering/re-sensing. **Every repair is a return value, not a log line** (`ShapeHealingReport`: per-pass counts, `NonManifoldEdgesBefore/After`, `OpenLoopsBefore/After`, `IsManifold`, `Notes`), matching the `MeshRepair`/`StepReadResult.Diagnostics` convention; `Analyze` is a dry run of the identical pipeline, and healing never modifies its input. `RefitStraightEdges` is OFF by default because it is the one pass that moves geometry, and boolean output whose vertices were already unified at the 1e-7 seam tier must not have its exactly-constructed lines dragged off their surfaces; curved gaps are *reported* rather than invented. **STEP export + import**: `StepWriter.Write/WriteFile` (AP214 `MANIFOLD_SOLID_BREP`, analytic surfaces/curves incl. rational NURBS, wrapper-curve simplification; swept surfaces not exportable) and `StepReader.Read/ReadFile` (full Part 21 parser + AP214 entity mapping back to `BrepSolid`; round-trips everything the writer emits — topology shared by entity identity, edge domains reconstructed exactly from vertex positions (closed-form conic phases, Newton with exact derivatives for B-splines), `SURFACE_OF_REVOLUTION` angle/generator-trims recovered from rails/rims by **bisection on the exact (radius, axial) profile residuals** (root solves — distance minimization stalls at √ε ≈ 1e-8, past weld tolerance); mm assumed, unknown entities skipped with diagnostics; verified on box/cylinder/drilled-plate genus-2/revolves/booleans/fillets at ~1e-12 volume agreement; known-lossy: `TransformedCurve(NurbsCurve)` exports as sampled degree-1 polylines). **Viewer picking**: click-select via unprojected ray + per-object triangle BVH + Möller–Trumbore, selection highlighted and named in the title bar (note: Avalonia's pointer stack ignores legacy synthetic `mouse_event` clicks — BUT **`SendInput` works**: synthetic clicks and drags injected via `SendInput` (absolute normalized coordinates, window activated first) DO reach the pointer stack, so interactive viewer features can be driven and screenshotted end-to-end rather than only exercised by calling `Pick`/`Select` directly). **Band-crossing booleans on whole-solid fillets landed, and the fix was one sentence longer than the filed diagnosis.** The tracer's "stops one march step short of a bounded domain" property is not merely an accuracy loss: where that domain edge also bounds the face being split, the curve crosses NOTHING, face splitting finds zero crossings, the face is whole-classified and the result cracks along the entire boundary (measured on `FilletAllEdges(20×14×8, r2) − Ø6 cylinder`: the four band curves ended 5.5e-5 and 1.1e-2 from the two tangency lines, zero crossings). `BrepBoolean.SnapTracerEnds` extends each traced polyline onto the EXACT solution of E(t) = S(u,v) — the boundary edge against the other solid's carrier, a 3×3 Newton seeded from the polyline's own last vertex (already on S, so only t moves) — **once, on the single curve object both faces share**, which is what makes the two solids agree; snapping per face during splitting would leave endpoints a sagitta apart and open a pinhole at every crossing. **That alone was not enough, and the second half is the third occurrence of a lesson already recorded twice**: `FaceSplitter.SplitByCurve`'s "is this stretch inside the face?" probe took the ARITHMETIC midpoint of the interior stretch, which on a polyline is a mid-chord point — 5e-3 off the surface on a fillet band, five thousand times the inverse-evaluation tolerance — so `TryProjectPoint` failed, the stretch was silently discarded as "leaves the surface entirely", and the face came back unsplit. Every site that samples a curve must ASK `FaceGeometry.ExactSampleParameters`; restating the test is how all three got it wrong. Verified: a Ø6 bore through two fillet bands and Ø4 cross-drills along Y and X are Validate-clean, closed, satisfy |A|−|A−B| = |A∩B| to 0.02–0.06% of the removed volume, and converge with density; **and a family nobody was aiming at came with it** — a bore swallowing a rounded rectangle's corner and breaking out through both adjacent walls now converges QUADRATICALLY onto its analytic volume (1.0e-4/2.7e-5/6.8e-6/1.7e-6 at 32/64/128/256). `BooleanFailureTests`' unclosable fixture had to move a SECOND time (to a shaft coincident with its own bore), and `Verified`'s unclosed-solid backstop is now unreachable from every `Shape`-level construction tried — the `TrimmedFaceRefusalTests` pattern again. Residual, filed: a tool drilled ALONG a band's own axis still refuses, and a baked tracer polyline keeps a fixed sample count while the grid around it refines, so facet-vs-surface agreement on those bands DEGRADES with density (0.999 at 32 → 0.90 at 192; no folds, volumes still converge). **Coincident (flush) PLANAR booleans landed** (`CoplanarFaces.cs` in Interop): flush embossing, stacked plates, butted blocks and a pocket floor flush with the host's own face all fuse into ONE solid with exact volumes, following the mesh boolean's model verbatim so the two engines cannot disagree — the shared region's rim is imprinted by the neighbours' ordinary transversal curves and the coincident fragments are classified by **normal agreement**, since a fragment lying on the other solid's boundary reads distance ~0 from its SDF (the B-Rep twin of the winding number being ½ there). Agreeing normals ⇒ the surface bounds union and intersection and vanishes from the difference; opposing ⇒ union and intersection bury it and only the difference keeps it; **exactly one copy survives and it is always the FIRST solid's**, the same deliberate asymmetry (see design.md §5). Coincident CURVED surface stays refused BY NAME before any splitting (`SurveyCoincidence` recognizes coaxial equal-radius cylinders and names the axis and radius). **Three rules the cases taught, all gated on a shared plane existing so transversal booleans are bit-for-bit unaffected**: a curve that never reaches a face's INTERIOR must not split it (when solids mate, each neighbour face's own boundary IS an intersection curve, and splitting a face along its own boundary is what the arrangement tracer cannot close); a face pair whose bounds meet in a single POINT is dropped (butting a boss against a plate puts two side walls corner to corner, and their carriers still cross in a full line — the line is real, the contact is not); and **a shared plane disqualifies the disjoint fast path** (two stacked plates of one footprint intersect only along their own boundary edges, so after the first rule they look disjoint and came back as two touching shells, which is the exact fusion failure the tier exists to fix). Two more findings worth keeping: **coplanar overlap must be decided by sampling the shared AREA, not by probing centroids** — two plates overlapping in a strip have neither centroid inside the other, so the first version missed the case that most needs it; and **the rim needs a second source**, because a sketch extrusion's wall is a bounded patch and `TryPlaneExtrudedSection` deliberately reports no section when the plane is flush with the generator's rim, which is exactly embossed text — so a coplanar face also takes its partner's OWN boundary curves as rim curves (skipped where an existing curve covers them), which is also the best weld available since the new edges ride geometry the other solid already references. `BooleanFailureTests`' unclosable fixture had to MOVE (the coplanar cross now fuses exactly); it is a bore swallowing a rounded corner and breaking out through both adjacent walls. Remaining future work: tangent/coincident CURVED boolean cases, fillet gaps that remain after the corner work (variable-RADIUS fillets, cliff/vertex-blend run terminations, arc-terminal runs; arc-rim sharp corners are a documented POLICY refusal — see design.md — and band-crossing booleans on whole-solid fillets carry a precise diagnosis in todo.md), NURBS surface export. **STEP follow-ups landed**: length units are resolved and SCALED on import (the authoritative unit is the one the GLOBAL_UNIT_ASSIGNED_CONTEXT references — an imperial file's CONVERSION_BASED_UNIT('INCH', 25.4 mm) chain necessarily *contains* a metric base-unit entity that is not the file's unit, so a bare scan of length-unit entities is wrong by construction); CONICAL_SURFACE/TOROIDAL_SURFACE synthesize onto `RevolvedSurface` (cone = slanted-line generator over the face's boundary-vertex axial range, apex snapped EXACTLY onto the axis so the pole machinery sees a pole and not a hair-thin rim; torus = the minor circle in the frame's x–z plane), which surfaced the missing **closed-generator two-rim disambiguation**: a split torus's two bands see the SAME two junction circles and min/max hands both faces the same half — the rim coedge senses (with the rim circle's own axis alignment folded in) say which rim is the generator's start, the same outward-band convention the single-rim pole case reads; `TransformedCurve(NurbsCurve)` exports EXACTLY by transforming control points (a rational curve is an affine combination of its control points at every parameter, so weights and knots are untouched for any affine map) — NURBS-profile extrusions now round-trip at reconstruction accuracy; PARABOLA/HYPERBOLA/OFFSET_CURVE_3D map both ways with conic edge trims in CLOSED FORM (the parabola's parameter IS the local y coordinate; asinh of the B-component for the hyperbola), plus `Parabola3d.ToNurbs()` (the one polynomial conic: exact quadratic Bézier). **Healing completion**: `RetrimCurvedEdges` (FixGaps' parametric mode — LOCAL Gauss–Newton foot parameters, break-before-negligible-step for idempotence; the perpendicular residual is reported, never repaired) and `RepairShells` (ShapeFix_Shell: sense-consistency flood — flipping a face re-winds loops AND toggles IsReversed together — plus a global outward vote from boundary-loop fan volumes against containment parity, honestly AMBIGUOUS for pole-bounded/closed-band components like spheres, and shell repartition to one shell per connected component, all bit-stable no-ops on well-formed input). **Inverse-evaluation seed selection**: the base 17×17 grid now refines from every LOCAL minimum and its neighbours (global-best seed FIRST, so previously-succeeding calls are bit-identical; a folded NURBS hairpin measured 80/205 round-trip failures single-seeded, 0 multi-seeded), and the Extruded/Revolved 1D overrides defer to the base grid on failure so "the override is never worse than the base" holds by construction — the fixture lesson worth keeping: a MIRROR-SYMMETRIC hostile fixture can be secretly benign, because every wrong-branch seed then has a same-branch mirror at the identical tangential offset and the argmin can never land on the wrong branch; the hairpin only bites with asymmetric branch parameterizations. **Inverse evaluation on a swept surface is a ONE-dimensional solve — this was ~100% of the B-Rep boolean's cost.** `ExtrudedSurface`/`RevolvedSurface` override `TryProjectPoint`: an extrusion's `v` is fixed by the direction component, so only `Q(C(u) − p)` constrains `u`; a revolve's `u` is the azimuth in closed form once `v` matches the generator's (radius, axial) profile. Both scan the *generator alone* at the base class's u resolution and refine with 1D Gauss–Newton on the exact `DerivativeAt`. The generic base scans a **17×17 (u,v) grid that re-evaluates the same generator point once per column — 289 curve evaluations where 17 carry all the information** — and since inverse evaluation is the inner loop of every face pullback, it dominated everything: 7-glyph engraving **65.3 s → 4.0 s**, a 5-hole drilled plate **14.4 s → 1.7 s**, the Interop test suite **4m16s → 22 s**, DocsGen **106 s → 41 s**, with volumes bit-identical and not one of the 37 rendered docs PNGs changing a byte. Profiling first was essential: the two *plausible* culprits (an over-conservative face-pair prefilter, and face-splitting/`SealSeams`) were both innocent — only 113 of 894 pairs survive the prefilter and all 113 intersections resolve in ~1 ms. **When a surface is a sweep, never inverse-evaluate it in 2D.** (`SweptSurface`/`NurbsSurface` still use the base grid, legitimately — an RMF frame varies along the path.) `Scene.PreMesh` and `AmbientOcclusion.Prime` also prime parts **in parallel** now (`ParallelFor.Blocks`; parts are independent by construction and the first failure *in scene order* is rethrown with its original stack, never a scheduling-dependent `AggregateException`). **Sketch-extrusion booleans (pockets, slots, engraving) are exact** — the fix for a silent open-mesh bug, and its lessons are load-bearing: an extrusion of a straight generator IS a plane, but a **bounded** one, so the analytic line must be clipped to the generator's parallelogram, not merely to the query region (`TryPlanarPatch`/`PlanarPatches`, straightness decided by sampling the actual generator at the weld tier — `Underlying` is only a type hint). Better still, when the generator's plane is **parallel to the cutting plane** the section is exactly the generator *translated* by `direction·v` (`TryPlaneExtrudedSection`): exact for ANY generator shape (lines, slot arcs, glyph Béziers), and — the load-bearing property — its endpoints come from the generator's own points, so consecutive profile segments hand over their shared corner **bit-for-bit** and the outline closes. Note the precondition is NOT "plane ⊥ direction", which is why sheared tools are exact too. **The marching tracer breaks its step AFTER `Correct` leaves the domain, so it always stops up to one march step short of a bounded generator's ends — never use it where a curve must terminate exactly on a boundary** (measured: cuts missing corners by 0.015–0.284 mm, so the outline never closed). Worse, when truncation left no usable curves at all, the disjoint fast path buried the tool as an internal cavity: a closed, `Validate`-clean solid with the *wrong* volume. `BrepBoolean.Verified` now enforces two-manifold output and throws `BrepBooleanException` naming the operation, counting unpaired edges, locating a crack, and suggesting the implicit route — but it catches **unclosed, not wrong-but-closed**, so end-to-end boolean tests must assert analytic volumes. Numerical notes from the Modeling work: the generic `Surface.TryProjectPoint` wraps (not clamps) periodic u so Newton can cross the seam; plane⊥cylinder intersection circles are **phase-aligned to the cylinder's frame** (arbitrary-perpendicular frames caused tessellation cracks on rotated bores — and `DrillTests` winding depends on this); `BrepBoolean.ProbePoint` routes full-period-wrapping loops to the band path (projection jitter gives degenerate sliver loops nonzero area, and the planar path would probe on the fragment boundary where the SDF is 0); `SurfaceIntersection.Promote` sanity-checks that the generator lies on the candidate cylinder (a `TransformedCurve`-wrapped circle's `Underlying` is the untransformed circle). **"Lies on the carrier" is not the same claim as "IS the carrier", and the gap fabricates surface.** Promotion also has to check the generator sweeps a WHOLE turn: a rounded rectangle's corner is a QUARTER arc extruded, every point of which lies on the full cylinder, so a start-point-only guard promoted it and the kernel then reported intersections around 270° of surface the face does not carry. The tracer duly traced those, and the resulting open curves ended strictly inside a *tool* face — so the failure surfaced three faces away, as `Open splitting curves must start and end outside the face` on the tool, for a boolean that is a plain hole. The filed diagnosis blamed the parallel-cylinder analytic path and the face-bounds prefilter; both were innocent, and the prefilter could not have helped anyway (the two AABBs touch at a corner, and it is deliberately conservative-over). The blast radius was also wider than the report: a THROUGH counterbore anywhere in a rounded-rect plate failed, dead centre included. The guard is now geometric — every sample on the cylinder AND the accumulated swept angle a full turn — which as a bonus lets a quarter-arc corner section against a parallel plane as the exact translated ARC instead of a whole invented circle. **A sketch extrusion's SIDE WALL is a plane, and until it was treated as one a bore drilled into it could not converge.** `TryPlanarPatch` already recognized the bounded parallelogram, but only the plane-vs-plane pair used it, so a wall meeting a quadric fell to the marching tracer and the rim came back as a fixed ~57-sample polyline where the identical bore on a flat cap got a `Circle3d`. A fixed sample count is a FLOOR no tessellation density can lower, which is the tell: the error did not merely stay large, it stopped converging and changed sign as the analytic n-gon reference crossed it — −7.4e-4 / −5.3e-5 / +4.7e-5 / +6.5e-5 at 32/64/128/256, now 7.1e-14 / 6.8e-14 / 4.3e-14 / −5.3e-14. `TryPatchQuadric` mirrors the main switch's analytic cases for a BOUNDED patch (the case that actually fires is revolve-⊥-axis, not cylinder: drill tools are axis-touching revolves, and `PlaneRevolved` keeps the generator's bounded axial extent so no circle is invented above a blind bore's end), and containment is decided **closed-form, not by sampling** — the patch's (s, t) are affine in the point, so on a conic each runs centre ± hypot(a, b). A conic poking out of the wall, and the axis-parallel line pair, DEFER to the tracer: clipping to the rim would produce arcs whose ends must weld to a face boundary, which is separate work rather than a silent side effect. **A left-hand thread is not a right-hand thread on some other frame** — every right-handed frame is a rotation of every other, so handedness cannot enter through the pose and has to enter the arithmetic; the near-miss that looks like it works is flipping two axes, which is a half-turn, not a reflection. **A grid quad's diagonal is chosen by GEOMETRY** (`PolygonFan` — shorter 3D diagonal, shared by `Triangulated`/`SignedVolume`/mass properties/`RenderMesh`/the writers). It used to be chosen by CORNER ORDER, which made a mirrored sheared band carry a systematically 3× larger volume deficit than its twin off the IDENTICAL vertex set (measured 0 of 131 200 differ at 1e-9); the two now measure identically. The tie guard is relative (1e-12 on squared lengths), because a UV-sphere quad's diagonals are mathematically equal and an exact comparison hands 408 of 960 splits to round-off. **A `CylinderSurface` band tessellates from its RING LOOPS, not from a parameter grid**, which inverts the trim-the-surface rule: wrap-splitting one keeps the whole cylinder on both sub-bands (shortening the loops IS the edit) where an extruded or revolved band must have its surface shortened too, its vCut is one exact dot product with no projection error to refine away, and — being unbounded in v — its "the cut coincides with a boundary ring" test must read the rings, since against an infinite domain it could never fire. **Generalizing a refusal can make failure WORSE**: letting the non-planar cylinder wrap-cut through split successfully and moved the failure three stages downstream, where it surfaced as `Directed edge appears twice` from the mesh builder; it refuses by name instead, pointing at the extruded-circle route that works.
5. **Interop completion** — remaining conversions, mesh↔SDF, robustness passes
6. **Simulation** 🔶 meshing done — **`EngrCAD.Fea`**: `TetMesher.Mesh(closedSurface)` fills any representation's surface mesh with tetrahedra. Pipeline: exact-predicate Bowyer–Watson Delaunay of the surface vertices → classification by winding number at each element's centroid → **boundary recovery** (refine until every inside/outside separating face lies on the input surface, verified and named when it does not) → optional Delaunay refinement to a radius-edge target and/or a `Func<Vector3d, double>` sizing field (an `Sdf` composes directly). Outputs `TetMesh` (SoA, every element positively oriented — checked EXACTLY, so `Volume` is a sum of positive terms with no cancellation), per-element region ids from multi-body input, and boundary facets each naming the input triangle they subdivide (`TetFacet.SourceTriangle` — the seam for boundary conditions and B-Rep face identity). `QuadraticTetMesh.From` is the 10-node layer as a PURE function of the linear mesh (mid-edge nodes keyed on the canonical `(min, max)` corner pair so elements sharing an edge share the node; corner indices unchanged so corner data transfers with no mapping; Abaqus C3D10 / VTK ordering). `TetQuality` reports minimum dihedral BESIDE radius-edge because bounding radius-edge provably cannot exclude slivers, plus histograms and `SliverCount`. **Three design decisions, each reached by a failure** (design.md §3b): recovery works per planar PATCH not per input triangle (a Delaunay triangulation picks its own diagonal across a coplanar quad, so demanding the input triangle never converges — a unit cube exhausted a 500 000-point budget); **classification comes BEFORE the boundary** (an exactly-coplanar quad makes the tetrahedralization contain a FLAT tetrahedron whose four faces are both diagonals at once, so area coverage reads exactly 2.0000× and refines forever — 40 of 72 patches on a 12×6 UV sphere; deriving the boundary from an independently-decided classification has no tie to break, and it took the suite from 95 s to 3 s); and refinement follows **Ruppert's rule in Ruppert's ORDER** (an encroaching circumcentre is never inserted, the sub-triangle is split instead — inserting first and repairing after cascades). Measured (win-x64, Release): `box 20³` at size 2.0/1.2/0.8 → 40 593/170 921/234 335 elements at 80 522/59 346/31 539 tets/s, mean-min dihedral 44.4/46.2/40.8°, radius-edge ≤ 1.82/1.88/2.33, slivers 1.0/0.7/1.6%; volume residuals 1.7e-15 … 1.1e-13 throughout and **zero recovery rounds on every well-formed surface**. **Refinement is not a quality option on curved bodies, it is what makes the mesh exist**: a sphere's tessellation vertices are ALL exactly cospherical, so a tetrahedralization with no interior vertices is slivers by construction (48×24 sphere: mean-min dihedral 5.5° and 85.9% slivers conforming-only, 39.8° and 4.8% with refinement). **The v1 limitation is stated rather than hidden**: recovery takes CAD tessellations (zero rounds on every fixture) but NOT irregular remeshed surfaces — near-uniform vertex spacing with no structure leaves enough triangles failing to be Delaunay faces that red subdivision cannot clear them, so a remeshed cylinder and sphere are both REFUSED by name (`RecoveryLimitationTests` pins it). Note the irony, which is the useful part: remeshing is the natural surface-quality prep and v1 recovery wants exactly the structure it removes — mesh the tessellation directly and control size with a sizing field. Docs: `docs/examples/fea-meshing.md`.

The Query layer and Viewer grow alongside each engine as it lands, not as separate phases.

## Conventions

- C# `LangVersion` latest, `Nullable` enabled, `ImplicitUsings` enabled, file-scoped namespaces
- Root namespace `EngrCAD.*`, matching project names
- Tests: xUnit; every geometric algorithm gets tolerance-aware assertions
- Central build props (`Directory.Build.props`) for shared settings once scaffolded
