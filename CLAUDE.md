# EngrCAD

**Detailed design rationale lives in [design.md](design.md)** (architecture, conventions,
algorithm choices, numerical lessons, roadmap); each `src/*` project also has a
`README.md` describing its contents. **[todo.md](todo.md)** is the idea backlog
(open work only — completed items are removed as they land; largely harvested from a
survey of geometry3Sharp at `C:\Users\chris\projects\git\geometry3Sharp` — study the
referenced g3 classes before implementing an item).

**Documentation upkeep is part of every change, not an afterthought.** When a feature
lands, the SAME integration pass must update: (1) the owning project's `README.md`;
(2) this file's current-status paragraph and roadmap (including any numerical lessons
learned — they are this project's institutional memory); (3) `design.md` when a design
decision or algorithm rationale was made; (4) `todo.md` (remove the done item, file
residual follow-ups as new open items); and (5) **the docs site** (`docs/`) for
user-facing features — an example page with a ` ```csharp render:<id> ` fence per
`docs/writing-examples.md`, verified with `dotnet run --project tools/EngrCAD.DocsGen
-- docs` (snippets are compiled/executed/rendered, so stale examples fail the build).
XML doc comments on new public API feed the generated API reference. A feature
without docs is not done.

A CAD application in modern .NET built around a **hybrid geometry kernel** that natively supports three representations:

- **B-Rep** — parametric surfaces (planes, conics, NURBS) wrapped in topology, for precision modeling
- **Implicit** — signed distance fields (SDF) as an AST of primitives and operators, for lattices and organic blends
- **Mesh** — discrete half-edge triangle meshes, for rendering, FEA, and 3D printing

A distinctive design goal is **LINQ-native geometry querying**: a custom `IQueryable` provider inspects expression trees and routes spatial predicates to spatial indexes (BVH/octree) instead of linear scans.

## Current status

Phases 1–4 plus Query v1 implemented (~794 tests): `EngrCAD.Core` (math + spatial incl. `Bvh.Nearest`), `EngrCAD.Mesh` (half-edge engine: booleans with seam zipping, Loop subdivision, QEM decimation), `EngrCAD.Implicit` (SDF AST), `EngrCAD.BRep` (curves/surfaces/topology foundations), `EngrCAD.Interop` (the full conversion triangle: implicit→mesh via manifold Surface Nets, B-Rep→mesh via tessellation, mesh→implicit via `MeshSdf` with angle-weighted pseudonormals — plus an opt-in `MeshSignSource.WindingNumber` sign source that accepts open meshes, default unchanged), `EngrCAD.Query` (BVH-backed IQueryable), **`EngrCAD.Modeling` (the unified `Shape` API: model once with one vocabulary — primitives incl. `MakeSphere`/`MakeTorus`, extrude/revolve/sweep, booleans, smooth blends/offset/shell/lattice, transforms — then decide the representation at the end: `ToBrep()`/`ToImplicit()`/`ToMesh()`; lowering bakes accumulated transforms into construction inputs for exact B-Reps, bridges non-native nodes through tessellation + `MeshSdf` or Surface Nets, and `Explain(target)` reports each node as Native/Bridged/Impossible — see `src/EngrCAD.Modeling/README.md` for the support matrix; `Shape.From(...)` wraps raw engine geometry so designs can drop down to any engine API and come back; **sketching**: `Sketch` (fluent builder — `LineTo`/`ArcTo`/`ArcThrough`/`BezierTo`/`QuadraticTo` — plus Rectangle/RoundedRectangle/Circle/Polygon/Slot primitives, `WithHole`, exact `Area()`) placed by `SketchPlane` and consumed by `Shape.Extrude/Revolve/Sweep`; lines/arcs→exact NURBS profiles for B-Rep, and the sketch's exact 2D signed distance (`SketchRegion`: per-segment exact distances + even–odd parity over precomputed y-monotone pieces) makes sketch extrusions and full revolutions implicit-**Native** via `Sdf.ExtrudedRegion`/`RevolvedRegion` (`IPlanarRegion` in EngrCAD.Implicit); axis-touching full-turn revolves (vases) work in **all** representations — `RevolveFullTurn` drops on-axis stretches (they revolve to nothing) and treats their endpoints as poles with no junction edge, splitting pole-to-pole generators at their midpoint so every face keeps a boundary loop (the sphere case); full-turn+holes/single-closed-curve stay B-Rep-Impossible and partial axis-touching revolves are rejected at construction; **hole feature**: `Shape.Drill(HoleSpec, points, depth, plane)` — simple/counterbore/countersink tools are axis-touching revolved sketches subtracted per point (exact everywhere; the tool overshoots the surface so booleans never see coplanar faces, and the countersink cone continues its slope so the surface diameter stays exact), with `StandardHoles` supplying metric ISO 273 clearance / DIN 974 cbore / ISO 10642 csk / coarse tap pilots / Tappex Trisert pilots (Trisert table flagged verify-against-datasheet). Kernel work that enabled it: analytic plane⊥`RevolvedSurface` intersection circles (multi-crossing generator bisection, phase-aligned to u=0), `SplitBandByWrapCurve` generalized to full-turn `RevolvedSurface` bands (generator `CurveSegment` split; single-loop pole bands allowed) with **vCut refined geometrically** (projection error ~1e-7 shifts cone rings radially past weld tolerance — solve the generator's axial coordinate against the exact cut curve instead), and `ProbePoint` probing pole-bounded single-loop faces halfway to the pole without the upward-ray parity check (which cannot see a rim below the probe). Note: CSG difference SDFs are correct-sign lower bounds near the subtracted tool's fictitious faces — don't assert exact distances there in tests. **B-Rep queries** (`BrepQueries` in BRep): `IsPlanar` (incl. extruded-line sides)/`IsCylindrical`/`IsLinear`/`IsCircular`/`Length`/`Bounds`/`IsConvex` + adjacency (`face.Edges()`, `solid.FacesOf(edge)`) + `PlanarFacesWithNormal` — the selection vocabulary for **rim features**: `Shape.Chamfer(setback, faceSelector)`/`Fillet(radius, faceSelector)` → `Filleting.ChamferRim/FilletRim` topology surgery (chamfer: straight rims miter with planar trapezoid strips + exact cone bands on circular rims; fillet: tangent-continuous line+arc rims via quarter-cylinder/torus-segment bands sharing junction arcs; sharp-corner fillets throw with guidance). Surgery gotchas learned: derive rim circles from edge samples (`ActualCircle` — `Underlying` circles of translated extrusion tops sit at the base), build all new rim edges in the top face's traversal direction (fixes every sense), TRIM domain-driven neighbor surfaces (extruded/revolved grids ignore loops — shortened bands must get shortened surfaces), and don't double-flip arc spans already measured from traversal-ordered points. **Patterns**: `PatternLinear/PatternCircular` (balanced union trees) + `BrepBoolean` disjoint fast path (no curves → whole-body classification, multi-shell unions, clone-reversed swallowed Difference tools — in-place `ReverseFace` would duplicate edge uses) + face-bounds pre-filtering and identical-carrier curve dedupe (patterned faces sharing planes produced the same split curve per pair, breaking the tracer). **STL export**: `StlWriter` (binary, merged multi-part with transforms) + `--export .stl`. **Parametric features** (`Features.cs`/`FeatureHistory.cs`/`StandardFeatures.cs`): FeatureScript-in-C# — `Feature` classes with `[Param]` properties (reflection metadata cached per type; init-only setters writable via reflection for JSON overrides), `FeatureContext` (Body + lazily lowered B-Rep for selectors + `TopPlane`), `FeatureHistory.Regenerate` (prefix caching keyed by instance-identity+parameter snapshot — a fresh instance always re-runs, which safely covers non-`[Param]` inputs like sketches/hole specs/selectors; validation-first; failure keeps the last good prefix and skips the rest; suppression; `SaveParameters`/`LoadParameters` JSON). Selector-based references are the topological-naming story (semantic queries re-run per regeneration). Rim-feature numeric lessons from the feature work: arc corner offsets must be computed exactly from the arc's radial (finite-difference tangents carry ~1e-9 angular error that rotates band generators past weld tolerance at scale — only *some* corners crack, whichever exceed 1e-9), and rim features validate that interior loops (holes) stay clear of the shrunk boundary.). **Threads**: `StandardThreads`/`ThreadSpec` (ISO 68-1 basic profile with exact formulas — H = √3/2·P, d2 = d − 3H/4, d1 = d − 5H/4; ISO 261/262 coarse M2–M12; tap drills consistent with the `Tapped` chart), `Helix3d` (BRep: exact analytic derivatives, closed-form arc length, `Frame3d`-based), `Sdf.Thread` (`ThreadSdf`: solid = 2D trapezoid profile membership in (r, wrap(z − P·θ/2π)) — **sign exact everywhere**, magnitude = exact 2D profile distance scaled by cos(lead angle at root) — lower-bound-style near the surface; clearance = exact profile-normal offset; 45° start/end chamfer cones), `Shape.ExternalThread`/`ThreadedHole(spec, points, depth, plane, clearance)` (implicit-**Native**; mesh via Surface Nets for chamfered/clearance forms — the 3D-printing route with flank-normal clearance, typical FDM 0.1–0.25 mm; **B-Rep-Native for the unmodified profile** — zero clearance, no chamfers — via `HelicalSurface` (co-rotating linear-generator sweep; u NOT periodic since z advances, so the whole wrap-split machinery doesn't apply) + `SpiralArc3d` (exact cap-plane cuts) + `SolidFactory.MakeThreadedRod`: **the entire lateral boundary is ONE boolean-free helical sweep** (root flats are part of the sweep — no coaxial tangent seam ever exists); threaded holes subtract ONE clipped-profile tool (void = max(basic form, r_pilot)) so the only intersecting pairs are helical-band×plane = exact spiral arcs, chain-split via `SplitByClosedCurveChain`; any length works (phase washes out — V = L·(2π/P)·∫½R² ds, Pappus per pitch); chamfered/clearance/mirrored threads stay B-Rep-Impossible with per-cause Explain messages (chamfer cones and distance-field offsets have no exact B-Rep counterpart; mirrored = left-handed). Helical lessons: the sheared grid makes cap cuts the grid's first/last columns (no trimmed tessellation needed; boundary points verbatim from shared edge polylines); angular sampling must epsilon-guard Ceiling (ulp-different equal spans must not round to different segment counts); downstream booleans may cut modeled threads only with axis-perpendicular planes (helical∩cylinder/tilted-plane fall to the tracer with no trimmed-helical tessellation — fails loudly); helical surfaces not STEP-exportable (swept bucket)). **Assemblies v1**: `Assembly` = named `Occurrence` list (each references a shared `Part` OR nested `Assembly` + a mutable `Frame3d` pose; DAG with cycle rejection at Add; names auto-suffix per level, `/` reserved for paths); **`PartInstance(Part, World, Path)` flattening is the seam** (`Assembly.Flatten()`/`Tab.Instances()`/`Scene.AllInstances`, World = frame chain × part transform, paths like "stack/clamp.2/bolt") — viewers/exporters only see instances; `Tab` holds assemblies beside loose parts; `Scene.AllParts` dedupes by reference so `PreMesh` meshes each distinct part ONCE (N instances share one mesh + GPU buffer set + pick BVH, drawn per-instance with composed matrices; `ViewportControl.SetInstances` is the host API, `SetParts` wraps it); viewer tree shows the hierarchy indented-flat with subtree visibility (effective = own AND ancestors) and occurrence-path selection; OBJ/STL/`--render` flatten instances (STEP still exports distinct parts un-posed). Mates/exploded/BOM deferred; GL-touching test classes must join the xUnit `"offscreen-gl"` collection (parallel EGL pbuffer creation is flaky). Modeling also owns the document model: `Part` (name + any-engine geometry + color + transform; `GetMesh(quality)` produces/caches the display mesh, `Scene.PreMesh()` runs it off the render thread) grouped into named `Tab`s in a `Scene` (`scene.Add(part)` shorthand targets a default "Model" tab; part names unique per tab; `Part` stays a leaf)**. **The Viewer is a library, not an app**: design code builds a `Scene` and calls `EngrCad.Show(scene)` (blocking; one call per process — Avalonia lifetime constraint; `onViewportReady` callback + thread-safe `ViewportControl.SetParts` support custom hosts; the internal `SceneHost` renders the tab strip with per-tab cameras — auto-framed on first visit, remembered after, kept in place on live reload). Rendering is an OpenGL viewport (Avalonia `OpenGlControlBase` + Silk.NET over ANGLE/GLES3) with a laptop-friendly orbit camera (drag orbit, shift+drag pan, ctrl+drag/scroll zoom, keyboard fallbacks) and click-picking that reports part *names*. **CAD chrome (dark theme)**: toolbar (Fit, Front/Top/Right/Iso, perspective/orthographic toggle — ortho frustum sized to keep the target plane's apparent size), model tree (visibility checkboxes, two-way selection sync via `ViewportControl.SelectionChanged`/`Select`/`SetVisible`/`Frame`), properties panel (kind/faces/closed/volume/area/size), status bar, gradient background, adaptive 1-2-5 ground grid + RGB axes, and a feature-edge overlay over polygon-offset fills — **sourced from the ACTUAL B-Rep edges for B-Rep-backed parts** (`Part.GetFeatureEdges`, cached and primed by `Scene.PreMesh`; `BrepFeatureEdges` in Interop samples the exact edge curves at display resolution ≥ 96 segments/circle via the tessellator's `SampleEdge` rules, so bore rims stay smooth circles at any tessellation; sharpness decided by adjacent-face outward normals at three interior probes — periodic-face seams and equal-carrier wrap-split junctions omitted, unprobeable edges kept: draw rather than hide; note a Shape part's B-Rep lowers a second time for edges — the mesh route doesn't retain its solid, a known cost flagged in todo), with `MeshFeatureEdges` (EngrCAD.Mesh: boundary + sharp-dihedral) as the fallback for SDF/mesh parts. **Per-part display modes** (`Part.DisplayMode`: Shaded/Wireframe/Translucent — wireframe via `WireframeEdges` over the line program, translucent alpha-blended with per-part back-to-front ordering and opaque silhouette edges; a per-tree-row cycler drives it), a **global view style** (`ViewportControl.ViewStyle` toolbar dropdown: Points/Wireframe/Shaded/ShadedWithEdges — one precedence rule in `RenderModes.Resolve` shared by both render passes: explicitly non-default part modes override the global style, default-Shaded parts follow it; Points = vertex point sprites via a dedicated point program, desktop GL needs ProgramPointSize enabled; Shaded = fills without the feature-edge overlay), and a **`Capture` screenshot button** (`ViewportControl.SaveScreenshot` → `glReadPixels` in the render pass → dependency-free `PngWriter`, path reported in the status bar). **Section planes**: a Section toolbar toggle clips everything beyond an **axis-aligned plane — X, Y, or Z** (toolbar axis cycler; `[`/`]` keys nudge 2% of the scene extent along the active axis; defaults to mid-bounds along that axis, and changing the axis re-centers) — implemented as fragment-shader `discard` (`dot(world, uSectionAxis) > uSectionOffset`) in the mesh, line, and point programs (edges/wireframes clip consistently with fills; grid/axes are scene furniture and stay unclipped), with exposed interiors detected via `gl_FrontFacing` and shaded as a flat darker cut material (axis-agnostic); custom hosts drive `ViewportControl.SectionEnabled`/`SectionAxis`/`SectionOffset` (`SectionHeight` kept as a delegating legacy alias); picking ignores the section plane in v1. **View cube** (top-right): stroke-font-labeled orientation widget mirroring the orbit camera (own ortho mini-projection, depth cleared, drawn last so it appears in `SaveScreenshot` captures but is excluded from offscreen renders by design); face/edge/corner clicks animate a 250 ms smoothstep shortest-yaw-path move to the orthogonal/45°/iso poses (`ViewCubeMath.PoseFor`, locked to the toolbar values; `ViewportControl.ViewCubeClick` for direct testing — synthetic mouse never reaches Avalonia); dragging on the cube orbits; all logic in `ViewCube.cs`. **Hover highlights**: pointer-move re-picks via the existing BVH raycast throttled by 4-DIP travel (`HoverThrottle`); one shader knob does both states — `uHighlight` 1.0 = selection gold, 0.35 = hover tint (wireframe/point parts blend line color instead); cube-region hover brightens the face set under the cursor and suppresses model hover; hover inherits picking's ignore-the-section-plane v1 behavior. **SDF isolines on the cut** (automatic when available): parts whose geometry is an `Sdf` — or a `Shape` with an implicit lowering, lowered once and cached per part — get iso-distance contours overlaid on the section plane (gold d=0 = exact surface cross-section, cool positive/warm negative families at ±k·spacing, 1-2-5 spacing from the parts' bounds — wall thickness at a glance, blend/offset debugging); extraction is `SdfContours.OnPlane` in Interop (UI-free marching squares over one batch-`Evaluate` planar grid, arbitrary plane, crossing error O(h²·curvature), shared cell-edge endpoints bit-identical so loops chain by exact equality), viewer side is `SectionContours`/`SectionContourRenderer` (plane passed as clip rule `(axis, offset)` — plane-general ahead of the section-axis generalization; recompute only on height/scene/visibility change, never per frame; lines lifted 1% of spacing below the plane so the shader discard can't eat them, depth-tested like feature edges); offscreen section renders draw the same isolines through the same renderer (one-shot, staleness caching moot; the axis-participates-in-the-build contract is pixel-locked by `OffscreenIsolineTests` — X vs Y sections at equal offsets must differ). Hard-won viewer lesson: **GLSL source strings must stay pure ASCII** — an em dash in a shader comment made ANGLE's translator reject the whole shader, the compile exception aborted `OnOpenGlInit` before the other programs were built, and the entire viewport rendered black. The former hardcoded showcase now lives in `samples/EngrCAD.Demo` (console app calling the Scene API — the consumer experience). **The live-modeling loop is `dotnet watch` + hot reload, not a custom CLI**: `EngrCad.ShowLive(Func<Scene>)` registers a `MetadataUpdateHandler` (`HotReload.cs`) that re-invokes the factory after each patch and calls `SetScene` (camera preserved; factory exceptions keep the last good scene and surface in the overlay; camera pose persists to a temp file across rude-edit restarts, 30-min freshness). `EngrCad.Run(args, factory)` wraps model programs: no args → ShowLive, `--view` → static Show, `--export part.step|part.obj` → headless export (STEP per B-Rep-representable part via `Part.Source`, OBJ merged with transforms), `--render out.png` → **headless offscreen render** (no window). **Host configuration**: `EngrCad.Configure()` fluent builder (`WithTitle/WithQuality/WithRenderSize/WithLog/WithViewportReady` → terminal `Run/Show/ShowLive/RenderToImage`) over a plain `EngrCadOptions` POCO — bindable as `IOptions<EngrCadOptions>` and seeded via `Configure(options)`, with an `IEngrCadLog` Info/Error seam (default `EngrCadLog.Console`; `EngrCadLog.From(delegates)` adapts to `ILogger` — deliberately NO Microsoft.Extensions dependency and no separate Extensions project, adapter pattern documented in the Viewer README). Mesh-quality precedence is explicit `Scene` options > `EngrCadOptions.Quality` > `MeshQuality` defaults, implemented by `Scene.HasExplicitOptions`/`ResolveQuality(fallback)`/`PreMesh(fallback)` and honored by display, hot reload, `--export .stl/.obj`, and `--render` (which also uses the configured image size); all Run/export/render reporting and the live-reload overlay messages route through the log seam. Offscreen rendering (`EngrCad.RenderToImage(scene, path, w, h, camera?, style?, sectionAxis?, sectionOffset?)`/`CanRenderToImage`, backed by `OffscreenRenderer` + a direct EGL-pbuffer `EglContext` over Avalonia's ANGLE natives + `PngWriter`) is the viewer self-verification loop — tests and agents render a scene and inspect pixels instead of screenshotting the demo app; it now has **full parity with the window** (display modes, global view style, axis sections with their SDF isoline overlays, translucent ordering — only the selection highlight stays neutral). Render options reach headless renders three ways with one precedence: `EngrCadOptions.RenderStyle`/`SectionAxis`/`SectionOffset` (builder `WithViewStyle`/`WithSection`), overridden by the `--render-style points|wireframe|shaded|shaded-edges` and `--section x|y|z <offset>` CLI switches (parsed up front, usage errors exit 2 before any GL), mirrored by optional `EngrCadBuilder.RenderToImage` parameters. Infra lesson: Avalonia's ANGLE `av_libglesv2.dll` exports EGL entry points under an `EGL_` prefix (not the standard `egl*`), so a hand-rolled EGL binding must try both spellings. Offscreen renders use 24-bit depth (16-bit + `PolygonOffset(1,1)` bit sawtooth notches out of silhouettes) and 2× supersample + box-downsample for AA (MSAA pbuffers are unreliable under ANGLE/WARP). **`RenderCore.cs` is the single source of truth for both render paths** (`ViewerShaders` — one shader set, offscreen sets neutral uniforms; `CameraMath` — scene-scaled `FrustumPlanes` replaced the fixed 0.1/200 planes and 110/120-unit distance clamps in BOTH paths, so large scenes frame fully; `RenderGeometry`) — never fork shader or camera code between window and headless again. `samples/EngrCAD.LiveDemo` is the parametric-bracket demo (`dotnet watch --project samples/EngrCAD.LiveDemo`); verified end-to-end: body edits hot-apply in ~0.1–0.7 s without restart. **Docs site**: DocFX under `docs/` (getting started + 13 example pages + API reference from XML docs) with **executable examples** — `tools/EngrCAD.DocsGen` extracts ` ```csharp render:<id> ` fences from the markdown, compiles/runs each via Roslyn scripting (snippet must define `Scene scene`), renders through `EngrCad.RenderToImage`, and writes `docs/examples/images/<id>.png`; nonzero exit on any snippet failure, so examples can't rot (44 snippets, 34 rendered; PNGs committed so the site builds without a GPU; `render:` fences take per-snippet `section:<x|y|z>,<offset>` and `style:<name>` options so cutaway examples use real section planes — with SDF isolines on the cut — instead of boolean-cut fakes). `.github/workflows/docs.yml` builds + deploys via actions/deploy-pages (windows runner for WARP rendering; Chris must set repo Settings → Pages → Source: GitHub Actions before first deploy). Remaining big rocks: SIMD passes, nuget.org publish (`Directory.Build.props` URLs still placeholders — a real remote exists at github.com/veggielane/EngrCAD).

- .NET SDK 10.0.302 installed **user-local** at `%USERPROFILE%\.dotnet` (win-arm64), on the user PATH with `DOTNET_ROOT` set. Build with `dotnet build EngrCAD.slnx`, test with `dotnet test EngrCAD.slnx`.
- Git repository initialized; commit only when Chris asks.
- Target framework: **.NET 10 (LTS)** via `Directory.Build.props`.
- **NuGet**: all `src/*` projects pack (shared metadata — `Version 0.1.0`, MIT, package READMEs — lives in `Directory.Build.props`; license/URLs are placeholders Chris must confirm before any nuget.org push). `src/EngrCAD` is a meta-package whose ProjectReferences become package dependencies (viewer kept separate so headless consumers don't pull Avalonia). Local feed workflow: bump `<Version>`, `dotnet pack EngrCAD.slnx -c Release -o C:\Users\chris\nuget-local`; the folder is registered as source `engrcad-local`, so consumers just `dotnet add package EngrCAD` (+ `EngrCAD.Viewer`). Note: NuGet caches by version — after repacking the *same* version, delete the cached copies under `%USERPROFILE%\.nuget\packages\engrcad*\<version>` (or just bump the version).

## Architecture

Three engines with different mathematics and data structures, plus interop and query layers on top of a shared core.

### Core (foundation for everything)
- Zero-allocation math: `readonly struct` `Vector3d`, `Matrix4x4d`, quaternions, `AABB`
- Central tolerance/epsilon policy for robust floating-point comparison
- Spatial acceleration: BVH and octree (used by all engines and by the query layer)

### Mesh engine (discrete)
- Half-edge data structure for O(1) topology traversal
- Algorithms: booleans, decimation, subdivision
- Bulk data stored data-oriented (SoA) for cache locality

### Implicit engine (volumetric)
- SDF evaluator: `(x, y, z) → distance`
- Primitives (sphere, box, cylinder, …) and operators (union, intersect, smooth blend) composed as an AST
- SIMD-batched evaluation; later, compilation of C# expression trees down to SDF graphs / IL / compute shaders

### B-Rep engine (parametric) — hardest, built last
- Geometry: planes, cylinders, cones, NURBS surfaces/curves
- Topology wrapper referencing geometry: Solid → Shell → Face → Loop → Edge → Vertex
- Surface–surface intersection engine; booleans and filleting on top

### Interop layer — conversion triangle complete
- Implicit → Mesh: `SurfaceNets.Polygonize` (manifold dual contouring)
- B-Rep → Mesh: `BRepTessellator.Tessellate` (shared edge polylines + ear clipping + welding)
- Mesh → Implicit: `MeshSdf` (BVH nearest-triangle + angle-weighted pseudonormal sign; requires closed mesh; composes with all `Sdf` operators)

### Query layer (LINQ) — v1 implemented
- `SpatialCollection<T>` (items + bounds expression + BVH) with `AsQueryable()`: a custom `IQueryProvider` rewrites expression trees, recognizes `SpatialPredicates` clauses (`.Within(box)`, `.WithinDistance(p, d)`, `.HitBy(ray)`) applied to the registered bounds accessor, answers them from the BVH, and re-applies the full predicate over candidates (interception is a pure optimization). Residual/non-spatial queries fall back to LINQ-to-Objects. `LastQueryUsedIndex` is the diagnostic.
- IMPORTANT: query predicates use the by-value `SpatialPredicates` extension methods, NOT `Aabb.Intersects` — expression trees cannot contain calls with `in` parameters, which the kernel API uses.
- Topology-traversal LINQ exists on mesh handles (`vertex.OutgoingHalfEdges().Where(...)`, `face.AdjacentFaces()`, `Face.Bounds`).
- Future: metadata indexes for B-Rep feature queries (cylindrical faces by radius), expression-tree→SDF compilation.

## Planned solution layout

```
EngrCAD.sln
src/
  EngrCAD.Core/       math structs, tolerances, AABB, BVH, octree
  EngrCAD.Mesh/       half-edge mesh engine
  EngrCAD.Implicit/   SDF primitives, operators, evaluator
  EngrCAD.BRep/       parametric geometry + topology
  EngrCAD.Interop/    conversions between representations
  EngrCAD.Query/      IQueryable provider, spatial/topology LINQ
  EngrCAD.Modeling/   unified Shape API (rep chosen at the end via To*)
  EngrCAD.Viewer/     Avalonia + Silk.NET (OpenGL) viewer *library* (EngrCad.Show)
samples/
  EngrCAD.Demo/       console showcase: builds a Scene, calls EngrCad.Show
  EngrCAD.LiveDemo/   live-modeling loop: EngrCad.Run + dotnet watch hot reload
tests/
  EngrCAD.Core.Tests/ (one xUnit test project per src project)
  ...
```

Kernel projects (`Core`, `Mesh`, `Implicit`, `BRep`, `Interop`, `Query`) must stay free of UI/rendering dependencies; only `EngrCAD.Viewer` references Avalonia/Silk.NET.

## Performance mandates (non-negotiable in kernel code)

- Math types are `readonly struct`; hot paths allocate nothing on the heap
- Use `Span<T>`/`Memory<T>`; temporaries come from `ArrayPool<T>` or `stackalloc`, never `new` per call
- Use `System.Runtime.Intrinsics` (SIMD) for SDF evaluation, ray/primitive intersection, and other batch kernels
- Bulk mesh data uses structs-of-arrays, not arrays-of-objects
- Never compare floats with `==`; all comparisons go through the central tolerance policy in `EngrCAD.Core`
- **The epsilon ladder** (audited repo-wide; every site is documented in place): new epsilons must pick a tier — **1e-9 absolute weld** (= `Tolerance.Default.Linear`: exactly-constructed shared geometry — tessellation welds, seam breaks, clone dedupe) / **1e-7 seam** (geometry built independently on two sides of one curve — `SealSeams`, boundary-vs-grid matching, MeshWelder zip) / **1e-6 inverse-evaluation** (named: `FaceGeometry.InverseEvaluationTolerance` — Gauss–Newton pullback; three decades looser than weld, do not tighten) / **scale-free algorithmic guards** (relative machine-epsilon degeneracy tests, exact-zero division guards, exact-zero *semantic* tests — these deliberately bypass `Tolerance` and say so in a comment). Never invent a fresh literal. Shewchuk `Predicates2d` error-bound constants are the algorithm — off limits.

## Roadmap (bottom-up — do not skip ahead)

1. **Core math & spatial acceleration** ✅ done — `Tolerance`, `Vector2d`/`Vector3d` (implicit conversion from tuples), `Matrix4d` (column-vector convention), `Quaterniond`, `Aabb`, `Ray3d`, `Bvh` (static, median-split; serves box/ray/nearest plus t-ordered `QueryAll`, tree–tree `QueryOverlap` (the imprint-boolean broad phase), allocation-free struct-metric `Nearest<TMetric>` — `MeshSdf.Evaluate` measured 104 B/call → 0 — and exposes per-node contiguous ranges via `NodeView`/`ItemsInTreeOrder`, so future hierarchies need not fork the way `MeshWindingNumber` did), `Octree` (dynamic), **`Predicates2d`** (adaptive-exact Shewchuk `Orient2d`/`InCircle` — sign exact for ALL finite doubles, stackalloc expansions, locked against BigInteger ground truth incl. the Kettner ulp-grid where naive determinants provably lie; its internal error-bound constants are the algorithm — never "clean them up"), **`Geometry2.Arrangement2d`/`ArrangementCell2d`** (2D segment arrangement: exact-incidence decisions via the predicates, rounded coordinates under a snap tolerance, tightest-turn cell extraction with CCW outers + CW holes — the sketch-engine robustness foundation; FaceSplitter's planar tracing is the same dance and could one day sit on it, deliberately not unified — boolean-critical), **`IndexPriorityQueue`** (O(1) id→slot, decrease-key; upgraded `MeshDecimator`'s lazy PQ — equal-or-better quality; lesson: never key dictionaries by packed `(min<<32)|max` longs — `long.GetHashCode` is `lo ^ hi`, collapsing structured edge keys into a handful of buckets, measured 4× whole-algorithm slowdown; tuple keys hash properly), **`ProgressCancel`** (optional-trailing-param cooperative cancellation, wired into Polygonize/Decimate), **`ParallelFor.Blocks`** (deterministic block-parallel-for — each index writes only its own slot; SurfaceNets sampling ~1.6× and dense GridSdf bake ~2.5–5× faster, output bit-for-bit identical), **`ConvexHull2`** (monotone chain) + **`Fitting2d`/`Fitting3d`** (`MinAreaBox`/`MinCircle` (Welzl)/`FitBox3` (PCA heuristic)/`FitPlane` → `Frame3d`), integer grid types (`Vector2i`/`Vector3i`/`Interval1i`/`AxisAlignedBox3i`; `DVector`/`MemoryPool` deliberately not adopted — List/ArrayPool cover the call sites), **`Frame3d`** (rigid pose: origin + orthonormal basis; `FromXY`/`FromNormal`/`FromZX`(STEP AXIS2)/`FromOrthonormal`, `ToWorld`/`ToLocal` for points/vectors/rays, `Then`/`Inverse` (transpose-exact), `ToMatrix`, `Renormalized`. **`FromNormal` delegates to `Vector3d.ArbitraryPerpendicular`** — the codebase's single arbitrary-perpendicular convention, locked by tests; the phase-alignment lesson is orthogonal: when a *natural* frame exists (a cylinder's own), use it, never an arbitrary one. Adopted by `SweptSurface.Frame`, `StepReader.Axis2`, sweep caps; `SketchPlane` wraps it; frame ctors on `PlaneSurface` + all conics; **`BrepQueries.Frame(face)` + `SketchPlane.On(face)` enable sketch-on-face** end-to-end through the Shape API)
2. **Mesh engine** ✅ done — half-edge structure (`HalfEdgeMesh` + `Vertex`/`HalfEdge`/`Face` handles for LINQ traversal), manifold-validating `Build`, boundary loops, metrics (area/volume/Euler), primitives (box, uv-sphere, n-gon-capped cylinder), triangulation, Loop subdivision, booleans (`MeshBoolean`: BSP/csg.js clipping + seam zipping for closed results; exact-intersection rewrite is future work), QEM decimation (`MeshDecimator`: quadric edge collapse with link-condition and normal-flip guards, boundary preserved exactly), `RenderMesh` extraction, OBJ export, plane cutting (`MeshPlaneCut.Cut`: keeps the side the normal points away from, exact canonical-direction crossing points, Sutherland–Hodgman face clipping — non-convex crossing faces are Newell-plane triangulated before clipping, not fanned from vertex 0 — returns ordered boundary loops, optional earcut caps with collinear-chord zip), winding-number classification (`MeshWindingNumber`: exact Van Oosterom–Strackee solid-angle sum + Barill/Jacobson order-2 multipole `FastWindingNumber` over its own contiguous-range hierarchy — built before `Bvh` exposed per-node ranges; could migrate now, stays put with locked tests — robust inside/outside for non-watertight meshes); viewer renders meshes
3. **Implicit engine** ✅ done — `Sdf` AST (`Evaluate`/batch/`Normal`/conservative `Bounds`), primitives (sphere, box, cylinder, torus, capsule, half-space, gyroid lattice), operators (union/intersect/subtract with `|`/`&`/`-` overloads, smooth blends, offset, shell, translate/rotate/scale; **N-ary** `Sdf.Union`/`Intersection`/`SmoothUnion` as flat single-node loops — children evaluated once per query; the N-ary smooth union folds the pairwise polynomial smooth min, so it matches chained binary exactly and reduces to hard min outside the blend band; `Sdf.Blend(a, b, d, Falloff.Wyvill|Exponential)` falloff-kernel blend whose bump is bounded by the blend distance — Wyvill's compact support makes it *exactly* union outside the band; smooth-op bounds clamp their expansion at 0 for blend ≤ 0; **sampled grids**: `Sdf.Sampled(cellSize[, region][, lazy])` bakes any `Sdf` to a dense or lazy 16³-block trilinear grid through the batch `Evaluate` seam — exact at nodes, O(h²) between, outside the box = boundary value + distance-to-region — the standard acceleration for expensive ASTs like `MeshSdf`); `SurfaceNets.Polygonize` in Interop converts implicit→mesh (manifold variant: one vertex per inside-corner component per cell). Future: SIMD batch evaluation, expression-tree→SDF compilation for the Query layer
4. **B-Rep engine** 🔶 modeling operations done — curves (`Line3d`, `Circle3d`, `Ellipse3d`, **`Parabola3d`/`Hyperbola3d`** (focal / cosh-sinh parameterizations completing the analytic conic family; parabola has closed-form arc length) and **`OffsetCurve3d`** (planar offset as first-class geometry, exact O′ = (1 − dκ)·C′ — |d| vs curvature radius deliberately unvalidated, as OCCT), with **`Curve3d` exposing virtual `DerivativeAt`/`SecondDerivativeAt`** overridden exactly by every analytic curve and wrapper; `NurbsCurve` with exact rational conics, **exact analytic derivatives** (`DerivativeAt`/`SecondDerivativeAt` via The NURBS Book A2.3 + the rational quotient rule; `TangentAt` overridden — the finite-difference note below now applies only to curves without an exact override) and **`NurbsCurve.InterpolatePoints`** (chord-length parameterization, natural-end open cubic via tridiagonal Thomas solve, or C2-seam closed periodic; exact pass-through), `ReversedCurve`/`TransformedCurve` wrappers with `Underlying` for sampling rules), surfaces (`PlaneSurface`, `CylinderSurface`, `SphereSurface`, `NurbsSurface`, `ExtrudedSurface`, `RevolvedSurface` with angle, `SweptSurface` with rotation-minimizing frames), topology (`BrepSolid`→…→`BrepVertex` with `Validate` + Euler–Poincaré incl. genus), `SolidFactory`: `MakeBox`/`MakeCylinder`/**`MakeCone`** (frustum or apex — the side is an exact `RevolvedSurface` of a slanted line generator, deliberately NOT a new surface type: the revolved-band machinery already provides projection/tessellation/intersection/wrap-split/STEP; rim circles built on the surface's own radial frame stay phase-aligned with u=0; a zero radius makes that end a pole like sphere poles) + **`Extrude` (incl. shear + hole profiles), `Revolve` (full or partial turn, holes on partial, pipe elbows from closed profiles), `Sweep` (RMF along open paths, holes)** over `Profile` (planar closed chain; winding auto-corrected). **Mesh engine additions**: `ConvexHull` (quickhull: extent-scaled epsilon for all visibility tests, coplanar points absorbed by consistent triangulation, degenerate inputs throw, output passes manifold-validating Build) and `HalfEdgeMesh.Transformed(Matrix4d)` (negative-determinant transforms flip winding so meshes stay outward-oriented). **`Shape.Cone`** (Native all three reps), **`Shape.Hull`** (mesh-construction: B-Rep-Impossible, implicit/mesh Bridged via quickhull over tessellated vertices — exact for polyhedral operands), **`Shape.Mirror(point, normal)`** (Householder reflection through the transform node; implicit-**exact** — the compiler decomposes improper similarities as m = (m·FlipZ)·FlipZ, lowers at identity, wraps `Sdf.Mirror`, places the proper remainder; mesh exact with winding flip; B-Rep Native for box/cylinder/extrude/sphere/torus/cone — mirrored cylinders keep exact `Circle3d` rims because FlipZ fixes the rim plane — but mirrored revolve/sweep/rim/drill stay Impossible in v1, exact via mesh/SDF instead). `BRepTessellator` handles planar (multi-loop, earcut) /cylinder/extruded/revolved/swept faces with edge-consistent grid sampling and seam zipping. Notes: default `Curve3d.TangentAt` uses 2nd-order one-sided differences at domain ends (sweep frames are sensitive to start-tangent error); `PolygonTriangulator` is a faithful mapbox-earcut port — its collinear filtering can merge exactly-collinear boundary runs, which the tessellator's zip pass repairs. **Surface–surface intersection done** (`SurfaceIntersection.Intersect(a, b, region)`): analytic curves for plane/plane (line), plane/cylinder (circle, ellipse, or parallel lines), plane/sphere, sphere/sphere, parallel cylinders; general marching tracer (grid seeding + BVH pairing + damped Gauss–Newton refinement + tangent-predictor/Newton-corrector stepping, periodic-aware, multi-branch, closed-loop detection) for all other pairs, returning `PolylineCurve3d` (exact at vertices, chordal between). New curves: `Ellipse3d`, `PolylineCurve3d`. **Trimming groundwork done**: `Surface.TryProjectPoint` (inverse evaluation — exact for plane/cylinder/sphere, Gauss–Newton otherwise), `FaceGeometry` (curve pullback into parameter space with periodic-u unwrapping, `LoopSignedArea`, point-in-face by upward-v ray parity with periodic segment compaction), `FaceSplitter.SplitByClosedCurve` (closed interior curves → face-with-hole + disk sharing one manifold edge; `createDisk:false` hands the edge's second use to another face). End-to-end drill rehearsal in `DrillTests`: box + intersection circles + cap splitting + bore-wall assembly → valid genus-1 solid with exact volume. **Open-curve face splitting done**: `TopologyEditor.SplitEdge` (splits an edge at a parameter, patching every using loop — neighbor faces stay consistent), `FaceSplitter.SplitByCurve` (full parameter-space arrangement: polyline crossing detection + 2×2 Newton refinement, boundary-edge splitting with endpoint-vertex reuse, interior curve segments as shared two-use edges via `CurveSegment`, sub-face tracing by smallest-clockwise-turn walking, CW loops assigned as holes to their smallest containing CCW loop). Verified through whole solids: single cut (exact volume), cross cut through a split-created vertex (4 quadrants), annular cap cut beside its hole (hole follows the correct side). **Trimmed-face tessellation + cut-through-hole booleans done**: `TrimmedFaceTessellator` (Interop) renders faces whose loops don't cover the surface's grid domain — exact-coordinate ear clipper (shortest-diagonal ears, ~1e-9 jitter blocking band, hole bridging) + strip-zip/pole-fan for band regions + surface-exact midpoint refinement (Steiner-by-refinement, no upfront classification); routing is a two-sided 3D match of loop samples against the natural grid boundary (the welding invariant, so existing faces can't misroute; trimmed failure falls back to grid). **Never earcut pulled-back curved-face loops** — `PolygonTriangulator` filters exactly-collinear vertices and iso-parameter runs ARE exactly uv-collinear but not 3D-collinear: a dropped sample is an unzippable crack, and jittered earcut breeds zero-area folds that refine into non-manifold welds. `FaceSplitter` hardened for the boolean path: tolerant partial-curve pullback runs (extrapolated run-end seeds), crossing refinement by 3D curve–curve Gauss–Newton (projected-uv Newton fails near bounded domain edges), inclusive crossing seeds (cuts through split-created vertices), and **reversed-face splitting** (CW-aware tightest-turn tracing, `IsReversed` preserved — cascaded booleans on boolean output need this). `BrepBoolean`: both sides of a shared closed intersection curve take a mandatory seam break at `Domain.Start` (the wrap-split side anchors its seam vertex there), and probe points prefer the largest fragment triangle's centroid (sliver centroids sit within the SDF's sagitta of the other solid's curved surface — misclassification). End-to-end: drilled box minus a slot narrower than the bore → Validate-clean genus-3 solid, closed manifold tessellation, volume within 7e-4 of analytic. Two-ring band faces with hole loops tessellate properly (seam in the largest u-gap, unrolled + ear-clipped with hole bridging; tracer `PolylineCurve3d` edges are sampled at exact vertex parameters — uniform sampling lands on chords ~1e-4 off-surface); `Refine` terminates via a monotone-decrease enqueue rule with fail-safe fallback. Remaining trimmed-face gaps: pole-bands-with-holes and |winding|>1 fall back to grid, no Delaunay quality flips. **Sphere-piercing booleans fixed** (was: silent wrong geometry when a sphere pierces box faces so every intersection curve is a closed circle interior to a face) — lessons: `BrepQueries.Bounds` must sample surface domains, not just edges (a hemisphere's equator-only edges hid the dome from the face-pair prefilter); tracer-clipped arcs can NEVER be refined against a boundary they stop short of — promote full-turn revolved spheres to exact analytic plane∩sphere circles (`TrySphereCarrier`, generator sampled, never `Underlying`); closed interior curves need mandatory seam breaks honored on BOTH sides (`SplitByInteriorClosedCurve` builds matching arc pairs); period-wrapping traced loops are band boundaries paired bottom-to-top by v, not area-classified holes. `Shape.Drill` validates up front (overlap/tangency per point pair; far-face coplanarity at B-Rep lowering via the `DrillShape` node) instead of failing deep in tessellation. **Cross-drill-through-bore booleans fixed** — the root cause was NOT the wrap-split but silent no-op pullback: tracer polylines sampled at uniform parameters sit step²/8r (~1e-4) off the carrier surface, past the 1e-6 inverse-evaluation tolerance, so `PullCurveRuns` returned zero runs and unsplit bands were whole-classified away. Lessons now baked in: **tracer polylines are on-surface only at their vertices — ALL pullback/parity/crossing sampling goes through one shared rule** (`FaceGeometry.ExactSampleParameters`: polylines → vertex parameters, `CurveSegment`-over-polyline → mapped base vertices, else uniform), never per-call-site patches; a **non-planar wrap cut cannot become a domain boundary** — `SplitBandByNonPlanarWrapCurve` sub-bands keep the original surface and rely on trimmed-face tessellation (the trim-the-surface rule applies only to constant-parameter cuts); multiple wrapping cuts on one band decide containment **per fragment by loop parity** (every fragment shares the full carrier, so every cut pulls back onto all of them). Equal-radius perpendicular cylinders (tangent bicylinder) are rejected loudly (overlapping v-ranges). **Automatic B-Rep booleans done** (`BrepBoolean.Union/Intersection/Difference` in Interop): per-face-pair `SurfaceIntersection`, seam-aligned splitting (each side takes the other's crossing params as mandatory breaks so tessellation welds), fragment classification by probing the other solid's `MeshSdf` (hybrid trick), reversed faces for subtracted tools (`BrepFace.IsReversed`, tessellator flips), wrap-splitting of periodic bands (`SplitBandByWrapCurve`: bore wall → two exactly-reconstructed sub-bands), and circle-extrusion→cylinder promotion for exact bore circles. Boolean output is **topologically sealed** (`TopologyEditor.SealSeams`: stale-use pruning, vertex unification, seam-edge merging; Difference re-winds reversed faces' loops) — results pass `Validate()` and Euler–Poincaré with correct genus. **Filleting**: `Filleting.FilletEdge` for closed circular rims (planar cap ↔ coaxial cylinder band) via exact quarter-torus `RevolvedSurface`; general fillet chains with corner patches are future work. **STEP export + import**: `StepWriter.Write/WriteFile` (AP214 `MANIFOLD_SOLID_BREP`, analytic surfaces/curves incl. rational NURBS, wrapper-curve simplification; swept surfaces not exportable) and `StepReader.Read/ReadFile` (full Part 21 parser + AP214 entity mapping back to `BrepSolid`; round-trips everything the writer emits — topology shared by entity identity, edge domains reconstructed exactly from vertex positions (closed-form conic phases, Newton with exact derivatives for B-splines), `SURFACE_OF_REVOLUTION` angle/generator-trims recovered from rails/rims by **bisection on the exact (radius, axial) profile residuals** (root solves — distance minimization stalls at √ε ≈ 1e-8, past weld tolerance); mm assumed, unknown entities skipped with diagnostics; verified on box/cylinder/drilled-plate genus-2/revolves/booleans/fillets at ~1e-12 volume agreement; known-lossy: `TransformedCurve(NurbsCurve)` exports as sampled degree-1 polylines). **Viewer picking**: click-select via unprojected ray + per-object triangle BVH + Möller–Trumbore, selection highlighted and named in the title bar (note: Avalonia's pointer stack ignores legacy synthetic `mouse_event` clicks — test picking with real input or by calling `Pick` directly). Remaining future work: coplanar/tangent boolean cases, general fillets (corner patches — the trimmed-band tessellation blocker is now gone), NURBS surface export, STEP unit scaling + conical/toroidal surface synthesis. Numerical notes from the Modeling work: the generic `Surface.TryProjectPoint` wraps (not clamps) periodic u so Newton can cross the seam; plane⊥cylinder intersection circles are **phase-aligned to the cylinder's frame** (arbitrary-perpendicular frames caused tessellation cracks on rotated bores — and `DrillTests` winding depends on this); `BrepBoolean.ProbePoint` routes full-period-wrapping loops to the band path (projection jitter gives degenerate sliver loops nonzero area, and the planar path would probe on the fragment boundary where the SDF is 0); `SurfaceIntersection.Promote` sanity-checks that the generator lies on the candidate cylinder (a `TransformedCurve`-wrapped circle's `Underlying` is the untransformed circle).
5. **Interop completion** — remaining conversions, mesh↔SDF, robustness passes

The Query layer and Viewer grow alongside each engine as it lands, not as separate phases.

## Conventions

- C# `LangVersion` latest, `Nullable` enabled, `ImplicitUsings` enabled, file-scoped namespaces
- Root namespace `EngrCAD.*`, matching project names
- Tests: xUnit; every geometric algorithm gets tolerance-aware assertions
- Central build props (`Directory.Build.props`) for shared settings once scaffolded
